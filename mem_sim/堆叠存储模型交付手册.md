# 堆叠存储模型交付手册

> 项目：`hbm_sim`  
> 支持标准：HBM3、HBM4、LPDDR5、LPDDR6  
> 实现语言：C++20  
> 模型粒度：周期级、命令级、事务数据级、行为级 PHY  
> 文档定位：保持完整章节的项目说明书，涵盖使用流程、必要原理和结果判断；代码、配置和当前测试结果是最终事实来源。



<a id="toc"></a>

## 目录

- [0. 文档用途与模型边界](#chapter-0)
- [1. 总体设计](#chapter-1)
  - [1.1 设计目标](#section-1-1)
  - [1.2 设计原则](#section-1-2)
  - [1.3 总体分层](#section-1-3)
  - [1.4 主动模型与被动模型](#section-1-4)
- [2. 模块详细设计](#chapter-2)
  - [2.1 配置模块](#section-2-1)
  - [2.2 Frontend 模块](#section-2-2)
  - [2.3 MemorySystem 与多 Stack 路由](#section-2-3)
  - [2.4 Memory Controller](#section-2-4)
  - [2.5 Mem PHY](#section-2-5)
  - [2.6 Mem Stack 与存储后端](#section-2-6)
  - [2.7 异步请求—响应接口](#section-2-7)
  - [2.8 统计、输出与验证](#section-2-8)
- [3. 平台与模型划分](#chapter-3)
- [4. 四种标准模型](#chapter-4)
- [5. 构建与部署](#chapter-5)
  - [5.1 依赖](#section-5-1)
  - [5.2 Clang 推荐构建](#section-5-2)
  - [5.3 G++ 构建](#section-5-3)
  - [5.4 Make 兼容入口](#section-5-4)
- [6. 仿真模型使用说明](#chapter-6)
  - [6.1 标准流程](#section-6-1)
  - [6.2 四标准快速运行](#section-6-2)
  - [6.3 合成流量](#section-6-3)
  - [6.4 多 Stack](#section-6-4)
  - [6.5 存储后端选择](#section-6-5)
  - [6.6 Trace 与数据正确性](#section-6-6)
  - [6.7 自定义配置](#section-6-7)
- [7. 仿真输出与查看方法](#chapter-7)
  - [7.1 stdout](#section-7-1)
  - [7.2 可选产物](#section-7-2)
  - [7.3 任意仿真的离线可视化](#section-7-3)
- [8. 平台与模型验证](#chapter-8)
  - [8.1 当前模型完成度](#section-8-1)
  - [8.2 可信度证据链](#section-8-2)
  - [8.3 默认回归](#section-8-3)
  - [8.4 项目内验证](#section-8-4)
  - [8.5 Ramulator2 差分](#section-8-5)
    - [8.5.1 DRAMsim3 辅助对照](#section-8-5-1)
  - [8.6 性能曲线和不确定性](#section-8-6)
  - [8.7 可行性结论的表述](#section-8-7)
- [9. 审计与完成情况](#chapter-9)
  - [9.1 来源分级](#section-9-1)
  - [9.2 当前已完成](#section-9-2)
  - [9.3 尚需器件级工作](#section-9-3)
- [附录 A：配置参数说明](#appendix-a)
  - [A.1 实验和输入](#appendix-a-1)
  - [A.2 Profile、组织与时钟](#appendix-a-2)
  - [A.3 Controller、映射和多 Stack](#appendix-a-3)
  - [A.4 PHY/DFI](#appendix-a-4)
  - [A.5 Refresh、RFM、低功耗和协议模式](#appendix-a-5)
  - [A.6 Timing 参数](#appendix-a-6)
  - [A.7 存储、物理、功耗、热和 ECC](#appendix-a-7)
  - [A.8 输出和验证参数](#appendix-a-8)
- [附录 B：普通输出指标说明](#appendix-b)
  - [B.1 身份、周期和完成](#appendix-b-1)
  - [B.2 性能、延迟和队列](#appendix-b-2)
  - [B.3 行行为和命令](#appendix-b-3)
  - [B.4 数据、后端与拓扑](#appendix-b-4)
  - [B.5 PHY 与 DFI](#appendix-b-5)
  - [B.6 功耗、热、ECC、RAS 和模式](#appendix-b-6)
  - [B.7 多 Stack 和验证动态字段](#appendix-b-7)
  - [B.8 补充精确输出字段](#appendix-b-8)
  - [B.9 带宽、延迟、传输率、功耗和热的联合分析实例](#appendix-b-9)
- [附录 C：四标准运行用例](#appendix-c)
  - [C.0 四标准通用查看工具和三后端审计](#appendix-c-0)
  - [C.1 HBM3](#appendix-c-1)
  - [C.2 HBM4](#appendix-c-2)
  - [C.3 LPDDR5](#appendix-c-3)
  - [C.4 LPDDR6](#appendix-c-4)
- [附录 D：新 HBM4 自定义用例](#appendix-d)
  - [D.1 定义研究问题和不可变条件](#appendix-d-1)
  - [D.2 完整自定义配置文件](#appendix-d-2)
  - [D.3 配置审查和小规模预运行](#appendix-d-3)
  - [D.4 正式仿真命令和预计效果](#appendix-d-4)
  - [D.5 模式选择、命令和预期差异](#appendix-d-5)
  - [D.6 仿真过程查看](#appendix-d-6)
  - [D.7 完整指标和参数查看](#appendix-d-7)
  - [D.8 可视化运行和分析](#appendix-d-8)
  - [D.9 结果记录模板](#appendix-d-9)
- [附录 E：完整检查清单](#appendix-e)
- [附录 F：代码与文档索引](#appendix-f)



---

<a id="chapter-0"></a>

## 0. 文档用途与模型边界

本手册可作为从构建、配置、运行到查看和验证结果的连续阅读入口。架构、性能解读、
存储后端、热图原理及测试入口均在对应章节说明，不要求先阅读专题指南才能完成常用操作。

手册与专题指南按说明深度分工：

| 内容 | 本手册保留 | 专题指南补充 |
|---|---|---|
| 架构与数据路径 | 第 1～3 章的分层、模块职责及接口关系 | [项目指南](文档/项目指南.md)的代码索引与扩展位置 |
| 配置与自定义 | 第 6 章、附录 A/C/D 的常用参数、联动注意事项和运行示例 | [cfg指南](文档/cfg指南.md)的完整参数字典、单位与推导规则 |
| 存储后端 | 2.6、6.5 的选择依据、容量边界及读写查验流程 | [项目指南](文档/项目指南.md)的后端实现与持久文件说明 |
| 输出与性能 | 第 7 章、附录 B 的普通指标、查看方法及带宽/延迟分析 | [输出指南](文档/输出指南.md)的全输出字段、文件列说明及深入分析 |
| 热图 | 2.6、7.3、B.9 的能量到温度、坐标含义及图像解读 | [项目指南](文档/项目指南.md#thermal)的完整坐标计算与耦合细节 |
| 测试与可信度 | 5.4.3、第 8～9 章的测试入口、结果判断和验证边界 | [项目指南](文档/项目指南.md#tests)的目标依赖与[审计](文档/审计.md)的证据说明 |

附录 A/B 保留常用输入、普通输出的说明和必要分析；完整参数表及诊断字段表分别由
cfg指南、输出指南维护。简要说明可以出现在操作所需的位置，完整字典不在多处重复维护。

本项目由“仿真平台”和“堆叠存储模型”两部分组成。平台负责流量、配置、调度、
统计、验证和输出；堆叠存储模型由可选的行为级 Mem PHY 与 Mem Stack 组成，负责
协议适配、命令/数据流水、真实 payload、物理位置、功耗、热和 ECC 等语义。

本项目包含：

- HBM3、HBM4、LPDDR5、LPDDR6 的统一配置和运行入口；
- 多 stack、多 channel、pseudo-channel、SID、rank、bank group、bank 的组织和路由；
- Controller、TimingEngine、刷新/RFM/PRAC、低功耗和标准专用协议行为；
- Direct/Behavioral 两种 Mem PHY 模式及 HBM/LPDDR 协议适配器；
- `sparse`、`mmap_sparse`、`chunk_file` 三种存储后端；
- 实际数据读写、掩码、期望值检查、golden image 和 SECDED shadow；
- command、response、DFI、memory image、mismatch、thermal 等可重放产物；
- 原生回归、Timing 边界、性能曲线、敏感性分析和外部参考差分工具。

本项目不是晶体管级、信号完整性级或量产器件模型。Behavioral PHY 不建模阻抗、
电压摆幅、抖动、眼图、训练算法的模拟电气收敛，不宣称通过 JEDEC/DFI 合规认证。
HBM3 与 LPDDR5 的部分默认参数来自 Ramulator2.1 可复核基线；HBM4 与 LPDDR6 采用
JEDEC-oriented 组织/协议和公开研究默认值。厂商 RL/WL、速度档、行时序、RFM/RAS、
PHY/DFI、功耗和封装热参数仍须用厂家模型或硬件结果校准。


<a id="chapter-1"></a>

## 1. 总体设计

<a id="section-1-1"></a>

### 1.1 设计目标

1. 在同一套平台内比较 HBM3/HBM4/LPDDR5/LPDDR6 的组织、时序与协议特性。
2. 支持从 host 请求到 DRAM transaction、命令、PHY 事件和真实数据落盘的完整路径。
3. 支持一颗或多颗独立 stack，统计系统级与 per-stack 性能、功耗和温度。
4. 允许替换调度器、地址映射、PHY 模式、存储后端和 timing/profile，而不重写主流程。
5. 结果可以由 CSV、memory image、验证报告和固定随机种子复核。
6. 在缺少厂家手册时，明确区分标准约束、外部参考、派生值和研究默认值。

<a id="section-1-2"></a>

### 1.2 设计原则

- **模块化**：Frontend、MemorySystem、Controller、PHY、MemoryImage、Stats 分层。
- **配置与模型分离**：标准 traits 定义能力，profile 给出参数，配置选择实验。
- **统一多标准接口**：公共请求和统计保持一致，标准差异通过 traits/semantics/adapter 表达。
- **多 stack 独立性**：每个 `[stack][channel]` 拥有独立 Controller 和 PHY 端点；每个
  stack 拥有独立 MemoryImage，数据、队列、温度和统计不串扰。
- **时序与数据并重**：Controller/TimingEngine 管命令合法性，MemoryImage 管真实 payload。
- **PHY 可选**：Direct 用于直接完成和对照；Behavioral 用于在线 FIFO、流水和协议行为。
- **可扩展后端**：存储介质实现不改变 DRAM 时序和数据正确性语义。
- **可追溯**：命令、DFI、响应、参数来源和校验结果可以导出。
- **研究建模**：研究默认值而非 JEDEC 精确值，行为级事件而非真实引脚波形。

<a id="section-1-3"></a>

### 1.3 总体分层

```text
配置/Profile/CLI
       │
       ▼
Frontend / TrafficStream / Trace parser
       │ HostRequest（可拆为多个 DRAM transaction）
       ▼
MemorySystem
  ├─ stack 选择、system→local address、ingress、QoS
  └─ [stack][channel] Controller
         ├─ read/write/priority/active/pending queues
         ├─ scheduler + row policy
         ├─ bank state + command semantics + TimingEngine
         ├─ refresh/RFM/low-power
         └─ MemPhy
              ├─ Direct：直接完成模式
              └─ Behavioral：状态机、FIFO、pipeline、HBM/LPDDR adapter
                     │
                     ▼
              per-stack MemoryImage / Mem Stack
                     ├─ MemoryBackend
                     ├─ row buffer / payload / init mask
                     ├─ floorplan / topology
                     ├─ power / thermal
                     └─ ECC shadow
       │
       ▼
HostResponse + Stats + command/DFI/response/memory/thermal artifacts
```

控制器按 channel 建立，PHY 也按 channel 接口切片建立。因此“一个 stack 一个 PHY”在
本项目中应理解为“一颗 stack 拥有一组 channel-local PHY 端点”，而不是多个 stack
共享一个全局 FIFO/状态机。HBM4 六 stack、每 stack 32 channel 时会建立 192 个
Controller 和 192 个 PHY 端点，但只有 6 个独立的 per-stack MemoryImage。

<a id="section-1-4"></a>

### 1.4 主动模型与被动模型

| 模式 | 入口 | 包含调度/时序 | 包含数据/物理/热 | 适用场景 |
|---|---|---:|---:|---|
| 主动完整路径 | `MemorySystem` | 是 | 是 | CLI、性能、协议和完整系统实验 |
| 单控制器实验 | `--single-controller` | 是 | 是 | 局部控制器实验与基准对照 |
| 被动单 stack | `StackModel` | 否 | 是 | 外部 MC 已产生合法 DRAM 操作 |
| 被动多 stack | `MultiStackMemoryModel` | 否 | 是 | SystemC/RTL/UCIe 外部驱动存储后端 |

若后期只需要“Mem PHY + Mem Stack”，可从 `MemPhy`、`MemoryImage`、后端和相关 traits
中提取；外部系统必须补上请求仲裁、命令合法性与时序责任，不能简单删除 Controller。

---

<a id="chapter-2"></a>

## 2. 模块详细设计

<a id="section-2-1"></a>

### 2.1 配置模块

配置文档由 `document.cpp` 完成选择和分层，`model.cpp` 统一联动推导与模型构建。
`config/fields.hpp` 集中定义普通字段的类型、别名和取值，以及 Timing 的
canonical name 与 nCK/ns/us 换算；配置导出和结果记录复用这些映射。
时序来源按字段保存，几何、密度、速率和时钟的交叉校验仍由模型构建流程执行。

#### 2.1.1 标准定义与能力声明

`StandardTraits`/标准负责标准身份、默认 profile、组织和协议能力，例如 HBM 的
SID/伪通道/行列双总线，LPDDR 的 WCK、分裂 ACT、DVFS 和链路保护入口。标准名字是
选择模型族的入口，不等于已经选择某家器件。

#### 2.1.2 两份标准主配置与用途分层

`configs/` 中只保留 6 份运行配置，避免“标准参数、验证对齐参数和示例参数”混在一起：

```text
configs/hbm.cfg、configs/lpddr.cfg                         两个标准家族主配置
configs/validation/{hbm3,hbm4,lpddr5,lpddr6}.cfg          四个验证配置
examples/configs/{hbm,lpddr,hbm_nstacks,lpddr_nstacks}.cfg 四个自包含 cfg Demo
experiments/local/*.cfg                                    自包含研究模型示例
examples/multistack_demos/                                 四标准多实例示例脚本
```

主配置是用户建立模型的完整参数入口；验证配置只固定与本项目或外部仿真器比较时的共同参数面；
研究模型通过复制对应主配置并在副本 `[override]` 写差异形成；四份 cfg Demo 展示单实例和
多实例的完整可运行副本，不需要额外继承层。验证配置不是 Ramulator2 或 DRAMsim3
的输入文件；外部比较脚本会分别启动两个可执行程序。
`configs/profile_index.csv` 只记录来源和适用边界，不参与解析。

四份 cfg Demo 可分别直接检查和运行：

```bash
./build-clang-debug/hbm_sim --config examples/configs/hbm.cfg --check-config
./build-clang-debug/hbm_sim --config examples/configs/lpddr.cfg --check-config
./build-clang-debug/hbm_sim --config examples/configs/hbm_nstacks.cfg --check-config
./build-clang-debug/hbm_sim --config examples/configs/lpddr_nstacks.cfg --check-config
```

前两份分别默认单 HBM Stack 和单 LPDDR device/package，后两份默认两个独立实例。四份文件
都保留对应家族主配置的全部核心字段和 default 注释，并在末尾 `[override]` 写 Demo 差异；
因此它们是可移动、可独立运行的配置，不依赖主配置路径。

仓库内部的 validation 配置在 `[meta]` 中用 `extends=相对路径` 继承主配置；解析器递归地先读
父配置再读子配置，循环继承会报错。普通用户配置不使用继承或多个 `--config`，从而避免移动
文件后相对路径失效、层级覆盖难以审计等问题。解析器支持多配置合并。完整顺序是：

```text
程序内置默认
  < 公共段 [memory_system]/[frontend]/[controller]/...
  < [family.<族>.*]
  < [standard.<标准>.*]
  < [preset.<标准>.<名称>.*]
  < [override]
  （同一层内：先加载的 config < 后加载的 config）
  < 命令行
```

因此 preset 能覆盖公共 `stack_count=1`，CLI 又能覆盖全部配置。相同 section 内重复 key
会带文件和行号报错；未知 section、未知 key、陌生 scheduler/row policy/backend/PHY
也会失败，不会静默回退。Timing 参数直接写在配置文件的内联段中。
Timing subsection 可使用 `source`，会归一化为 `timing_override_source`。`[override]` 等普通 section
应直接写 `timing_override_source`。它们与 `vendor_profile` 一起用于标注数值来源；
`--strict-timing-table` 可在仍存在必须校准的默认值时拒绝运行。
来源按字段所属文档/section 绑定，与书写前后顺序无关；其他段或子配置的无来源覆盖
默认标为 research_default，不继承原值的 vendor 声明。
模板和 resolved 快照使用 `schema_version=3`；所有配置输入执行同一套参数联动和矛盾检查。配置版本不表示 HBM、LPDDR 或模型算法版本。
`nRC >= nRAS+nRP`、速率/CK 和密度/几何一致性也由库级 `validate_spec` 检查，
一致性检查适用于所有运行模式。内部单 Channel 控制器视图保留父器件时序和
密度，并记录父 Channel 数用于校验；它不同于用户主动缩减组织几何。
`dfi_version` 同样只是 Behavioral PHY 接口语义的参考标签，
不会因改成另一个字符串而自动获得相应 DFI 信号或合规性。

建立用户配置时复制整个对应家族主配置，在副本中修改已有 `[model]`，并把差异集中写入
文件末尾已有的 `[override]`。例如：

```ini
[model]
name = my_hbm4_case  # 可读名称
base_standard = hbm4  # HBM 副本选择 hbm3/hbm4；LPDDR 从 lpddr.cfg 复制

[override]
requests = 4096  # 本实验相对标准主配置的差异
seed = 11  # 固定后可准确重放同一随机请求序列
stack_count = 2
```

该片段只是完整副本中需要编辑的位置，不是可单独保存的短配置。先执行
`--check-config --dump-resolved-config outputs/my_case/resolved.cfg`；再检查展开文件中的
standard、组织、timing 来源、PHY、刷新、后端、功耗热和输出路径。所有长期保存的实验值应
带同行注释，说明单位、物理含义及联动参数。详细步骤见 6.7 和 `configs/README.md`。

#### 2.1.3 DramSpec 与 finalize

配置由 `src/config/model.cpp` 形成主输入，schema 3 先算几何密度和最终 CK，再展开
`profiles.cpp` 的协议/刷新分支、换算 ns→nCK、应用独立覆盖与明确的 Timing 依赖。
`finalize_spec()` 最后校验 spec 并重建约束表/来源表；它自身不负责读取配置或选择速率表。
代码不得依赖“配置文件中字段出现的先后顺序”推导模型语义。

<a id="section-2-2"></a>

### 2.2 Frontend模块

Frontend 支持：

- `stream`、`random` 合成流量；
- 普通 trace：`R/W ADDRESS`；
- 带 payload 的 `data=HEX`、`expect=HEX`、`mask=HEX`；
- burst：`BW/BR ADDRESS len=... pattern/check=...`；
- 维护请求：`M REFpb|REFab|RFMpb|RFMab|REFdb ADDRESS`；
- `stack=N` 显式指定 stack-local 地址，`qos=N` 指定优先级；
- 初始化/训练/低功耗控制序列。

一个 host line 可按 `dram_transaction_bytes` 拆成多个 DRAM transaction。因此：

- `host_requests` 是上层请求数；
- `dram_transactions` 是控制器实际接收的拆分事务数；
- `reads/writes` 与完成量应按 transaction 口径解释；
- burst 的多个子事务由 `host_request_id` 聚合为一个 HostResponse。

大量请求通过 `TrafficStream` 按周期生成，不预先构造完整请求数组，避免输入规模直接
转化为宿主机内存峰值。

<a id="section-2-3"></a>

### 2.3 MemorySystem 与多 Stack 路由

MemorySystem 负责：

1. 通过 `interleaved` 或 `blocked` 选择目标 stack；
2. 将 `system_address` 转换为 stack-local `address`；
3. 请求进入每 stack ingress；
4. 按 `fcfs` 或 `strict_priority` QoS、`stack_dispatch_width` 分发；
5. 再按 channel mapper 选择 `[stack][channel]` Controller；
6. 完成子事务后重组 HostResponse。

`interleaved` 以 `stack_interleave_bytes` 为条带，适合均衡带宽；`blocked` 将连续容量
分给不同 stack，适合显式数据放置。trace 中 `stack=N` 表示输入地址已经是该 stack
的局部地址，绕过系统地址的 stack 选择，但仍执行该 stack 内的 channel 解码。

多 stack 不是一个 Controller 复制统计标签：每个 stack 的请求入口、MemoryImage、
温度、功耗和完成统计均独立；系统总带宽与容量再做聚合。

<a id="section-2-4"></a>

### 2.4 Memory Controller

#### 2.4.1 队列

- read buffer：普通读；
- write buffer：普通写；
- priority buffer：刷新、RFM、初始化和控制命令；
- active：已进入当前服务过程的事务；
- pending：已发出、等待数据/PHY/存储完成的事务。

写高/低水位控制读写模式切换；buffer 深度影响 frontend 反压和排队延迟。

#### 2.4.2 调度器与行策略

- `fcfs`：按到达先后选择可发命令；
- `frfcfs`：优先 ready/row-hit，再考虑年龄；
- `open_page`：保留已打开行；
- `closed_page`：列访问后自动关闭；
- `closed_cap`：同一行达到 `row_policy_cap` 后关闭。

调度器只能在 TimingEngine、bank 状态和 PHY 接收条件均满足时发命令。统计中的
row hit/miss/conflict 描述调度时的行缓冲关系，不等于应用级 cache 命中率。

#### 2.4.3 Bank 状态与命令语义

公共命令包括 ACT、PRE、RD、WR、RDA、WRA、REF、RFM 和模式/低功耗命令。
HBM/LPDDR 专用语义由 `dram/semantics` 与 adapter 处理。Bank 状态至少区分关闭、
激活行和刷新/维护占用；非法的列命令、重复激活或过早预充不能绕过状态检查。

#### 2.4.4 TimingEngine

TimingEngine 使用表驱动约束，按 channel、rank、bank group、bank 等作用域记录最近命令，
表达 `tRCD`、`tRP`、`tRAS`、`tRC`、`tCCD`、`tRRD`、`tFAW`、`tWTR`、`tRTW`、
刷新窗口及标准专用间隔。核心 timing 应采用边界测试验证：第 `t-1` 拍禁止，第 `t` 拍
允许，而不是只看一条长 trace 是否完成。

#### 2.4.5 Refresh、RFM/PRAC 与低功耗

刷新可选择 per-bank 或 all-bank，并提供 postpone、pull-in、credit 限制。RFM 可按 ACT 阈值触发 per-bank/all-bank 维护。它们是控制器策略模型，不应当被解读为特定厂商芯片内部 TRR 的完全复现。

低功耗支持 power-down/self-refresh 的入口、退出和延迟。显式发送 PDE/SREFEN 时，Behavioral PHY 需要对应 PDX/SREFEX 才能返回 Ready。如果此后提交普通请求，Controller 会自动将正确的退出命令注入 priority path，并在该命令真正 issue 后才清除显式低功耗状态。自定义控制序列若希望在没有普通请求时主动退出，仍应显式给出成对命令。自动空闲 residency 另是 Controller-only 抽象，不会伪造一条未发给 PHY 的 PDE/SREFEN。

<a id="section-2-5"></a>

### 2.5 Mem PHY

#### 2.5.1 功能边界

PHY 位于 Controller 与 Mem Stack 之间，将控制器级命令和数据事务转换为带时序、
通道和协议语义的行为事件：

- 接收命令、写数据和读返回；
- 建模 command/write/read FIFO 容量与流水延迟；
- 建模 Reset→Init→Training→Ready→LowPower 生命周期；
- 生成 DFI-oriented command/data beat 和 signal-like CSV；
- 调用 HBM/LPDDR adapter 完成标准专用的命令展开和校验；
- 在完成时访问 per-stack MemoryImage，并把读 payload 返回 Controller。

它不产生真实的 CK/WCK/DQ/DQS 电压波形，也不计算 setup/hold、串扰、抖动、BER、
眼图或封装信号完整性。DFI 版本字段是接口语义参考标签，事务级建模不是完整 DFI 端口合规实现。

当前多数配置使用 `dfi_version=6.0.1` 标签；HBM3 验证夹具例外，记为项目语义标签 `hbm3_project_behavioral`，因为 DFI 6.x 已移除 HBM3 支持。signal-like CSV 的 `dfi_cs_n`、`dfi_reset_n`、
`dfi_wrdata_mask` 等沿用项目历史格式，不是规范端口原名；规范使用 `dfi_cs`、
`dfi_reset`、`dfi_wrdata_dbi_mask` 和 `dfi_cmdaddr`。不能靠字段改名推导电平极性或 CA packing。

#### 2.5.2 Direct 与 Behavioral

| 模式 | 含义 | 适用 |
|---|---|---|
| `direct` | 直接完成模式；按直接完成时序执行 | 回归对照、隔离 PHY 影响 |
| `behavioral` | 生命周期、FIFO、pipeline、adapter、DFI event | 正式 full-stack 仿真 |

Direct 不等于零延迟真实 PHY，Behavioral 也不等于 pin-level PHY。比较两模式时要同时
检查命令发射、自动预充完成点和 PHY pipeline 的统计口径。Controller 的 bank 状态会在
RDA/WRA 发令时进入自动预充语义，而真实 `MemoryImage` 的 row buffer 在对应数据完成时才
关闭；Direct 与 Behavioral 都遵守这一数据层完成点，避免两模式产生不同 payload 行行为。

#### 2.5.3 HBM 与 LPDDR 适配

HBM adapter 处理 pseudo-channel/SID、行列命令总线、HBM 特定 metadata/CRC/retry
入口；LPDDR adapter 处理 ACT1/ACT2、WCK sync/train、DVFS、DBI/link ECC/CA parity
入口。公共 Controller 不通过大量标准名分支硬编码这些差异。

PHY 反压指标按“本拍目标总线因 PHY 无法接收而未发出”计周期，一个 tick、一条总线
至多增加一次；它不是候选调度项被拒绝的累计次数。

`phy_command_fifo_depth` 的占用时长由 `command_pipeline_cycles` 决定。默认
pipeline 只有 1 拍时，16/32/64 项 FIFO 通常不会满，所以“命令反压为 0”
是默认参数的预期结果，不能解读为已验证了特定 CA 链路带宽。当前模型尚未提供
“每 M 拍释放 N 条 CA 命令”的独立 token-rate 参数；需要该精度时应扩展 PHY
服务率模型，而不是只增大 FIFO 深度。

<a id="section-2-6"></a>

### 2.6 Mem Stack 与存储后端

#### 2.6.1 MemoryImage

MemoryImage 是每颗 stack 的真实数据状态，管理：

- payload 与逐字节初始化 mask；
- 写掩码、版本号、最后写者；
- 行缓冲打开/关闭和当前行数据；
- system/local 地址对应的 DRAM 组织和物理坐标；
- ECC shadow、命令能量和稀疏热节点；
- memory dump、golden compare 和 mismatch report。

未初始化读取应通过统计/报告暴露；写后读和 `expect=` 是验证数据链闭环的主要方法。

#### 2.6.2 三种后端

| 后端 | 数据保存 | 优点 | 主要限制 |
|---|---|---|---|
| `sparse` | 宿主内存稀疏表 | 快、无文件管理 | 大覆盖集占 RAM，进程退出后消失 |
| `mmap_sparse` | 稀疏映射文件及 sidecar | 可持久化、随机访问 | 需要显式容量和兼容文件头 |
| `chunk_file` | 文件 + 有界 chunk cache | 满容量长跑、受控 RAM | cache miss 和写回有宿主 I/O 成本 |

更换后端只改变宿主机保存方式，不改变 Controller/TimingEngine/PHY、DRAM 地址映射、
row buffer、ECC、功耗和热的模型语义。公平比较必须固定配置、trace、初始 image、随机
种子，并为文件后端使用全新的文件集合。

#### 2.6.3 物理、功耗、热和 ECC

模型从 DRAM 地址派生 stack/die/layer/tile/grid/subarray/mat/cell/microbump 坐标，
只为已访问区域分配稀疏状态，不预分配完整芯片阵列。功耗支持配置的命令 pJ 或
DRAMsim3-oriented IDD/VDD 公式。热模型是行为级稀疏三维耦合模型：命令能量产生局部
温升，节点随时间冷却，并在同层四邻域和相邻层之间交换温差；TSV 参数修正纵向耦合。
它不求解材料热容、层厚和边界热阻，不应称为完整 RC/有限元热模型。功耗与温度适合
相对趋势和热点定位，绝对值需要器件、封装和散热参数校准。

ECC shadow 提供 payload line 的 SECDED 行为、单比特纠正和周期性注错入口，但不替代
真实链路 ECC、on-die ECC 和系统级 RAS 的全部编码、时延和覆盖范围。

<a id="section-2-7"></a>

### 2.7 异步请求—响应接口

异步接口是上层 CPU/NoC/UCIe/SystemC/RTL 仿真器与 `MemorySystem` 之间的协议中立边界。
它把“请求何时被接收”“系统推进一拍”“子事务何时完成”“host line 何时重组完成”分开，
从而表达真实反压，而不是一次函数调用同步返回数据。它不定义 UCIe flit、AXI channel、
NoC credit 或 CDC；适配器需要把这些协议映射到本接口的 ready/valid 语义。

#### 2.7.1 请求层级和拆分

一个上层 `HostRequest` 可以拆成多个 `Request`/DRAM transaction。例如 64B host line 在
32B `dram_transaction_bytes` 下拆成两个子事务：

```text
host_request_id = 100, transaction_count = 2
  transaction 0: id=T0, transaction_index=0, address=0x1000, transfer_bytes=32
  transaction 1: id=T1, transaction_index=1, address=0x1020, transfer_bytes=32
  T0 != T1
```

这里的 `T0/T1` 是示意符号，不表示它们与 `host_request_id=100` 存在数值换算关系。
`split_host_request()` 拆分时先复制 host 请求；子事务从 `TransactionSplitTrafficStream`
输出前再获得单调递增且彼此不同的 `id`。两个子事务以相同的 `host_request_id` 为聚合键，
并由 `transaction_index/count` 确定位置和完整性。

关键字段：

| Request 字段 | 含义和约束 |
|---|---|
| `id` | transaction stream 分配的子事务追踪 ID；同一 host 的子事务彼此不同，不参与优先级 |
| `host_request_id` | host 请求聚合标签；在响应真正 pop 前不能复用 |
| `transaction_index/count` | 子事务位置和总数；首个提交必须为 index 0，index 必须小于 count |
| `address/system_address` | 路由前全局地址和路由后 stack-local 地址 |
| `inject_cycle/arrival` | 上层希望发送的拍与系统实际接收拍，用于反压统计 |
| `target_stack/qos_class` | 显式 stack 和 stack ingress QoS；高数字优先级更高 |
| `payload/byte_mask` | 写数据和逐 byte 写掩码 |
| `expected_payload` | 读数据期望值，用于独立正确性检查 |

同一 host 的所有 transaction 必须保持 type/count 一致，每个 index 只能成功提交一次。违反
这些约束会抛出 `invalid_argument`，不会静默覆盖旧请求。

#### 2.7.2 请求 ready/valid 与入口反压

```cpp
bool accepted = memory_system.try_submit(request);
```

- 返回 `true`：等价于本拍 `valid && ready`，Request 已复制进目标 stack ingress，调用方
  可以前进到下一个 transaction；
- 返回 `false`：目标 stack ingress 已满，调用方必须保留完全相同的 Request，下一拍重试；
- 每个 system cycle 对同一阻塞只累计一次 frontend stall，不按重复候选调用次数累加；
- `stack_ingress_buffer_size` 决定入口容量，`stack_dispatch_width` 决定每拍向 channel
  Controller 分发的最大数量；目标 channel 反压时该 stack 保留请求，其他 stack 仍可推进。

Maintenance 不能调用 `try_submit()`，必须调用：

```cpp
bool accepted = memory_system.try_submit_maintenance(maintenance_request);
```

它同样受到 ingress 反压，但刷新、RFM、初始化等维护请求按定义不产生 HostResponse。

#### 2.7.3 TransactionResponse 与 HostResponse

Controller 每完成一个 DRAM transaction 产生 `TransactionResponse`，其中包含 request/host
ID、index/count、system/local address、stack/channel、arrival/issued/completion cycle、
读 payload、初始化 mask、ECC、forward/coalesce 和 status。

MemorySystem 按 `host_request_id + transaction_index` 重组 `HostResponse`：

- `transactions` 始终按 index 排序；
- 读数据和初始化 mask 按 index 拼接；
- arrival/first-issued 取最早值，completion 取最晚值；
- 子事务跨 stack/channel 时，host 级 stack/channel 记为 `-1`；
- 状态按严重度合并：`ok < ecc_corrected < uninitialized_data < data_mismatch < retry < timeout
  < ecc_uncorrectable < invalid_transaction`；
- 写响应只表示模型定义的写完成点，不返回写 payload。

#### 2.7.4 响应保留模式

`ResponseDeliveryMode` 决定对外保留哪种视图：

| 模式 | HostResponse | TransactionResponse | 用途 |
|---|---:|---:|---|
| `Disabled` | 否 | 否 | 默认批处理性能实验；完成对象及时丢弃，只留统计/trace |
| `HostOnly` | 是 | 否 | CPU/NoC/UCIe frontend 的默认异步模式 |
| `TransactionOnly` | 否 | 是 | 上层自行重组或调试乱序 transaction |
| `Both` | 是 | 是 | 同时审计子事务和 host 重组，内存开销最高 |

第一次成功 `try_submit()` 会把 Disabled 自动切到 HostOnly。也可在系统尚未运行且处于
`quiescent()` 时显式设置：

```cpp
MemorySystemOptions options;
options.response_delivery_mode = ResponseDeliveryMode::Both;
options.transaction_response_queue_capacity = 64;
options.host_response_queue_capacity = 16;
MemorySystem memory(spec, options);
```

容量 0 表示无限制。有限响应队列满时，MemorySystem 不从 Controller pop completion，反压会
真实传播到 Controller/pending；不会丢响应。若 host 长期不消费，吞吐下降或仿真停在响应
反压状态是预期行为。

callback 是非消费型旁路通知：注册 callback 后响应仍留在队列，最终仍必须 `pop_*()`。
`front_*()` 返回的引用只在下一次 pop/队列修改前有效，需要长期保存时应复制。

#### 2.7.5 完整外部事件循环

HostOnly 的最小循环是：

```cpp
MemorySystemOptions options;
options.response_delivery_mode = ResponseDeliveryMode::HostOnly;
options.host_response_queue_capacity = 32;  // 0 可表示无限制
MemorySystem memory_system(spec, options);

std::optional<Request> pending;
bool source_done = false;

while (!source_done || pending.has_value() || !memory_system.quiescent()) {
  if (!pending && !source_done) {
    Request next;
    if (source.next(next)) pending = std::move(next);
    else source_done = true;
  }

  if (pending && pending->inject_cycle <= memory_system.clock()) {
    const bool accepted = pending->type == RequestType::Maintenance
        ? memory_system.try_submit_maintenance(*pending)
        : memory_system.try_submit(*pending);
    if (accepted) pending.reset();
  }

  memory_system.step();

  while (memory_system.has_response()) {
    HostResponse response = memory_system.pop_response();
    consume(response);
  }
}

memory_system.finish(/* remaining_frontend_requests = */ 0);
```

若选择 TransactionOnly/Both，还要独立 drain：

```cpp
while (memory_system.has_transaction_response()) {
  TransactionResponse response = memory_system.pop_transaction_response();
  inspect_transaction(response);
}
```

`finish()` 生成与批处理 `run()` 相同的聚合统计和 command trace；若因外部 cycle limit 停止，
应把尚未送入的 frontend 数量传给 `finish(remaining)`，否则 `remaining_requests` 会偏小。

#### 2.7.6 idle、responses_drained 与 quiescent

| 查询 | 为 true 的条件 | 适合用途 |
|---|---|---|
| `idle()` | ingress、Controller、PHY、pending 等执行路径已空 | 判断内存执行是否完成 |
| `responses_drained()` | transaction/host 队列、重组表、tag 和 Controller response 均空 | 判断响应是否全部消费 |
| `quiescent()` | `idle() && responses_drained()` | 安全退出、切换 response mode、复用系统状态 |

因此 `idle()==true` 但 `quiescent()==false` 通常表示 HostResponse 已 valid、等待上层 pop。
事件循环退出条件必须使用 `quiescent()` 或同时检查执行与响应，不能只检查 `idle()`。

#### 2.7.7 CLI 中的真实异步路径

普通 CLI 不要求响应时走低开销 `run()` 批处理。启用任一 response mode 后，CLI 会真实执行
`try_submit/try_submit_maintenance → step → pop_*_response` 循环。只给 Host trace 自动选择
`host`，只给 transaction trace 自动选择 `transaction`，两者同时给出自动选择 `both`：

```bash
mkdir -p outputs/async
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 --requests 128 \
  --response-delivery-mode both \
  --host-response-queue-capacity 16 \
  --transaction-response-queue-capacity 64 \
  --response-trace outputs/async/host_responses.csv \
  --transaction-response-trace outputs/async/transaction_responses.csv \
  --stats-json outputs/async/stats.json \
  | tee outputs/async/stats.txt

column -s, -t < outputs/async/host_responses.csv | less -S
column -s, -t < outputs/async/transaction_responses.csv | less -S
python3 tools/view_stats.py outputs/async/stats.json \
  --keys host_requests,dram_transactions,remaining_requests,hit_cycle_limit

# CSV 去掉表头后的记录数；直接检查导出的响应数量
python3 - <<'PY'
import csv
for name in ('host_responses', 'transaction_responses'):
    with open(f'outputs/async/{name}.csv', newline='') as stream:
        print(f'{name}: {sum(1 for _ in csv.DictReader(stream))} records')
PY
```

这里的“真实异步”指 cycle-stepped valid/ready 风格的提交、反压和响应队列，不是启动
一个操作系统异步线程。在当前 HBM4 示例中，128 个 64 B host request 会拆成 256 个
32 B DRAM transaction，但上层最终仍收到 128 个重组后的 `HostResponse`。因此示例输出可
这样解读。表中的 `*_exported`、`injection_stall_cycles` 是内部诊断口径，不属于上面的
公共 JSON 字段；普通查验用 CSV 记录数核对响应数量，只有排查注入反压才按 7.1.2 采集：

| 字段 | 示例值 | 含义 |
|---|---:|---|
| `host_responses_exported` | 128 | 已从 host 响应队列 pop 并写入 CSV 的上层请求数；不是拆分后 transaction 数 |
| `transaction_responses_exported` | 256 | 写入 transaction CSV 的 DRAM 子事务数；HBM4 默认每个 64 B HostRequest 拆成两个 32 B transaction |
| `injection_stall_cycles` | 0 | frontend 提交时没有因 stack ingress/控制器无法接收而重试；不代表延迟为 0，也不代表 PHY 反压为 0 |
| `remaining_requests` | 0 | `finish()` 时 frontend 未提交量、ingress、Controller 队列和 pending 路径已清空 |
| `hit_cycle_limit` | false | 不是因 `max_cycles` 截断；应与 `remaining_requests : 0` 联合判定完成性 |

`host_responses.csv` 每行是一个 host response；`transaction_count`是它包含的 DRAM transaction 数，
`arrival/first_issued/completion_cycle` 可用于分解等待和服务时间，`status` 区分正常、未初始化数据等
响应，`data_hex` 和 `initialized_mask_hex` 用于上层 payload 审计。长行使用 `less -S`
左右滚动，不要让终端自动折行破坏列对齐。

`transaction_responses.csv` 每行对应一个 Controller 完成的子事务，并额外给出
`request_id/host_request_id/transaction_index/count`、system/local address、stack/channel 和
`issued_cycle`。`*_consumed` 统计 CLI 已 pop 的数量，`*_exported` 只统计实际写入 CSV 的行数。
显式启用 response mode 但不提供 trace 时，CLI 仍会每拍消费所选队列，等价于上层始终 ready；
需要任意 ready/credit 波形时，应由库调用者实现 2.7.5 的外部事件循环。

运行时 `stats.txt` 仍然可能在结束前为空，因为聚合统计在末尾统一输出；CSV 则是边 pop
边写。另一终端可用下式观察响应文件增长：

```bash
watch -n 1 'wc -l outputs/async/*responses.csv outputs/async/stats.txt'
```

响应模式和两种 response trace 只能用于默认 MemorySystem，不能与 `--single-controller`
同用，因为单 Controller 路径没有 system-level transaction/host 聚合层。



<a id="section-2-8"></a>

### 2.8 统计、输出与验证

所有外部时间应优先使用 `system_cycles`。多 controller 环境中的
`aggregate_ctrl_cycles` 是控制器周期求和，不能拿它直接计算系统壁钟带宽。
`achieved_bw_GBps` 使用 payload 字节；`achieved_if_bw_GBps` 在相同时间分母上加入配置的
metadata/ECC，是记账等效需求带宽。当前保护 bit 不延长命令或数据总线占用，因此后者
不是 pin-accurate 实测链路带宽，也可能高于理论 payload 峰值。

验证分为：在线状态/数据检查、仿真后 command/DFI validator、CTest 原生回归、
外部参考差分和最终厂家/硬件校准。通过 Ramulator/DRAMsim 差分只能说明共同抽象在给定
配置下相符，不能证明某个 HBM4/LPDDR6 器件等价。

---

<a id="chapter-3"></a>

## 3. 平台与模型划分

| 层 | 主要对象 | 责任 | 可替换项 |
|---|---|---|---|
| 配置层 | traits/profile/config/CLI | 标准、参数、来源、派生和合法性 | profile、override |
| 平台层 | Frontend/MemorySystem/Controller/Stats | 输入、路由、调度、时序、响应、统计 | scheduler、mapping、QoS |
| PHY 层 | MemPhy + adapter | 生命周期、FIFO、流水、协议事件 | direct/behavioral、adapter |
| Stack 层 | MemoryImage/Backend | payload、row buffer、物理、ECC、功耗热 | backend、floorplan、模型参数 |
| 工具层 | Python validators/visualizer | 回归、差分、曲线、离线展示 | 外部参考、输出格式 |



---

<a id="chapter-4"></a>

## 4. 四种标准模型

| 标准 | 主要组织/协议特性 | 当前配置定位 | 校准重点 |
|---|---|---|---|
| HBM3 | stack、channel、PC、SID、BG/bank | Ramulator2.1-derived 研究基线 | 目标 speed-bin、RL/WL、功耗热 |
| HBM4 | full-stack、32 channel、行列总线、metadata/RAS 入口 | JESD270-4A-oriented + 研究默认 | 厂商 timing、RFM、PHY、封装热 |
| LPDDR5 | channel/subchannel、WCK、低功耗 | Ramulator2.1-derived 对照基线 | 器件组织、WCK/训练、IDD |
| LPDDR6 | x12/BL24、ACT1/2、WCK、DVFS、链路保护入口 | JESD209-6-oriented + 研究默认 | RL/WL、训练、link ECC、功耗 |

四种标准共享请求、调度、状态、数据和输出框架，但不应通过简单改名互相替代。
每次实验或论文结果应同时记录 standard、profile、speed bin、density、stack height、
timing source、commit 和完整命令行。

当前主配置的可寻址容量经地址几何复算为：HBM3 16 GiB/Stack（16 Gibit/die × 8-Hi）、
HBM4 32 GiB/Stack（32 Gibit/die × 8-Hi）、LPDDR5 2 GiB（单个 16 Gibit x16 channel）、
LPDDR6 4 GiB（当前模型两个 16 Gibit x12 Sub-Channel）。四者的 frontend host line 均为
64 B，单条 DRAM transaction 均为 32 B，因此普通 host 请求默认拆成两个 transaction。
外部差分 preset 可为对齐参考模型而使用不同的验证几何，不能拿它替代主配置容量。

LPDDR6 BL24 的 `nBL=6 CK`，与 32 B payload 分开理解：x12 × BL24 为 288 bit，
含 256 bit 数据与 32 bit 非数据；输出接口字节是配置开销的等效需求，不是完整引脚槽计数。
同 BG 的列间隔另按速度档选取，不能把 BL24 直接当作 nBL 或列地址换算因子。
REFdb 在同一刷新行内使用短间隔，完整轮转后到下一行使用长间隔；每轮禁止重复 Bank，
REFab 和自刷新退出重置计数。固定相邻 BG 配对仍是项目支持范围，不是完整 dBG 编码。

验证用 LPDDR5/6 将参考模型原始列数 1024 / 地址预取因子 16 换算为 64 个事务槽，
单 Channel/单 Subchannel 共同面为 2 GiB；旧版放大 16 倍的几何已更正。
DRAMsim3 旧 HBM 共同面使用 32 个 64 B 槽、2 KiB 行、512 MiB/Channel 和 4 层。
地址单位换算、边界测试与外部执行证据见配置验证目录说明及审计第 4.1 节。

---

<a id="chapter-5"></a>

## 5. 构建与部署

<a id="section-5-1"></a>

### 5.1 依赖

- Linux 或具备 POSIX 兼容层的环境；
- 支持 C++20 的 Clang 或 G++；
- CMake 3.16+ 与 Ninja（推荐），或 GNU Make；
- Python 3（验证和可视化工具）；
- Bash（CTest smoke、示例和 Make 工具目标会调用 shell 脚本）；
- 可选 ccache、clangd、LLDB；按本手册的查验命令操作时，还建议安装
  `rg`、`column`、`less`、`file`、`xxd`。

核心 C++ 不依赖第三方库。Ramulator2/DRAMsim3 只用于可选外部验证，不参与默认构建。

#### 5.1.1 新环境的基础配置

Ubuntu/Debian 示例：

```bash
sudo apt update
sudo apt install build-essential cmake ninja-build python3 bash

# 选择 Clang 时再安装；版本不必固定为 18，只要完整支持 C++20
sudo apt install clang clangd lldb ccache

# 按本手册查看、对齐和检查产物时建议安装
sudo apt install ripgrep bsdextrautils less file vim-common

cmake --version
ninja --version
python3 --version
clang++ --version   # 或 g++ --version
```

进入仓库根目录后先做一个全新的 out-of-source 构建：

```bash
cmake -S . -B build-port -G Ninja \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_CXX_COMPILER=clang++ \
  -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
cmake --build build-port --parallel
ctest --test-dir build-port --output-on-failure
./build-port/hbm_sim --config configs/hbm.cfg --standard hbm4 --requests 128
```

不需要设置项目专用环境变量。配置和 profile 中使用相对路径时，应从仓库根目录运行；若从
其他工作目录启动，应把 `--config`、trace、profile 和输出路径改成绝对路径或正确相对路径。

#### 5.1.2 移植声明

项目的算法核心、Controller、TimingEngine、PHY、统计和 `sparse` 后端只依赖 C++20 标准库，
适合移植到常见 64-bit Linux/Unix 环境。文件后端在 `src/core/memory_backend.cpp` 中直接使用
`mmap`、`open`、`ftruncate`、`pread/pwrite`、`fsync` 和 `unistd/fcntl`，因此：

- Linux 原生环境无需修改；
- WSL 建议把仓库和后端文件放在 WSL Linux 文件系统，而不是低性能的跨系统挂载目录；
- macOS/其他 POSIX 系统应验证 `off_t`、稀疏文件和 `mmap` 行为；
- Windows 原生 MSVC 不能直接编译当前文件后端，需要用 WSL，或为这些 POSIX API 实现
  Win32/file-mapping 适配层；只移植 `sparse` 仍需在编译期隔离 POSIX 实现文件。

文件后端 `.meta` 使用项目版本化的本机二进制结构，虽然检查 magic、版本、record size、
line size 和容量，但不承诺跨大小端、跨 ABI 或跨版本直接搬运。跨架构迁移应先用
`--dump-memory-image` 导出文本/项目二进制 checkpoint，在目标环境重新创建空后端并加载，
不要只复制 data/`.init`/`.meta`/`.present` 后假定兼容。

#### 5.1.3 移植时通常需要修改的位置

| 环境差异 | 需要检查或修改的位置 | 一般不需要改的位置 |
|---|---|---|
| 编译器路径/版本 | CMake 的 `CMAKE_CXX_COMPILER`；`.vscode/settings.json`、`tasks.json` 中 `/usr/bin/clang++-18`/`clangd-18` | `src/` 模型逻辑 |
| 构建目录 | `.vscode/settings.json` 的 `build-clang-debug`、launch/task 中程序路径 | CMake source list |
| 非 POSIX 文件系统 | `src/core/memory_backend.cpp` 的 mmap/file descriptor 实现 | `MemoryBackend` 抽象接口、`sparse` 语义 |
| 没有 Bash | `tests/*.sh`、`examples/*.sh`、CTest shell 项、Make 工具目标 | C++ 单元测试可执行文件 |
| Python 命令不同 | CMake/Make/Python 工具中的 `python3` 调用 | C++ CLI |
| 项目路径改变 | VS Code 配置、外部 Ramulator 根目录、用户配置中的绝对路径 | `${workspaceFolder}` 和仓库内相对路径 |
| 新标准/新器件 | 对应主配置、`profile_index.csv`，必要时 traits/profile/finalize | 构建系统通常无需修改 |

不要把 `/usr/bin/clang++-18`、个人目录或 Ramulator 路径写进 C++ 源码；它们应停留在本地
IDE、构建命令或实验配置中。

#### 5.1.4 最小部署集合

本项目没有 `cmake --install` 目标。只运行仿真时至少需要：

```text
hbm_sim                         编译后的可执行文件
configs/hbm.cfg、lpddr.cfg     两份标准家族主配置
examples/configs/*.cfg         单实例/多实例完整 cfg Demo（按需复制）
examples/ 或自定义 trace/image  输入数据
outputs/                        可写输出位置
```

需要仓库验证时还要复制 `configs/validation/`；需要自定义研究模型时复制相应主配置到自己的
实验目录。需要离线验证/可视化时还要复制 `tools/` 并准备 Python 3；需要 CTest 时还要保留
`tests/`、源码和构建树。部署后先运行 `--help`、128 请求 smoke、一个数据 trace 和 dashboard
生成，再开展长时间实验。

#### 5.1.5 移植验收清单

- 编译器实际启用 C++20，且 `sizeof(std::uint64_t)==8`；
- Debug/Release 都能构建，`ctest` 全部通过；
- 四标准各完成一个小规模运行；
- `sparse` 数据写后读通过；
- 若启用文件后端，创建、关闭、重开和 sidecar 头校验通过；
- command/DFI CSV 能生成，Python 工具能读取；
- 多 Stack `{stack}` 文件展开正确；
- 新环境与原环境使用同一配置/seed 时，完成量和命令级不变量一致；
- 浮点功耗/热允许记录编译器差异，但必须在预先声明的误差范围内。

<a id="section-5-2"></a>

### 5.2 Clang 推荐构建

```bash
cmake -S . -B build-clang-debug -G Ninja \
  -DCMAKE_BUILD_TYPE=Debug \
  -DCMAKE_CXX_COMPILER=/usr/bin/clang++-18 \
  -DCMAKE_CXX_COMPILER_LAUNCHER=ccache \
  -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
cmake --build build-clang-debug --parallel
ctest --test-dir build-clang-debug --output-on-failure
```

<a id="section-5-3"></a>

### 5.3 G++ 构建

```bash
make -j"$(nproc)"
./build/hbm_sim --help
make test
```

这样 G++ 固定使用 `build/`，Clang/CMake 固定使用 `build-clang-debug/`，日常只保留这两棵
构建树。PHY 随 core 一起构建，不需要独立构建目录。

<a id="section-5-4"></a>

### 5.4 Make 兼容入口

Makefile 使用 `build/`，默认 `CXX ?= g++`、`-std=c++20 -O2 -Wall -Wextra -pedantic`。
Make 是独立的构建入口；完整、可枚举的回归见 CMake/CTest 和 `ctest -N`。

#### 5.4.1 构建、增量构建和运行

```bash
# 默认 G++ Release-like 构建，生成 build/hbm_sim
make -j"$(nproc)"

# 默认 HBM4 stream/10000 requests
make run

# 只构建目标，不运行
make build/hbm_sim

# 可选诊断：会临时创建第三棵 build-clang-sanitize/，不属于日常构建
make sanitize
make sanitize-test
```

Make 会用 `-MMD -MP` 生成 `.d` 依赖文件，头文件变化可触发相关 object 增量重编译；还会
记录编译器版本、`CPPFLAGS/CXXFLAGS/LDFLAGS/LDLIBS` 构建签名，任一项变化时自动重编译，
避免普通链接误用旧 ASan/UBSan object。仍不要把 Make 与 CMake 产物写入同一目录。若临时
运行 sanitizer，验收后删除 `build-clang-sanitize/`，恢复只保留两棵日常构建树。

#### 5.4.2 示例目标

| 命令 | 实际行为/预计产物 |
|---|---|
| `make examples` | 依次运行 HBM4 stream、HBM3 random、LPDDR6 trace、LPDDR5 trace |
| `make examples_hbm4` | 运行 `examples/run_hbm4_stream.sh` |
| `make examples_hbm3` | 运行 `examples/run_hbm3_random.sh` |
| `make examples_lpddr6` | 运行 `examples/run_lpddr6_trace.sh` |
| `make examples_lpddr5` | 运行 `examples/run_lpddr5_trace.sh` |

#### 5.4.3 测试和验证目标

日常执行 `make test` 即可覆盖其子目标；单项命令用于定位失败，不必在总测试后逐个重跑。
性能/敏感性扫描及外部参考按实验需要另跑。目标依赖和适用范围见[项目指南](文档/项目指南.md#tests)。

| 命令 | 覆盖内容 |
|---|---|
| `make smoke` | CLI、四标准和主要输出的 shell smoke |
| `make sequence-test` | 构建并运行 C++ `sequence_tests` |
| `make phy-test` | 构建并运行 C++ `phy_tests` |
| `make config-test` | 检查分层与继承、preset/override 优先级、未知值拒绝、主配置与示例的一致性及字段注释 |
| `make scheduler-contract-test` | 调度器和请求依赖契约 |
| `make result-test` | C++/Python 公共结果、缺省及失败输出契约 |
| `make refactor-test` | 共享工具和统一运行接口回归 |
| `make phy-smoke` | Behavioral PHY CLI 路径 |
| `make visualization-smoke` | 从实际 CLI 产物生成 dashboard |
| `make multistack-backend-smoke` | HBM4 双 Stack 的 sparse/mmap/chunk 隔离和持久化 |
| `make multistack-demos-smoke` | 四标准多 Stack demo 与 dashboard |
| `make architecture-sweep-smoke` | 定向 bank/geometry/refresh 工作负载与趋势检查 |
| `make architecture-sweep` | 完整 HBM4/LPDDR6 定制架构扫描 |
| `make timing-boundary-validation` | 构建 timing probe 并审计 `t-1/t` 矩阵 |
| `make model-validation` | 数据、后端、DFI、物理、功耗热和身份检查 |
| `make performance-validation` | 生成/检查 CI 规模性能曲线 |
| `make sensitivity-validation` | Timing 敏感性与不确定性检查 |
| `make test` | 上述核心/CLI/配置/多 Stack/架构 smoke、Timing 边界和 model validation；不含完整性能/敏感性/外部参考 |

完整 Make 检查流程：

```bash
mkdir -p outputs/checks
set -o pipefail

make test 2>&1 \
  | tee outputs/checks/make_test.log
test_status=${PIPESTATUS[0]}

make performance-validation 2>&1 \
  | tee outputs/checks/performance_validation.log
performance_status=${PIPESTATUS[0]}

make sensitivity-validation 2>&1 \
  | tee outputs/checks/sensitivity_validation.log
sensitivity_status=${PIPESTATUS[0]}

printf 'test=%s performance=%s sensitivity=%s\n' \
  "$test_status" "$performance_status" "$sensitivity_status"

rg '(^PASS |^FAIL |tests passed|validation:|performance curves:|sensitivity/uncertainty:)' \
  outputs/checks/*.log | less -S
```

该流程使用 `build/` 中的 G++ 主程序和 test binary；Clang 的等价完整回归应执行 5.2 的
`ctest --test-dir build-clang-debug --output-on-failure`。Make 会先增量构建 `build/hbm_sim`。
`set -o pipefail` 和 `PIPESTATUS[0]` 用于保留 `tee` 前面 Make 的真实退出码；三个 status
均为 0 才是完整通过。

`make test` 覆盖上表的单元、CLI 和集成测试；并行构建时不要依赖日志顺序。
Timing 边界工具的“checks passed”是验证器检查项数，不是时序约束条数。
具体边界矩阵按标准列出案例；`skipped` 表示不适用或无法构造，`failed` 非零会使工具失败。

Make 中的 `model-validation` 实际就是：

```bash
python3 ./tools/model_validation.py --binary ./build/hbm_sim
```

主要 PASS 项含义如下：

| PASS 项 | 它在证明什么 | 注意 |
|---|---|---|
| `project_validation_config_identity` | 验证专用 profile/mode/channel 身份正确，防止跑错配置 | 不是厂商器件身份认证 |
| `peak_bandwidth_formula` | 按 data rate × bus width/8 得到的理论峰值与 CLI 一致 | 不代表 workload 可达到峰值 |
| `interface_rate_formula` | Mbps/pin 到 Gbps/pin 的换算一致 | 不是信号完整性或 BER 验证 |
| `timing_source_audit` | 被检查的 Timing 项均带有合法来源标签，`bad_sources=[]` | 不代表 research default 已获厂商确认 |
| `research_defaults_are_visible` | 临时/研究默认值没有被伪装成 JEDEC/vendor 值 | 不代表这些值已完成器件校准 |
| `act_to_read_timing` | ACT 到 RD 的实际命令间隔符合配置 | 只覆盖该构造场景 |
| `closed_row_read_completion` | closed-row read 的提交到完成时间链符合模型 | 不是所有读延迟的固定值 |
| `dfi_read_latency` | DFI 读返回事件与命令间隔符合配置 | 不包含模拟电气信号延迟 |
| `online_validators` | command validator 和 DFI validator 都实际运行且 PASS | `off` 不等价于 PASS |
| `dfi_real_payload_roundtrip` | 32 B 写 payload 通过存储/DFI 路径后读回 32 B | 不是长时间随机数据压测 |
| `project_storage_payload_semantics` | write commit、checked read、mismatch 和 sparse backend 的项目语义闭环 | 不说明其他 backend 的宿主性能 |
| `dfi_mask_and_source` | byte mask 合法，payload 来源包含 memory image/无 payload/request payload | 不是 PHY 引脚波形校验 |
| `dfi_expected_payload_guard` | expected 读数据独立参与比较，不是用 actual 自证 | 不会替代更大 trace 的覆盖 |
| `physical_stack_coordinates` | 访问产生 layer/bank/subarray/cell 坐标 | 是行为级映射，不是版图 sign-off |
| `power_thermal_event_path` | 3 个 power event 导致 3 次 thermal update，事件链闭合 | 不是厂商功耗/温度校准 |

脚本还会验证同地址顺序、错误退出码、文件后端持久化、四标准范围、burst frontend、
带宽阈值、NCL 敏感性及 manifest 绑定。检查项数量以当前脚本输出为准；必须同时核对
进程退出码，不能只搜索某几个 `PASS`。

`performance-validation` 默认对 HBM3/HBM4/LPDDR5/LPDDR6，分别使用 100/50/0% 读比例
和 64/32/16/8/4/2/1 tick 注入间隔，共生成 84 个确定性曲线点。它检查高负载吞吐
不低于低负载、吞吐不超过物理峰值，以及有读流量时高负载延迟不低于低负载。
这是内部曲线形状和峰值边界验证，不是厂商带宽校准；以退出码及汇总中的失败数判定结果。

`sensitivity-validation` 默认对四标准的 `nCL/nRCDRD/nRP/nCCDS/nRRDS/nFAW`
分别做 -10%/标称/+10% 单因素扰动，得到 72 行 OAT 结果；再用固定 seed 组合采样生成
p05/p50/p95 顺序区间，并检查 power scale 与能量/温度的单调性。`finite_response` 只证明结果
有限且非负；只有名为 `*_monotonic` 的项才断言单调。p05/p50/p95 是确定性参数不确定区间，
不是硬件统计置信区间。

Make 入口默认只打印性能/敏感性结果，不保存 CSV/JSON。需要表格与可视化输入时，直接
调用工具的 `--csv-out/--json-out`，见第 7.3 节。

#### 5.4.4 可视化和外部参考

```bash
# 生成 outputs/visualization/example/dashboard.html
make visualize-example

# 使用本地 Ramulator2；ramulator-validation 是同义目标
make reference-validation \
  RAMULATOR2_ROOT=/path/to/ramulator2
make ramulator-validation \
  RAMULATOR2_ROOT=/path/to/ramulator2
```

`/path/to/ramulator2` 是占位符，必须替换为本机 Ramulator2 仓库根目录，不能原样执行；
例如本机若位于 `/home/guxing/learn/memorystack/ramulator2`，就将该绝对路径赋给
`RAMULATOR2_ROOT`。两个 Make 目标等价，只需任选一个运行，不需要先后都执行。

`make visualize-example` 会先确保 `build/hbm_sim` 已构建，再运行一个小规模真实仿真以生成
command/DFI/stats/thermal 产物，还会生成 HBM4 下 2 种读比例 × 7 个注入间隔的 14 个性能曲线点，
最后由 `tools/visualize.py` 把数据嵌入自包含 HTML。生成完后：

```bash
test -s outputs/visualization/example/dashboard.html
ls -lh outputs/visualization/example
xdg-open outputs/visualization/example/dashboard.html
```

`test -s` 只检查 HTML 存在且非空；浏览器中还应先看 Run summary 身份与 validation，再看
Trace explorer、Command mix 和 DFI activity。该 HTML 不需要服务器。如果远程主机无图形桌面，
可在项目根目录运行：

```bash
python3 -m http.server 8000 --bind 127.0.0.1 --directory outputs/visualization/example
```

然后从可访问该主机的浏览器打开 `http://HOST:8000/dashboard.html`；涉及跨主机时应遵守
本机网络/防火墙规则，或直接下载 HTML 本地打开。任意自己的仿真如何生成 dashboard 见第 7.3 节。

`reference-validation` 不生成 dashboard。它会定位并调用给定目录中的 Ramulator2，将双方
可共同观测的命令/延迟面做差分，结果打印到终端；可用 `2>&1 | tee outputs/acceptance/ramulator.log`
保存。要做图形对比，应先分别导出可视化支持的 CSV/JSON，不要把差分 PASS 误读为图形产物。

外部参考不属于默认 `make test`，结果只证明共同命令面的差分关系。

#### 5.4.5 清理目标

```bash
# 删除 Make/G++ 构建目录 build/；不影响 CMake/Clang 的 build-clang-debug
make clean

# 先列出，再删除仓库根目录全部 build、build-*（含 CMake/Ninja 构建树）
make list-builds
make clean-build

# 删除 outputs/ 下除 outputs/README.md 外的全部仿真产物
make clean-outputs

# 与 clean-outputs 相同的别名
make delete
```

不同编译器必须使用不同构建目录。PHY 是 `hbm_sim_core` 的组成部分，不单独建立
`build-phy*`。本项目日常只保留 Make/G++ 的 `build/` 和 CMake/Clang 的
`build-clang-debug/`；可选 sanitizer 产生的 `build-clang-sanitize/` 应在诊断后删除。
VS Code/clangd 使用 `build-clang-debug/compile_commands.json`。

`make clean-build` 是明确的全构建树清理，会删除它列出的目录，但不会删除 `outputs/`；
`make clean-outputs` 则相反，只清理运行产物。两者不要混为一谈。

---

<a id="chapter-6"></a>

## 6. 仿真模型使用说明

<a id="section-6-1"></a>

### 6.1 标准流程

```text
选择 standard/profile
  → 选择 workload/trace
  → 选择 stack/PHY/backend
  → 配置输出与验证
  → 运行仿真
  → 检查退出码和 validation 字段
  → 检查数据、性能、功耗热和 trace
  → 保存完整配置、命令、commit 与产物
```

<a id="section-6-2"></a>

### 6.2 四标准快速运行

下表给出快速入口。需要查看完整的产物导出、指标筛选、预计效果和可视化命令时，直接跳到
[附录 C：四标准运行用例](#appendix-c)。

| 标准 | 快速配置 | 完整运行案例 |
|---|---|---|
| HBM3 | `configs/hbm.cfg + hbm3` | [C.1 HBM3](#appendix-c-1) |
| HBM4 | `configs/hbm.cfg + hbm4` | [C.2 HBM4](#appendix-c-2) |
| LPDDR5 | `configs/lpddr.cfg + lpddr5` | [C.3 LPDDR5](#appendix-c-3) |
| LPDDR6 | `configs/lpddr.cfg + lpddr6` | [C.4 LPDDR6](#appendix-c-4) |

```bash
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm3
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4
./build-clang-debug/hbm_sim --config configs/lpddr.cfg --standard lpddr5
./build-clang-debug/hbm_sim --config configs/lpddr.cfg --standard lpddr6
```

四条命令按需选一条执行；用 `--requests 1024` 指定请求数。

不带 `--config` 时使用 C++ 内置协议基准；
主配置还包含平台、负载和 PHY 等默认设置，两者不保证等价。
为便于复现实验，建议显式指定配置并保存生效配置快照。

<a id="section-6-3"></a>

### 6.3 合成流量

```bash
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 \
  --pattern random --requests 10000 --read-ratio 70 \
  --addr-stride 64 --inject-interval 2 --seed 42
```

合成流量不需要先制作 trace，frontend 会按参数逐条生成 host request：

| 参数 | 含义 |
|---|---|
| `--pattern stream` | 地址按 `request_id × addr_stride` 增长，适合连续/跨行步长实验 |
| `--pattern random` | 在聚合地址空间内随机选择 cache line；此时 `addr_stride` 不参与地址生成 |
| `--random-address-space-bytes N` | 把 random 限制在聚合系统地址空间前 N 字节；0 使用模型总容量，非零值必须按 `line_size` 对齐且至少容纳一个 HostRequest |
| `--requests 10000` | 生成 10000 个拆分前 host request；一个 host request 可拆成多个 DRAM transaction |
| `--read-ratio 70` | 每条 host request 有 70% 概率为读，写比例为 30% |
| `--inject-interval 2` | 第 N 条请求的最早注入时间为 `N×2` controller ticks；0 表示全部从 cycle 0 开始可注入 |
| `--seed 42` | 固定地址与读写类型的随机序列，便于重放 |

当 `mmap_sparse/chunk_file` 只为实验建立了小容量文件时，应把
`--random-address-space-bytes` 设为不超过实际可访问范围的对齐值；否则 random 的默认
full-model 地址空间可能生成后端容量之外的地址。该参数只改变随机地址采样范围，不改变
`DramSpec` 标称容量、峰值带宽或组织结构。

建议先用 128/512 请求检查身份和完成性，再扩大到 10000 或更多。上例只把统计打到
stdout；如果需要查看具体生成的地址/命令，同时导出 command 和 response trace：

```bash
mkdir -p outputs/synthetic
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 \
  --pattern random --requests 512 --read-ratio 70 \
  --inject-interval 2 --seed 42 \
  --cmd-trace outputs/synthetic/commands.csv \
  --response-trace outputs/synthetic/responses.csv \
  --stats-json outputs/synthetic/stats.json \
  | tee outputs/synthetic/stats.txt

python3 tools/view_stats.py outputs/synthetic/stats.json \
  --keys run_status,completed_reads,completed_writes,simulation_time_ns,achieved_bw_GBps,avg_read_latency_ns,row_hit_pct,data_mismatches
column -s, -t < outputs/synthetic/responses.csv | less -S
```

stdout 的 summary 精简报告会展示负载、种子和注入间隔等主参数，但实验仍应
保留完整命令、resolved config 和输入 trace 作为身份证据。需要曲线时，应用 `tools/performance_curve.py`
输出 CSV/JSON 再加入 dashboard，而不是手工把多次 stdout 拼在一起。改变 injection interval 时，
标准、profile、pattern、requests、read ratio、seed 和其他配置必须固定，才能形成可比较的
注入率—延迟—吞吐曲线。

<a id="section-6-4"></a>

### 6.4 多 Stack

```bash
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 --stack-count 6
```

或覆盖：

```bash
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 \
  --stack-count 6 --stack-mapping interleaved \
  --stack-interleave-bytes 256 \
  --stack-ingress-buffer-size 256 --stack-dispatch-width 4 \
  --stack-qos-policy strict_priority --requests 10000
```

多 Stack 不是把一颗 stack 的统计乘以 N。`MemorySystem` 对每个 `[stack][channel]` 创建独立
Controller，每个 controller 有自己的 PHY，每个 stack 有独立 ingress、MemoryImage、功耗和热状态。
`interleaved + 256 B` 将系统地址按 256 B stripe 在 stack 间轮转，进入 stack 后再转成局部地址并解码
channel/bank/row。Trace 中显式 `stack=N` 会覆盖自动映射。

#### 6.4.1 配置规则和参数联动

`stack_count` 表示独立存储封装/实例数量；HBM 的 `stack_height` 表示一颗 Stack 内的
DRAM die 层数，两者不能相乘后当成同一参数。LPDDR 也复用系统级 `stack_count` 名称，
此时它表示多个独立 LPDDR device/package 实例，不表示 TSV 堆叠。

| 配置 | 联动规则 |
|---|---|
| `stack_count=N` | 创建 N 个 ingress、N 份 MemoryImage/backend/功耗热状态，以及每 Stack 每 channel 的 Controller/PHY |
| `aggregate_capacity_bytes` | 单 Stack 逻辑容量 × N；不是后端宿主文件的实际占用 |
| `stack_mapping=interleaved` | 以 `stack_interleave_bytes` 为 stripe 轮转 Stack；stripe 必须是 `line_size` 的整数倍 |
| `stack_mapping=blocked` | 每颗 Stack 获得连续的系统地址区间；小地址 workload 可能只触达 Stack 0 |
| `stack_ingress_buffer_size` | 每颗 Stack 的入口深度，不是全系统共享深度 |
| `stack_dispatch_width` | 每 Stack 每 system tick 最多向目标 channel Controller 分发的 transaction 数 |
| `stack_qos_policy` | `fcfs` 按到达顺序；`strict_priority` 先比较 `qos`，同优先级保持可重放顺序 |

schema 3 还联动 `data_rate_mbps → speed_bin_mbps/tCK_ps`、逻辑几何 → `density_gb`、
Channel/PC 数 → 默认总 DQ 位宽。缺省或 `auto` 表示推导；显式密度/时钟与几何/速率
矛盾时报错。修改 columns=32→16 会使逻辑容量和推导密度减半，不必再手工修改密度。
完整规则与物理层级边界见[cfg指南](文档/cfg指南.md)。

interleaved 的概念映射是：

```text
stripe_index = system_address / stripe_bytes
target_stack = stripe_index % stack_count
local_address = (stripe_index / stack_count) * stripe_bytes
              + system_address % stripe_bytes
```

blocked 则按单 Stack 容量确定目标区间并减去该区间起始地址。Trace 的 `stack=N` 明确
表示地址已经是 Stack-local，因而绕过上述自动选择，但后续 channel/bank/row 解码仍执行。
文件后端和 thermal/memory dump 必须使用 `{stack}`，或由程序自动插入 `.stackN`；不能让
两个 Stack 指向同一 backend 文件。

#### 6.4.2 tick、CK 与系统时钟

##### 6.4.2.1 CK：JEDEC 时序使用的 DRAM 参考周期

CK 是内存协议时序的参考时钟周期。配置中的 `tCK_ps` 表示一个 CK 的时间，JEDEC timing 则以 nCK 保存，例如 nRCD=30 表示 30 个 CK。

CK 是协议/器件层概念。它用于回答：ACT 后至少经过多少个 DRAM 时钟周期才允许列命令。

##### 6.4.2.2 tick：仿真器内部最小离散时间步

本项目允许把一个 CK 再划分成多个 controller tick：

```text
每 CK 的 tick 数 = tick_multiplier
一个 tick 的时间 = tCK_ps / tick_multiplier
nCK timing 对应的 tick 数 = nCK × tick_multiplier
```

HBM4 默认：

```text
tCK_ps          = 500 ps
tick_multiplier = 2
1 tick          = 250 ps
```

因此 nRCD=30 的等待时间为：

```text
30 CK × 2 tick/CK = 60 tick = 15 ns
```

HBM 使用两个 tick/CK，主要是为了表达 rising/falling edge 和 row/column 命令配对等半周期行为。LPDDR6 当前默认 `tick_multiplier=1`、`tCK_ps=375 ps`，所以一个 tick 就等于一个 CK。

##### 6.4.2.3 system clock/system cycle：MemorySystem 的全局推进计数

`MemorySystem::tick()` 每次会：

1. 从各 Stack ingress 向 channel 分发请求；
2. 让所有 `[stack][channel]` Controller 各推进一个 tick；
3. 收集完成响应；
4. 把全局 `clk_` 加一。

因此当前实现中，system cycle 与 controller tick 是一比一推进的同一离散时间域，并不是另一个独立频率的 CPU/UCIe 时钟。之所以保留 system 概念，是为了区分：

- `system_cycles`：全系统实际经过了多少个并行时间步；
- 单 Controller 的 `cycles`：该 Controller 经过的 tick；
- `aggregate_ctrl_cycles`：所有 Controller cycle 的总和。

例如 1 个 Stack、32 个 Controller 运行 1000 个 system tick 时，`aggregate_ctrl_cycles` 通常约为 32000，而实际仿真时间仍只有 1000 tick，不能把 32000 当成串行经过的时间。

仿真时间换算为：

```text
tick_frequency = (10^12 / tCK_ps) × tick_multiplier
elapsed_seconds = system_cycles / tick_frequency
```

当前外部 CPU、NoC、UCIe 或 SystemC 时钟尚未作为第四个独立时钟域实现。将来接入外部平台时，才需要明确外部周期与 MemorySystem tick 的频率比、跨时钟队列和握手延迟。`data_rate_mbps` 是接口数据率参数，也不是另一个可逐拍调度的时钟。

本项目只有一个离散事件推进单位 `system/controller tick`。`tCK_ps` 是 DRAM CK 周期，
`tick_multiplier` 是每 CK 被切成的 tick 数：

```text
tick_time_ps = tCK_ps / tick_multiplier
timing_wait_ticks = timing_value_nCK * tick_multiplier
simulated_time_s = system_cycles * tick_time_ps * 1e-12
```

例如 HBM4 `tCK_ps=500`、`tick_multiplier=2` 时，1 tick=250 ps，`nRCDRD=30`
会约束 ACT→RD 至少 60 ticks。HBM 双边沿/双命令总线和 DFI phase 都在这个 tick 域记录；
`inject_interval`、trace 首列 cycle、响应 arrival/completion、command/DFI CSV cycle 也使用
同一个 tick。`data_rate_mbps`/总线位宽决定理论接口速率，不能通过单独增大
`tick_multiplier` 伪造带宽；改变速率档时必须一起复核 `data_rate_mbps`、`tCK_ps`、
所有 ns/us→nCK 换算和 PHY latency。

建议先小规模运行并查看当前真实动态字段：

```bash
mkdir -p outputs/multistack
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 --stack-count 6 \
  --requests 1024 --stats-json outputs/multistack/stats.json \
  | tee outputs/multistack/stats.txt

python3 tools/view_stats.py outputs/multistack/stats.json \
  --keys stack_count,aggregate_capacity_bytes,system_cycles,completed_reads,completed_writes,remaining_requests,hit_cycle_limit,avg_read_latency_ns
```

精简报告末尾的 Stack 表和 JSON 的 `stacks` 数组记录每实例的
`completed_reads/completed_writes/achieved_bw_GBps/avg_read_latency_ns`。
各 Stack 读写数求和应等于全局完成数；读写数非零仅表示该 Stack 有完成事务，不表示负载均衡。
日常先对比每实例的读写数、带宽和延迟；温度分布看 thermal map，入口反压等内部计数
仅在 7.1.2 专项诊断中查看。需要时间线时再加
`--cmd-trace` 生成 dashboard，按 Stack 过滤器切换查看。

<a id="section-6-5"></a>

### 6.5 存储后端选择

三种后端共用同一个 `MemoryImage` 语义层，只改变 payload、初始化 mask 和
metadata 在宿主机上的保存方式。切换后端不应改变命令序列、完成数或读写数据。

| 后端 | 宿主保存方式 | 适用场景 | 人工查验重点 |
|---|---|---|---|
| `sparse` | 进程内只保存已触达 line | 低密度、快速功能/数据验证 | stdout + final txt/CSV；进程退出后无原始 backend |
| `mmap_sparse` | data/init/meta/present 四文件，由 OS 稀疏映射 | 大逻辑容量、持久化、稀疏实占 | 四件齐全、header、逻辑/实占大小、重开及 golden |
| `chunk_file` | 相同四文件 + 有界 chunk cache | 需明确限制主数据 cache 的文件后端 | 除上述项外，还要核对 chunk size/cache entries |

这三种是“存储媒介选择”，不是三套 DRAM 协议模型。功能对比应使用相同 profile、
trace、初始 image、容量和 line size；不应用原始 `.bin` 字节布局代替统一 final image 的语义比较。

#### 6.5.1 选择和最小运行方法

```bash
# 内存稀疏后端
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 \
  --memory-backend sparse

# mmap sparse 文件后端
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 \
  --memory-backend mmap_sparse --memory-capacity-bytes 1073741824 \
  --memory-data-file outputs/hbm4_mmap.bin

# chunk file 后端
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 \
  --memory-backend chunk_file --memory-capacity-bytes 1073741824 \
  --memory-data-file outputs/hbm4_chunks.bin \
  --memory-chunk-size 2097152 --memory-chunk-cache-entries 16
```

文件后端可能产生 `.init`、`.meta`、`.present` sidecar。更改 block size/容量/格式后应
使用新路径，避免把旧文件误当作兼容 checkpoint。多 stack 路径可用 `{stack}` 占位符；
未使用占位符时程序自动插入 `.stackN`。

<a id="section-6-5-2"></a>

#### 6.5.2 共同的可重放人工查验准备

下面使用 1 MiB 逻辑容量做人工查验，以减少全量导出和 metadata 扫描成本。三次
运行必须使用同一 `examples/memory_image.txt`、`examples/data_check.trace`、profile 和容量。
每次重做实验应换一个新目录，不要直接复用旧的文件后端。

```bash
mkdir -p outputs/backend_audit
backend_audit_out="$(mktemp -d outputs/backend_audit/single_hbm4_XXXXXX)"
mkdir -p "$backend_audit_out"/{sparse,mmap,chunk}
printf 'output directory: %s\n' "$backend_audit_out"

./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 \
  --memory-image examples/memory_image.txt --trace examples/data_check.trace \
  --memory-backend sparse --memory-capacity-bytes 1048576 \
  --cmd-trace "$backend_audit_out/sparse/commands.csv" \
  --dump-memory-image "$backend_audit_out/sparse/final.txt" \
  --dump-memory-csv "$backend_audit_out/sparse/final.csv" \
  --mismatch-report "$backend_audit_out/sparse/mismatch.txt" \
  --validate-cmd-trace --validate-dfi-trace \
  --stats-json "$backend_audit_out/sparse/stats.json" \
  | tee "$backend_audit_out/sparse/stats.txt"
```

首先查运行完整性，再查数据：

```bash
: "${backend_audit_out:?run the preceding 6.5.2 preparation block in this shell first}"
python3 tools/view_stats.py "$backend_audit_out/sparse/stats.json" \
  --keys memory_backend,completed_reads,completed_writes,remaining_requests,hit_cycle_limit,data_checked_reads,data_mismatches,storage_lines_allocated
sed -n '1,20p' "$backend_audit_out/sparse/final.txt"
sed -n '1,10p' "$backend_audit_out/sparse/final.csv"
sed -n '1,40p' "$backend_audit_out/sparse/mismatch.txt"
```

通过标准是 `remaining_requests : 0`、`hit_cycle_limit : false`、`data_mismatches : 0`，且
`cmd_validation`/`dfi_validation` 都为 `pass`；CSV 中的
address、data、initialized mask、version、writer 和物理坐标应与文本导出一致。`sparse` 是
进程内容器，退出后没有原始 backend 文件；`final.txt/final.csv` 就是它的人工审计产物。

下面的字段解释区分公共结果与可选内部诊断。普通运行先用 `completed_* + remaining_requests + hit_cycle_limit` 检查运行完整；
`data_mismatches/golden_mismatches` 证明数据比较结果（sparse 基线未使用 `--verify-golden`
时不会出现 `golden_mismatches`）；`storage_*` 和 `rowbuf_*` 说明实际分配/读写的 line 以及
ACT/RD/WR/PRE 驱动的 row-buffer 行为。`unique_written_lines`、`storage_*_line_accesses`、
`rowbuf_*` 属于专项诊断字段，下表仅保留其专项诊断含义（采集方法见 7.1.2）。

| 统计族 | 人工解读 |
|---|---|
| `memory_backend/capacity/line_bytes` | 本次真正使用的后端、逻辑容量和 backend line 粒度 |
| `completed_reads/writes` | 已完成的 DRAM transaction；需与 host request/拆分关系一起解释 |
| `storage_lines_allocated` | 最终 backend 中存在的 line 数，可包含由初始 image 带入的 line |
| `unique_written_lines` | 本次仿真中至少被写一次的不同 line 数 |
| `storage_read/write_line_accesses` | 存储语义层累计的 line 读/写访问次数，不是唯一 line 数 |
| `rowbuf_activations/precharges` | 由 ACT/PRE/自动预充驱动的 row-buffer 打开/关闭数 |
| `rowbuf_hits/misses` | 数据操作在当前 row-buffer 上的命中/未命中 |
| `rowbuf_dirty_writebacks` | 关闭 dirty row 时写回 MemoryImage 的 line 数 |

`final.txt` 首行是列说明，后续每行是一条已分配 line；`data=` 是 payload，`init=` 是逐 byte
初始化 mask，`version/last_writer/last_write_cycle` 用于重放溯源，后面是物理/热坐标。
`final.csv` 是同一语义的表格版。`mismatch.txt` 只有 `# cycle ... expected actual` 表头表示没有
错误记录，不是“没有生成”。对齐查看可用：

```bash
: "${backend_audit_out:?run the preceding 6.5.2 preparation block in this shell first}"
less -S "$backend_audit_out/sparse/stats.txt"
less -S "$backend_audit_out/sparse/final.txt"
column -s, -t < "$backend_audit_out/sparse/final.csv" | less -S
cut -d, -f1,2,3,5,6,7,13,18,19,20 \
  "$backend_audit_out/sparse/final.csv" | column -s, -t | less -S
less -S "$backend_audit_out/sparse/mismatch.txt"
```

`cut` 一行只选 address/data/init/version/writer/cycle/channel/bank/row/column，适合终端宽度；需要全列时
用第三条并在 `less` 中左右滚动，或用 `libreoffice --calc final.csv`/Excel/WPS 打开。

#### 6.5.3 `mmap_sparse` 的人工查验

```bash
: "${backend_audit_out:?run 6.5.2 in this shell first}"
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 \
  --memory-image examples/memory_image.txt --trace examples/data_check.trace \
  --memory-backend mmap_sparse --memory-capacity-bytes 1048576 \
  --memory-data-file "$backend_audit_out/mmap/data.bin" \
  --cmd-trace "$backend_audit_out/mmap/commands.csv" \
  --dump-memory-image "$backend_audit_out/mmap/final.txt" \
  --dump-memory-csv "$backend_audit_out/mmap/final.csv" \
  --mismatch-report "$backend_audit_out/mmap/mismatch.txt" \
  --verify-golden "$backend_audit_out/sparse/final.txt" \
  --validate-cmd-trace --validate-dfi-trace \
  --stats-json "$backend_audit_out/mmap/stats.json" \
  | tee "$backend_audit_out/mmap/stats.txt"

ls -lh "$backend_audit_out"/mmap/data.bin*
stat --format='%n logical_bytes=%s allocated_blocks=%b block_bytes=%B' \
  "$backend_audit_out"/mmap/data.bin*
od -An -tc -N8 "$backend_audit_out/mmap/data.bin.meta"
od -An -tx1 -N64 "$backend_audit_out/mmap/data.bin.meta"
python3 tools/view_stats.py "$backend_audit_out/mmap/stats.json" \
  --keys memory_backend,golden_verified,golden_mismatches,data_mismatches,storage_lines_allocated
diff -u "$backend_audit_out/sparse/final.txt" \
  "$backend_audit_out/mmap/final.txt"
```

`od -tc` 应显示 `H B M B A C K \0`；64-byte header 后跟每 line metadata。`stat` 中 `%s`
是逻辑文件大小，`%b×%B` 才是近似实际磁盘占用，稀疏文件两者可差很多。
`data.bin` 不能单独解读，还需 `.init`、`.meta`、`.present`；四件中任一缺失都不是
完整 checkpoint。`golden_mismatches : 0` 且 `diff` 无输出才证明其与 sparse 语义一致。
`golden_verified` 是实际与 golden 比较的 line 数，不是布尔 PASS；它应与本用例有效 final line
数一致，而是否通过由 `golden_mismatches : 0` 和进程退出码联合判定。

`data.bin` 保存 payload，`.init` 保存初始化状态，`.meta` 保存格式头和每 line metadata，
`.present` 标识哪些 line 实际存在。二进制文件不要用 `cat`；人工只看头部和宿主文件特性：

```bash
: "${backend_audit_out:?run 6.5.2 in this shell first}"
file "$backend_audit_out"/mmap/data.bin*
du -h "$backend_audit_out"/mmap/data.bin*
xxd -g 1 -l 128 "$backend_audit_out/mmap/data.bin.meta" | less -S
column -s, -t < "$backend_audit_out/mmap/final.csv" | less -S
less -S "$backend_audit_out/mmap/mismatch.txt"
```

`ls/stat` 中的逻辑大小说明可寻址文件范围，`du` 或 `allocated_blocks×block_bytes` 才近似说明
当前实占磁盘。`diff -u` 完全无输出就是相同；有 `+/-` 行才是差异。

另外做一次“关闭后重开”检查：用相同容量、line size 和四件路径重启程序，
但第二次不再重放写 trace，只将已持久化的状态导出到新文件：

```bash
: "${backend_audit_out:?run 6.5.2 and 6.5.3 in this shell first}"
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 --requests 0 \
  --memory-backend mmap_sparse --memory-capacity-bytes 1048576 \
  --memory-data-file "$backend_audit_out/mmap/data.bin" \
  --dump-memory-image "$backend_audit_out/mmap/reopened.txt" \
  --dump-memory-csv "$backend_audit_out/mmap/reopened.csv" \
  --stats-json "$backend_audit_out/mmap/reopen_stats.json" \
  | tee "$backend_audit_out/mmap/reopen_stats.txt"

diff -u "$backend_audit_out/mmap/final.txt" \
  "$backend_audit_out/mmap/reopened.txt"
```

程序能通过 header 检查并再次得到同样的 final image，才证明
持久化可重开。如果改了容量、transaction bytes 或当前磁盘格式，程序应拒绝不兼容
`.meta`，此时应换新路径，不应手工修改 header。

#### 6.5.4 `chunk_file` 的人工查验

```bash
: "${backend_audit_out:?run 6.5.2 in this shell first}"
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 \
  --memory-image examples/memory_image.txt --trace examples/data_check.trace \
  --memory-backend chunk_file --memory-capacity-bytes 1048576 \
  --memory-data-file "$backend_audit_out/chunk/data.bin" \
  --memory-chunk-size 65536 --memory-chunk-cache-entries 4 \
  --cmd-trace "$backend_audit_out/chunk/commands.csv" \
  --dump-memory-image "$backend_audit_out/chunk/final.txt" \
  --dump-memory-csv "$backend_audit_out/chunk/final.csv" \
  --mismatch-report "$backend_audit_out/chunk/mismatch.txt" \
  --verify-golden "$backend_audit_out/sparse/final.txt" \
  --validate-cmd-trace --validate-dfi-trace \
  --stats-json "$backend_audit_out/chunk/stats.json" \
  | tee "$backend_audit_out/chunk/stats.txt"

ls -lh "$backend_audit_out"/chunk/data.bin*
stat --format='%n logical_bytes=%s allocated_blocks=%b block_bytes=%B' \
  "$backend_audit_out"/chunk/data.bin*
od -An -tc -N8 "$backend_audit_out/chunk/data.bin.meta"
python3 tools/view_stats.py "$backend_audit_out/chunk/stats.json" \
  --keys memory_backend,golden_verified,golden_mismatches,data_mismatches,storage_lines_allocated
diff -u "$backend_audit_out/sparse/final.txt" \
  "$backend_audit_out/chunk/final.txt"
diff -u "$backend_audit_out/sparse/commands.csv" \
  "$backend_audit_out/mmap/commands.csv"
diff -u "$backend_audit_out/sparse/commands.csv" \
  "$backend_audit_out/chunk/commands.csv"
```

`chunk_file` 的人工数据判定仍以 `final.txt/final.csv`、golden 和 mismatch report 为准；
原始文件用于检查四件是否存在、header 是否正确、磁盘占用是否受控。内存上限的粗略主数据量是
`memory_chunk_size × memory_chunk_cache_entries`，不包括 metadata、bitmap 和 C++ 容器开销。
对齐查看与 mmap 相同：

```bash
: "${backend_audit_out:?run 6.5.2 in this shell first}"
file "$backend_audit_out"/chunk/data.bin*
stat --format='%n logical=%s allocated=%b*%B' \
  "$backend_audit_out"/chunk/data.bin*
xxd -g 1 -l 128 "$backend_audit_out/chunk/data.bin.meta" | less -S
column -s, -t < "$backend_audit_out/chunk/final.csv" | less -S
less -S "$backend_audit_out/chunk/mismatch.txt"
```

重开检查与 mmap 相同：必须原样提供四件路径和格式参数，但不重放写 trace，然后再导出文本做语义比较：

```bash
: "${backend_audit_out:?run 6.5.2 and 6.5.4 in this shell first}"
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 --requests 0 \
  --memory-backend chunk_file --memory-capacity-bytes 1048576 \
  --memory-data-file "$backend_audit_out/chunk/data.bin" \
  --memory-chunk-size 65536 --memory-chunk-cache-entries 4 \
  --dump-memory-image "$backend_audit_out/chunk/reopened.txt" \
  --stats-json "$backend_audit_out/chunk/reopen_stats.json" \
  | tee "$backend_audit_out/chunk/reopen_stats.txt"

diff -u "$backend_audit_out/chunk/final.txt" \
  "$backend_audit_out/chunk/reopened.txt"
```

#### 6.5.5 三种后端的最终判定表

| 检查面 | `sparse` | `mmap_sparse` | `chunk_file` | 通过条件 |
|---|---|---|---|---|
| 数据语义 | 文本/CSV | 文本/CSV + golden | 文本/CSV + golden | 三份 final image 一致，mismatch 为 0 |
| 协议行为 | stats/command trace | 同左 | 同左 | 完成数、命令数和 system cycles 一致 |
| 持久化 | 不适用 | 重开后导出 | 重开后导出 | 重开不丢数据 |
| 磁盘结构 | 无 | data + 3 sidecar | data + 3 sidecar | magic/header 正确，四件齐全 |
| 宿主资源 | RSS | `stat` 逻辑/实占 | `stat` + cache 设置 | 与选择后端的目标相符 |

原始 `.bin` 不要在三种后端之间做逐字节 `cmp`；它们的物理布局本来就不同。应比较
统一导出的 text/CSV、golden 结果和协议统计。若只有数据一致而 system cycles 不同，
需要检查是否复用了旧 backend，或三次运行的 config/CLI 并非完全一致。

<a id="section-6-5-6"></a>

#### 6.5.6 多 Stack 后端查验

多 Stack 时每个 stack 有独立 `MemoryImage` 和 backend。`--memory-capacity-bytes`
表示**每颗 Stack 的后端容量**，而 `--random-address-space-bytes` 表示合成 random
workload 可以使用的**聚合系统地址范围**。在当前 `interleaved` Stack 映射下，小容量后端必须满足：

```text
random_address_space_bytes <= stack_count × memory_capacity_bytes
```

下面固定为 6 Stack、每 Stack 1 MiB，因此聚合随机地址范围是
`6 × 1048576 = 6291456 B`。文件后端会重开已有状态，不能把上一次失败或其他用例留下的
文件混入本次查验；下面用 `mktemp -d` 每次创建新目录。多 Stack 的 backend 和导出路径均显式
使用 `{stack}`：

```bash
mkdir -p outputs/backend_audit
multi_backend_out="$(mktemp -d outputs/backend_audit/multi_6stack_1m_XXXXXX)"
printf 'output directory: %s\n' "$multi_backend_out"

./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 --stack-count 6 \
  --requests 1024 --random-address-space-bytes 6291456 \
  --memory-backend mmap_sparse \
  --memory-capacity-bytes 1048576 \
  --memory-data-file "$multi_backend_out/data_{stack}.bin" \
  --dump-memory-image "$multi_backend_out/final_{stack}.txt" \
  --dump-memory-csv "$multi_backend_out/final_{stack}.csv" \
  --mismatch-report "$multi_backend_out/mismatch.txt" \
  \
  --stats-json "$multi_backend_out/stats.json" \
  | tee "$multi_backend_out/stats.txt"
```

若改成 N Stack、每 Stack C Byte，应同步改为 `--stack-count N`、
`--memory-capacity-bytes C` 和不超过 `N×C` 的对齐随机范围。若希望 random 使用完整 HBM
标称容量，则不要把文件后端限制为 1 MiB；不能只缩小 backend 而继续在完整模型容量内取地址，
否则会正确报告 `request address range exceeds DRAM capacity`。

第一步检查仿真是否完整结束，以及所有 Stack 是否被当前 seed 触达：

```bash
: "${multi_backend_out:?run the preceding 6.5.6 simulation block in this shell first}"
python3 tools/view_stats.py "$multi_backend_out/stats.json" \
  --keys run_status,completed_reads,completed_writes,simulation_time_ns,achieved_bw_GBps,avg_read_latency_ns,row_hit_pct,data_mismatches
```

本例 HBM4 的 64 B HostRequest 会拆成两个 32 B transaction，故 1024 个 host request 应形成
2048 个 DRAM transaction。通过条件是 `remaining_requests=0`、`hit_cycle_limit=false`、
`data_mismatches=0`，并且所有 `stack_N_reads/writes` 分别求和后等于全局完成数。可执行检查：

```bash
: "${multi_backend_out:?run the preceding 6.5.6 simulation block in this shell first}"
python3 - "$multi_backend_out/stats.json" <<'PY'
import json
import pathlib
import sys

result = json.loads(pathlib.Path(sys.argv[1]).read_text())
stats = result["metrics"]
assert result["run_status"] == "completed"
assert len(result["stacks"]) == result["model"]["stack_count"]
assert sum(s["completed_reads"] for s in result["stacks"]) == stats["completed_reads"]
assert sum(s["completed_writes"] for s in result["stacks"]) == stats["completed_writes"]
assert stats["completed_reads"] + stats["completed_writes"] == stats["dram_transactions"]
assert stats["remaining_requests"] == stats["remaining_pending"] == 0
assert stats["hit_cycle_limit"] is False
assert result["validation"]["data_mismatches"] == 0
print("PASS: per-Stack totals equal global totals and the run completed")
PY
```

JSON 使用上面的 `stacks` 数组；`stack_N_reads/writes/bw_GBps/...` 仅是 diagnostic
文本及结果读取器的字段，不应拿来检索精简 stdout。第二步检查 6 套 backend、sidecar 和导出文件是否
彼此独立；每个 `.meta` 都应显示 `H B M B A C K \0`：

```bash
: "${multi_backend_out:?run the preceding 6.5.6 simulation block in this shell first}"
for stack in 0 1 2 3 4 5; do
  for file in \
    "$multi_backend_out/data_${stack}.bin" \
    "$multi_backend_out/data_${stack}.bin.init" \
    "$multi_backend_out/data_${stack}.bin.meta" \
    "$multi_backend_out/data_${stack}.bin.present" \
    "$multi_backend_out/final_${stack}.txt" \
    "$multi_backend_out/final_${stack}.csv"
  do
    test -s "$file" && echo "PASS $file" || echo "MISSING/EMPTY $file"
  done
done

for meta_file in "${multi_backend_out}"/data_*.bin.meta; do
  printf '%s: ' "$meta_file"
  od -An -tc -N8 "$meta_file"
done
```

第三步按字段查看每颗 Stack 的最终数据，避免 `data` 长字段使 `column` 输出难以阅读：

```bash
: "${multi_backend_out:?run the preceding 6.5.6 simulation block in this shell first}"
python3 - "$multi_backend_out" <<'PY'
import csv
import pathlib
import sys

root = pathlib.Path(sys.argv[1])
for path in sorted(root.glob("final_*.csv")):
    with path.open(newline="") as stream:
        rows = list(csv.DictReader(stream))
    print(f"\n[{path.name}] allocated_lines={len(rows)}")
    for row in rows[:5]:
        print("address=", row["address"], "stack=", row["stack"],
              "channel=", row["channel"], "bank=", row["bank"],
              "row=", row["row"], "data_prefix=", row["data"][:32])
PY
```

人工查验不能只打开 Stack 0。应对照 `stack_N_writes` 判断对应 `final_N.*` 是否理应含数据；
只有读、没有写或根本未被 workload 触达的 Stack，其导出较小不代表 backend 失效。若需要验证
同一 local 地址在不同 Stack 不会串扰，以及文件后端能够跨进程恢复，应运行：

```bash
make multistack-backend-smoke
```

该回归用不同 payload 定向写入 Stack 0/1 的相同 local 地址，再启动第二个进程读回，并覆盖
`sparse`、`mmap_sparse` 和 `chunk_file`。若手工比较不同后端，只比较相同输入下各 Stack 的
`final_N.txt`、golden 结果和统计；不要对物理布局不同的原始 `.bin` 执行 `cmp`。

<a id="section-6-6"></a>

### 6.6 Trace 与数据正确性

#### 6.6.1 Trace、HostRequest 与 DRAM transaction 层级

```text
一条普通 R/W trace
  → 一个 HostRequest（host_request_id）
  → 按 dram_transaction_bytes 拆成 1..N 个 Request
  → MemorySystem 选择 Stack，Controller 选择 command 序列
  → 每个 Request 形成 TransactionResponse
  → 同一 host 的全部 transaction 按 index 重组为 HostResponse
```

64 B host line 配 32 B transaction 会拆成两个 Request；`host_requests=1`、
`dram_transactions=2`，completed read/write 和 per-Stack read/write 采用 transaction 口径。
每个子事务拥有独立 `id`，共同保留 `host_request_id`，并通过
`transaction_index/transaction_count` 重组。burst `BW/BR len=N` 先按 host line 展开，
随后仍走相同 transaction 拆分。维护 trace 进入 priority/maintenance 路径，不产生
HostResponse。trace 首列 cycle 只是最早可注入时间；入口满时实际 arrival 会更晚。

CLI 通过公共 `MemorySystem::run(RequestSource&, ..., RunOptions)` 驱动
`try_submit → step → pop_response`，CSV 写出由响应回调完成；
`--response-trace` 导出真实 HostResponse，库调用者可选择 HostOnly/TransactionOnly/Both。
退出条件必须同时满足 traffic 耗尽、系统无在途 transaction、响应队列已 drain，不能只看
`idle()`。

#### 6.6.2 Trace 语法与数据检查

```text
0 W 0x1000 data=0011223344556677 mask=ffffffffffffffff
20 R 0x1000 expect=0011223344556677
100 BW 0x2000 256 pattern=inc stack=2 qos=7
300 BR 0x2000 256 check=last_write stack=2 qos=7
```

Trace 按空白分隔，空行和 `#` 注释被忽略。基本格式是 `[cycle] R|W address [key=value ...]`：

| 记号 | 含义 |
|---|---|
| `R` / `W` | 普通读/写；可选首列 cycle 是最早注入时间 |
| `data=HEX` | 写 payload；没有 payload 的合成写会由 request id/地址生成可重放数据 |
| `expect=HEX` | 读完成时独立比较 expected/actual |
| `mask=HEX` | 逐 byte 写 mask，非 0 byte 才更新相应位置 |
| `BW` / `BR` | burst write/read；地址后跟 byte 数，frontend 再拆成 DRAM transaction |
| `pattern=inc` | burst write 数据生成规则；也可使用明确 payload |
| `check=last_write` | burst read 与该地址最近一次写入比较 |
| `stack=N qos=N` | 显式目标 stack 和 QoS 类；未指定 stack 时按系统地址映射 |

`--trace` 存在时 synthetic `pattern` 被忽略。`--requests 0` 在 trace 模式下表示读取完整文件，
而不是“运行 0 条”；设为正数则限制展开后的请求数。

```bash
mkdir -p outputs
set -o pipefail
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 \
  --trace examples/data_check.trace --requests 0 \
  --dump-memory-image outputs/final_memory.txt \
  --dump-memory-csv outputs/final_memory.csv \
  --mismatch-report outputs/mismatch.txt \
  --fail-on-data-mismatch \
  --stats-json outputs/data_check_stats.json \
  | tee outputs/data_check_stats.txt
run_status=${PIPESTATUS[0]}
printf 'hbm_sim exit=%s\n' "$run_status"
```

当前 `examples/data_check.trace` 包含 3 写、4 读，覆盖同地址写后读、偏移写、mask 写和部分读。
在未修改示例和默认 HBM4 参数的前提下，公共 JSON 的预期核心值如下
（这是字段核对摘录，不是逐行照搬终端输出）：

```text
completed_reads               : 4
completed_writes              : 3
host_requests                 : 7
dram_transactions             : 7
data_checked_reads            : 4
data_mismatches               : 0
remaining_requests            : 0
hit_cycle_limit               : false
```

这些计数说明 7 条 trace 已完成、4 次带 expected 的读都通过、3 次写已 commit，且
不是周期上限截断。它们不会把最终 payload 全部打到 stdout；具体数据在三个文件中人工查看：
下面通过公共 JSON 查看完成/检查量，写入效果通过最终 image 核对；内部 commit 计数仅在专项诊断展示。

```bash
python3 tools/view_stats.py outputs/data_check_stats.json \
  --keys completed_reads,completed_writes,host_requests,dram_transactions,data_checked_reads,data_mismatches,remaining_requests,hit_cycle_limit

less -S outputs/final_memory.txt
column -s, -t < outputs/final_memory.csv | less -S
cut -d, -f1,2,3,5,6,7,13,18,19,20 \
  outputs/final_memory.csv | column -s, -t | less -S
less -S outputs/mismatch.txt
wc -l outputs/final_memory.txt outputs/final_memory.csv outputs/mismatch.txt
```

`final_memory.txt` 一行对应一个已分配 line，查 `address/data/init/version/last_writer`及物理坐标；
CSV 是同样内容的对齐表格。`mismatch.txt` 只有表头表示无 mismatch；如果有数据行，则每行给出
cycle/request/address/expected/actual 及物理位置。`wc -l` 用于区分“只有表头”和“有错误记录”。

如需二进制稀疏 checkpoint，将后缀换为 `.bin`：

```bash
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 \
  --trace examples/data_check.trace --requests 0 \
  --dump-memory-image outputs/final_memory.bin

file outputs/final_memory.bin
stat --format='%n bytes=%s' outputs/final_memory.bin
xxd -g 1 -l 128 outputs/final_memory.bin | less -S
```

`.bin` 适合用 `--memory-image` 重载或 `--verify-golden` 做语义检查，不适合通过肉眼解析全部结构；
需要人工看内容时应同时导出 txt/CSV。

数据不匹配且指定 `--fail-on-data-mismatch` 时退出码为 3；故障注入只想记录时使用
`--allow-data-mismatch`。后者允许返回 0，但 `data_mismatches` 和 mismatch report 仍必须按错误结果解读。

<a id="section-6-7"></a>

### 6.7 自定义配置

本节给出通用的完整流程；一款假定新 HBM4 的逐字段示例见
[附录 D：新 HBM4 自定义用例](#appendix-d)。

#### 6.7.1 第一步：复制对应家族主配置

HBM 从 `configs/hbm.cfg` 复制，LPDDR 从 `configs/lpddr.cfg` 复制。自定义文件必须完整、自包含；
无需先登记型号，也不依赖 `extends`、开发者配置或额外 preset。双 Stack HBM4 的起点是：

```bash
mkdir -p experiments/local outputs/my_case
cp configs/hbm.cfg experiments/local/my_hbm4.cfg
# 编辑副本的 [model] 和文件末尾 [override] 后检查：
./build-clang-debug/hbm_sim --config experiments/local/my_hbm4.cfg \
  --dump-resolved-config outputs/my_case/resolved.cfg --check-config
```

主配置已经包含 workload、Controller/row policy、地址映射、Stack 路由、PHY、
refresh/RFM、后端、ECC和功耗热的常用主输入；完整 Timing 从协议基准展开，无需重复抄表。
长期实验在完整副本 `[override]` 中保存差异；
一次性扫描变量放在 CLI。不要直接改仓库主配置，否则不同实验会共享临时覆盖值。固定合并
次序为：

```text
程序内置默认 < 公共段 < family < standard < preset < [override] < CLI
```

仓库已提供 `experiments/local/my_hbm4.cfg` 作为完整副本示例。下面只摘录需要修改的部分；
不是一份可以替代完整文件的短配置：

```ini
[model]
name = my_hbm4_two_stack_research  # 结果中的可读名称
base_standard = hbm4  # 选择 HBM4 标准语义

[override]
stack_count = 2  # 独立 Stack 数
pattern = random  # 合成随机流量
read_ratio = 70  # 读比例，百分比
seed = 2026  # 固定随机序列，便于重放
inject_interval = 2  # system tick
scheduler = frfcfs  # 调度算法
row_policy = open_page  # 行策略
memory_backend = sparse  # payload 保存方式
```

这只是“HBM4 语义派生的自定义研究模型”。如 organization/timing 没有目标器件
证据，必须保持 `validation_mode=exploratory`，不能命名为真实产品。

#### 6.7.2 第二步：先做小规模配置验证

```bash
mkdir -p outputs/my_case
./build-clang-debug/hbm_sim --config experiments/local/my_hbm4.cfg \
  --standard hbm4 \
  --dump-resolved-config outputs/my_case/resolved.cfg \
  --requests 128 --max-cycles 1000000 \
  --floorplan true --power-model true --thermal-model true \
  --cmd-trace outputs/my_case/commands.csv \
  --response-trace outputs/my_case/responses.csv \
  --dfi-trace outputs/my_case/dfi.csv \
  --dfi-signal-trace outputs/my_case/dfi_signal.csv \
  --dump-timing-table outputs/my_case/timing.csv \
  --dump-memory-image 'outputs/my_case/final_memory_{stack}.txt' \
  --dump-memory-csv 'outputs/my_case/final_memory_{stack}.csv' \
  --mismatch-report outputs/my_case/mismatch.txt \
  --dump-thermal-map 'outputs/my_case/thermal_map_{stack}.txt' \
  --validate-cmd-trace --validate-dfi-trace \
  --stats-json outputs/my_case/smoke_stats.json \
  | tee outputs/my_case/smoke_stats.txt
```

预期：退出码为 0，报告显示 completed、无剩余工作量、命令和 DFI 检查均为 pass；
组织显示双 Stack，末尾 Stack 表有 0、1 两行。机器判定读取 `smoke_stats.json` 的
`run_status/metrics/validation/stacks`，不对人读 stdout 做键值解析。若小规模不能完成，不要直接扩大 requests。

#### 6.7.3 第三步：用 CLI 自定义 workload、调度和地址模式

```bash
./build-clang-debug/hbm_sim --config experiments/local/my_hbm4.cfg \
  --standard hbm4 \
  --requests 50000 --seed 7 --pattern random --read-ratio 80 \
  --inject-interval 1 --scheduler frfcfs --row-policy open_page \
  --addr-mapping RoBaRaCoCh \
  --stats-json outputs/my_case/high_load_stats.json \
  | tee outputs/my_case/high_load_stats.txt
```

预期：CLI 值覆盖配置文件；更高注入率通常提高吞吐并增加排队延迟，新的地址映射会改变
活跃 channel 和 row hit/conflict 分布。比较时只能改变研究变量，其余配置和 seed 应固定。

这条命令按完整 50,000 请求运行，不是快速 smoke。Debug 构建加上多 Stack、功耗和热耦合
可能耗时数分钟甚至更久；stdout 的汇总在结束后才输出，`tee` 文件暂时为空不能证明死锁。
需要进度时追加 `--progress-interval 10000`，进度写到 stderr；性能测量优先使用已验证的
Release 构建，并保持相同配置/seed。不要通过随意缩减请求数后宣称原高负载实验已完成。

#### 6.7.4 第四步：选择 PHY 模式

```bash
# 正式行为级路径
./build-clang-debug/hbm_sim --config experiments/local/my_hbm4.cfg \
  --requests 1024 --mem-phy behavioral \
  --stats-json outputs/my_case/behavioral_stats.json \
  | tee outputs/my_case/behavioral_stats.txt

# 直接完成模式对照
./build-clang-debug/hbm_sim --config experiments/local/my_hbm4.cfg \
  --requests 1024 --mem-phy direct \
  --stats-json outputs/my_case/direct_stats.json \
  | tee outputs/my_case/direct_stats.txt
```

精简报告展示 PHY 模式、时间、带宽和延迟；Behavioral 的生命周期、FIFO、pipeline、
反压等内部计数仅在专项诊断打印，需要时按 7.1.2 采集。Direct 用于
隔离 PHY 影响。两者不能被解释为“真实 PHY”和“无 PHY”的器件级精度对比。

#### 6.7.5 第五步：选择存储后端

本节使用自包含的 `experiments/local/my_hbm4.cfg`，默认是 2 Stack、`random` workload。
为了在三种后端之间保持完全相同的地址空间，下面统一采用每 Stack 1 GiB 后端，并把 random
的聚合系统地址范围限制为 `2 × 1 GiB = 2147483648 B`。`--memory-capacity-bytes` 是
每 Stack 容量，不是两颗 Stack 的总容量。

文件后端会重开已有 data/sidecar；不能复用一次失败或已完成运行的持久文件。
下例由 `mktemp -d` 创建本次专用目录，并打印实际路径便于归档。

```bash
mkdir -p outputs/my_case
backend_case_out="$(mktemp -d outputs/my_case/backend_XXXXXX)"
mkdir -p "$backend_case_out"/{sparse,mmap,chunk}
printf 'output directory: %s\n' "$backend_case_out"

# 进程内 sparse；也显式使用相同的每 Stack 容量和聚合随机范围
./build-clang-debug/hbm_sim --config experiments/local/my_hbm4.cfg \
  --requests 1024 --random-address-space-bytes 2147483648 \
  --memory-backend sparse --memory-capacity-bytes 1073741824 \
  --dump-memory-image "$backend_case_out/sparse/final_{stack}.txt" \
  --dump-memory-csv "$backend_case_out/sparse/final_{stack}.csv" \
  \
  --stats-json "$backend_case_out/sparse/stats.json" \
  | tee "$backend_case_out/sparse/stats.txt"

# 持久 mmap；使用独立的新文件路径
./build-clang-debug/hbm_sim --config experiments/local/my_hbm4.cfg \
  --requests 1024 --random-address-space-bytes 2147483648 \
  --memory-backend mmap_sparse \
  --memory-capacity-bytes 1073741824 \
  --memory-data-file "$backend_case_out/mmap/data_{stack}.bin" \
  --dump-memory-image "$backend_case_out/mmap/final_{stack}.txt" \
  --dump-memory-csv "$backend_case_out/mmap/final_{stack}.csv" \
  --verify-golden "$backend_case_out/sparse/final_{stack}.txt" \
  \
  --stats-json "$backend_case_out/mmap/stats.json" \
  | tee "$backend_case_out/mmap/stats.txt"

# 有界 chunk cache
./build-clang-debug/hbm_sim --config experiments/local/my_hbm4.cfg \
  --requests 1024 --random-address-space-bytes 2147483648 \
  --memory-backend chunk_file \
  --memory-capacity-bytes 1073741824 \
  --memory-data-file "$backend_case_out/chunk/data_{stack}.bin" \
  --memory-chunk-size 2097152 --memory-chunk-cache-entries 16 \
  --dump-memory-image "$backend_case_out/chunk/final_{stack}.txt" \
  --dump-memory-csv "$backend_case_out/chunk/final_{stack}.csv" \
  --verify-golden "$backend_case_out/sparse/final_{stack}.txt" \
  \
  --stats-json "$backend_case_out/chunk/stats.json" \
  | tee "$backend_case_out/chunk/stats.txt"
```

检查三次运行的完成性、golden 和关键统计：

```bash
: "${backend_case_out:?run the preceding 6.7.5 simulation block in this shell first}"
python3 tools/view_stats.py \
  "$backend_case_out/sparse/stats.json" \
  "$backend_case_out/mmap/stats.json" \
  "$backend_case_out/chunk/stats.json" \
  --labels sparse,mmap_sparse,chunk_file \
  --keys run_status,completed_reads,completed_writes,remaining_requests,hit_cycle_limit,data_mismatches,golden_verified,golden_mismatches

for stack in 0 1; do
  diff -u "$backend_case_out/sparse/final_${stack}.txt" \
          "$backend_case_out/mmap/final_${stack}.txt"
  diff -u "$backend_case_out/sparse/final_${stack}.txt" \
          "$backend_case_out/chunk/final_${stack}.txt"
done
```

通过条件是三次运行的 `completed_reads/writes` 相同，均为 `remaining_requests=0`、
`hit_cycle_limit=false`、`data_mismatches=0`；两个文件后端还应为
`golden_verified>0`、`golden_mismatches=0`，且每颗 Stack 的 `final_N.txt` 与 sparse
基线无差异。后端只改变 payload/metadata 在宿主机中的保存方式，不应改变 DRAM 命令与周期。

文件后端的 data、`.init`、`.meta`、`.present` 必须作为一组管理。若 Stack 数改为 N、
每 Stack 后端容量改为 C，则 random 聚合范围必须同步设为不超过 `N×C` 的 `line_size`
对齐值。text/CSV、golden、magic/header、磁盘实占、重开和多 Stack 人工查验方法见
[6.5.2–6.5.6](#section-6-5-2)。

#### 6.7.6 第六步：查看运行过程和输出

```bash
# 关键完成/性能/验证指标
python3 tools/view_stats.py outputs/my_case/high_load_stats.json \
  --keys run_status,completed_reads,completed_writes,simulation_time_ns,achieved_bw_GBps,avg_read_latency_ns,row_hit_pct,data_mismatches

# 查看最终 Timing 及来源
sed -n '1,20p' outputs/my_case/timing.csv

# 查看命令/响应/DFI 文件头和少量事件
sed -n '1,8p' outputs/my_case/commands.csv
sed -n '1,8p' outputs/my_case/responses.csv
sed -n '1,8p' outputs/my_case/dfi.csv

# 数据和热输出
sed -n '1,20p' outputs/my_case/final_memory_0.txt
sed -n '1,20p' outputs/my_case/mismatch.txt
sed -n '1,20p' outputs/my_case/thermal_map_0.txt
```

预期：终端/`.txt` 是“模型—参数—结果”精简报告，机器表格来自 JSON；CSV 第一行为列名；没有数据错误时 mismatch 文件为空或只含
无错误摘要；thermal map 只有在启用 thermal 且发生存储事件时才有有效节点。

#### 6.7.7 第七步：生成和查看可视化

使用 6.7.2 预运行生成的统计、事件和双 Stack 热图：

```bash
python3 tools/visualize.py \
  --command-trace outputs/my_case/commands.csv \
  --dfi-trace outputs/my_case/dfi.csv \
  --stats outputs/my_case/smoke_stats.json \
  --thermal-map outputs/my_case/thermal_map_0.txt \
  --thermal-map outputs/my_case/thermal_map_1.txt \
  --title 'My HBM4 two-stack experiment' \
  --max-events 50000 \
  --out outputs/my_case/dashboard.html
xdg-open outputs/my_case/dashboard.html
```

页面可按 Stack/Channel/命令/请求过滤，并切换热图的 Stack 与 layer。
单 Stack 仿真只传实际生成的一份热文件；没有启用热模型时省略 --thermal-map。
详细操作见 [7.3](#section-7-3)。


---

<a id="chapter-7"></a>

## 7. 仿真输出与查看方法

<a id="section-7-1"></a>

### 7.1 stdout

stdout 默认只展示“模型—参数—结果”，适合终端阅读或 `tee` 留档。
实际标题为 `MODEL / PARAMETERS / RESULTS`；指标标签、状态说明和警告使用英文，
用户自定义的模型名称不做翻译。
`summary` 输出这份精简报告；统计工具、回归比较和可视化
读取同次运行导出的 JSON，不能解析 `stats.txt`。先做一次小规模运行：

```bash
mkdir -p outputs/run1
set -o pipefail

# 第一轮用小规模，并真正启用两个 validator
/usr/bin/time -f 'elapsed=%E exit=%x' \
  ./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 \
  --requests 256 --validate-cmd-trace --validate-dfi-trace \
  --progress-interval 10000 \
  2>outputs/run1/stderr.txt \
  --stats-json outputs/run1/stats.json \
  | tee outputs/run1/stats.txt
run_status=${PIPESTATUS[0]}
printf 'hbm_sim exit=%s\n' "$run_status"

# 可选：需要机器对齐表时只选择关心的指标，不必重复打印全部结果
python3 tools/view_stats.py outputs/run1/stats.json \
  --keys run_status,simulation_time_ns,achieved_bw_GBps,avg_read_latency_ns

# 编译/运行错误与 time 总结在 stderr 文件
less -S outputs/run1/stderr.txt
```

首先检查：退出码、`completed_reads/writes`、`remaining_requests`、
`cmd_validation`、`dfi_validation`、`data_mismatches`、`hit_cycle_limit`；再解释性能。

这条命令的运行过程是：读取配置和 profile → 生成 frontend 流量 → 逐 cycle 推进多
Controller/PHY/Stack → drain 请求和响应 → 运行 command/DFI validator → 末尾打印统计。
`--progress-interval 10000` 每 10000 个 system cycle 向 stderr 写一行
`progress cycle=... completed_reads=... completed_writes=... remaining_frontend=...`，所以可在
`stderr.txt` 中观察进展；最终聚合统计仍只在结束时写 stdout。上例保持
`--stats-view summary`，JSON 保存精简公共结果，不包含几百项内部计数。
因此既能精简终端，也不需要为可视化或统计工具重新启用 diagnostic。

这也是第一轮显式使用 `--requests 256` 的原因。完成性和 validator 均通过后，再去掉该
覆盖以使用 `configs/hbm.cfg` 中的 10000 requests。另一终端可用：

```bash
pgrep -af 'build-clang-debug/hbm_sim'
watch -n 1 'ps -o pid,etime,%cpu,%mem,rss,stat,cmd -C hbm_sim'
```

`PID` 是进程号，`ELAPSED` 是运行时间，`%CPU≈100` 通常表示正在占用一个核计算，`RSS`
是实驻内存，`STAT=R` 表示正在运行，`S` 表示当前等待/休眠。单次的 `R` 或 `S` 不能单独证明
是否死锁；要结合 elapsed 增长、CPU、错误输出和最终退出码。在 `watch` 窗口按 `Ctrl+C`
只停止 watch；要停止仿真，应在启动仿真的终端按 `Ctrl+C`。

精简报告/JSON 中 `cmd_validation`、`dfi_validation` 为 `not_run`（diagnostic 文本为 `off`）
只表示本次未运行 validator，不是验证通过。
要获得字段值 `pass`，必须像上例显式加 `--validate-cmd-trace --validate-dfi-trace`。性能值只在
`remaining_requests=remaining_pending=0`、`hit_cycle_limit=false`、退出码 0 且完成事务数
等于提交事务数的完整运行上解释。数据检查量为 0 时，即便错误为 0，也没有独立数据验证证据。

仅 diagnostic 保留专项英文 `key : value` 行；summary 为精简人读报告。
不要把未输出字段当成零。上例的 `--stats-json outputs/run1/stats.json` 保存分区机器结果；
显式 diagnostic 才会额外附加内部诊断字段，普通 metrics 不含全部配置。
`tools/view_stats.py` 与可视化的 `--stats` 可读取 JSON。运行状态、字段单位和实验接受条件见
[输出指南](文档/输出指南.md)。

#### 7.1.1 通用对齐查看速查

`less -S` 禁止长行自动折行，可用左右方向键水平滚动，按 `/text` 搜索，按 `q` 退出。
对运行产物统一处理如下：

| 文件类型 | 对齐/安全查看命令 | 说明 |
|---|---|---|
| stdout stats `.txt` | `less -S stats.txt` | 精简人读报告；使用 JSON 做机器字段检索 |
| 结果 `.json` | `python3 tools/view_stats.py stats.json --keys run_status,achieved_bw_GBps,avg_read_latency_ns` | 少量指标对齐查看；可视化也读这份文件 |
| 空白分隔 text | `column -t file.txt \| less -S` | 适合列数稳定的 trace/简单表；`key=value` 长行直接 `less -S` 更清楚 |
| CSV | `column -s, -t < file.csv \| less -S` | 本项目当前 CSV 字段不含嵌入逗号；超宽时先 `cut -d, -f...` 选列 |
| CSV 图形表格 | `libreoffice --calc file.csv` | 也可用 Excel/WPS；导入分隔符选逗号 |
| JSON | `python3 -m json.tool file.json \| less -S` | 先进行语法检查并缩进排版 |
| 二进制 `.bin/.meta` | `file FILE`; `stat FILE`; `xxd -g 1 -l 256 FILE \| less -S` | 不要 `cat`；只人工看 magic/头部，完整语义用模拟器重载/导出 |
| HTML | `xdg-open dashboard.html` | 无桌面时下载本地打开，或用 `python3 -m http.server` |
| 大文件 | `wc -l FILE`; `sed -n '1,20p' FILE`; `tail -n 20 FILE` | 先看规模和首尾，避免整个打印到终端 |

Markdown 表中的 `\|` 只是为了在文档单元格中显示 shell 管道，复制代码时实际命令使用普通 `|`。
`column -s,` 适用于本项目当前导出；如将来 CSV 字段允许引号内嵌逗号，应使用 Excel/WPS/
LibreOffice 或 Python `csv` 解析器，不要再用简单 `column -s,`。

<a id="section-7-1-2"></a>

#### 7.1.2 可选专项诊断：不属于普通仿真流程

只有排查队列、PHY 反压、低功耗转换、接口开销或后端内部访问计数时才需要本节。
内部字段的完整字典见[输出指南](文档/输出指南.md#full-fields)；附录 B 保留普通报告说明和基本分析方法。
以下是一个独立的小规模 HBM4 诊断例子：完整诊断只保存文件，终端仅查看选定的三项。

```bash
mkdir -p outputs/diagnostics
diagnostic_out="$(mktemp -d outputs/diagnostics/hbm4_XXXXXX)"
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 \
  --requests 128 --stats-view diagnostic \
  --stats-json "$diagnostic_out/diagnostic.json" \
  > "$diagnostic_out/diagnostic.txt"
diagnostic_status=$?
printf 'diagnostic exit=%s; files=%s\n' "$diagnostic_status" "$diagnostic_out"
if [ "$diagnostic_status" -eq 0 ]; then
  python3 tools/view_stats.py "$diagnostic_out/diagnostic.json" \
    --keys phy_command_backpressure,phy_data_backpressure,read_q_avg_per_ctrl
fi
```

这不是对之前仿真的补充读取，而是一次新仿真。要诊断某个自定义/对照用例，必须复用
其实际 config、CLI 覆盖、请求数、seed、trace 和初始 image，并把所有输出改到新目录。
不能把上面 128 请求的诊断数拼入另一轮性能结果；文件后端尤其不能重用已有 data/sidecar
假装从相同初态重放。`--dump-resolved-config` 可保存本次最终参数，但不替代输入 trace/image。
只查配置和 Timing 来源时直接看 resolved.cfg、timing.csv，不需要重跑 diagnostic。

本节是唯一主动展开内部诊断输出的手工入口；专项验证脚本可能在内部采集诊断文件，
那是测试证据，不要求用户把文件全部打印到终端。普通流程始终保留 summary 和公共 JSON。

<a id="section-7-2"></a>

### 7.2 可选产物

| 参数 | 产物 | 用途 |
|---|---|---|
| `--stats-json` | JSON schema 2 | 精简机器结果、有效性、变化清单和每 Stack 结果；普通模式无 diagnostics 分区 |
| `--dump-resolved-config` | cfg | 全部实际生效参数，用于复现；不挤进日常结果表 |
| `--cmd-trace` | command CSV | 命令时间线、地址、stack/channel/bank |
| `--response-trace` | HostResponse CSV | host 聚合完成、状态、payload |
| `--transaction-response-trace` | TransactionResponse CSV | 子事务 ID、拆分序号、路由、完成状态和 payload |
| `--dfi-trace` | DFI beat CSV | 命令/数据 beat、expected/actual |
| `--dfi-signal-trace` | signal-like CSV | DFI-oriented 信号事件审计 |
| `--dump-timing-table` | timing CSV | 最终值、单位、来源 |
| `--dump-memory-image` | txt/bin | 最终稀疏数据与 metadata |
| `--dump-memory-csv` | CSV | Excel/脚本查看数据状态 |
| `--mismatch-report` | text | expected/actual 和定位信息 |
| `--dump-thermal-map` | text | 稀疏热 grid/tile |

一个同时生成所有主要产物的实际命令如下。HBM 主配置默认启用 sparse 存储、floorplan、功耗、
热和 payload ECC shadow；trace 与初始 image 仍由命令显式传入，不能只加载配置后就假定存在
数据闭环：

```bash
mkdir -p outputs/full_artifacts
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 \
  --memory-image examples/memory_image.txt --trace examples/data_check.trace \
  --cmd-trace outputs/full_artifacts/commands.csv \
  --response-trace outputs/full_artifacts/responses.csv \
  --transaction-response-trace outputs/full_artifacts/transaction_responses.csv \
  --response-delivery-mode both \
  --dfi-trace outputs/full_artifacts/dfi.csv \
  --dfi-signal-trace outputs/full_artifacts/dfi_signal.csv \
  --dump-timing-table outputs/full_artifacts/timing.csv \
  --dump-memory-image outputs/full_artifacts/final_memory.txt \
  --dump-memory-csv outputs/full_artifacts/final_memory.csv \
  --mismatch-report outputs/full_artifacts/mismatch.txt \
  --dump-thermal-map outputs/full_artifacts/thermal_map.txt \
  --validate-cmd-trace --validate-dfi-trace \
  --stats-json outputs/full_artifacts/stats.json \
  | tee outputs/full_artifacts/stats.txt
```

运行后按产物类型查看：

```bash
# 文件是否存在、规模是否合理
ls -lh outputs/full_artifacts
wc -l outputs/full_artifacts/commands.csv \
       outputs/full_artifacts/responses.csv \
       outputs/full_artifacts/dfi.csv

# 最终 timing 及来源
sed -n '1,20p' outputs/full_artifacts/timing.csv

# 命令、DFI、响应的表头和前几个事件
sed -n '1,8p' outputs/full_artifacts/commands.csv
sed -n '1,8p' outputs/full_artifacts/dfi.csv
sed -n '1,8p' outputs/full_artifacts/responses.csv

# 数据正确性、最终内容和热点
python3 tools/view_stats.py outputs/full_artifacts/stats.json \
  --keys cmd_validation,dfi_validation,data_checked_reads,data_mismatches,ecc_uncorrectable_errors
sed -n '1,20p' outputs/full_artifacts/mismatch.txt
sed -n '1,20p' outputs/full_artifacts/final_memory.txt
sed -n '1,20p' outputs/full_artifacts/thermal_map.txt
```

预计效果：命令 CSV 至少反映实际发出的 ACT/WR，并按行状态/读转发情况出现 RD/PRE；不能要求
每种命令都非零。DFI CSV 含 command/data beat，response CSV
按 host 请求聚合，timing CSV 显示最终 nCK 和来源，memory dump 反映 trace 写入后的数据，
`cmd_validation` 与 `dfi_validation` 应为 `pass`，非故障注入用例的 `data_mismatches` 应为 0。

完整 command/DFI trace 可能非常大，只应对有限窗口启用。性能长跑优先保留统计、性能 JSON
和抽样 trace；若必须保留全量 trace，应同时估算磁盘空间和后处理时间。

<a id="section-7-3"></a>

### 7.3 任意仿真的离线可视化

#### 7.3.1 先确认可视化工具本身可用

```bash
make visualize-example
xdg-open outputs/visualization/example/dashboard.html
```

该入口使用项目固定示例，只用于确认 Python 生成器、HTML 和浏览器工作正常。真正分析自己的
仿真时，不应反复查看 example，而应把本次仿真的产物显式传给 `tools/visualize.py`。

#### 7.3.2 为任意仿真准备独立产物目录

下面的模板适用于任意 `--config`；只需修改 `SIM_OUT` 和配置文件：

```bash
SIM_OUT=outputs/visualization/my_experiment
mkdir -p "$SIM_OUT"

./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 --requests 512 \
  --floorplan true --power-model true --thermal-model true \
  --cmd-trace "$SIM_OUT/commands.csv" \
  --dfi-trace "$SIM_OUT/dfi.csv" \
  --dfi-signal-trace "$SIM_OUT/dfi_signal.csv" \
  --dump-thermal-map "$SIM_OUT/thermal_{stack}.txt" \
  --validate-cmd-trace --validate-dfi-trace \
  --stats-json "$SIM_OUT/stats.json" \
  | tee "$SIM_OUT/stats.txt"
```

其中 command trace 是必需输入；DFI、stats、thermal map 和 performance JSON 都是可选
增强项。若采用 Direct PHY，DFI 面板可能比 Behavioral 少；若关闭 thermal，热图为空是
正确结果，不是 dashboard 故障。

#### 7.3.3 可选：生成性能曲线

性能曲线脚本执行一组确定性的标准/注入间隔 sweep，它不是单次 trace 的时间线。只有在
dashboard 需要同时展示饱和趋势时才生成：

```bash
: "${SIM_OUT:?run 7.3.2 in this shell first}"
python3 tools/performance_curve.py \
  --binary ./build-clang-debug/hbm_sim \
  --standards hbm4 \
  --intervals 1,2,4,8,16 \
  --read-ratios 50,100 \
  --requests 512 --pattern random --seed 2026 \
  --csv-out "$SIM_OUT/performance.csv" \
  --json-out "$SIM_OUT/performance.json"
```

预计得到多组 `offered_requests_per_tick`、`achieved_bw_GBps` 和
`avg_read_latency_ticks`。若只分析自定义 vendor profile，当前脚本的标准入口不自动继承
该 profile，应把性能曲线作为标准基线，或另行扩展脚本后再声称是自定义器件曲线。

#### 7.3.4 生成自包含 HTML

```bash
: "${SIM_OUT:?run 7.3.2 in this shell first}"
python3 tools/visualize.py \
  --command-trace "$SIM_OUT/commands.csv" \
  --dfi-trace "$SIM_OUT/dfi.csv" \
  --stats "$SIM_OUT/stats.json" \
  --performance-json "$SIM_OUT/performance.json" \
  --thermal-map "$SIM_OUT/thermal_0.txt" \
  --title 'HBM4 custom experiment' \
  --max-events 50000 \
  --out "$SIM_OUT/dashboard.html"

xdg-open "$SIM_OUT/dashboard.html"
```

如果跳过了 7.3.3，上述命令也要删去
`--performance-json "$SIM_OUT/performance.json"`，否则工具会因输入文件不存在而报错。

`dashboard.html` 已嵌入采样事件和统计，无需 HTTP 服务，可复制到另一台机器直接打开。
`--max-events` 限制嵌入 HTML 的 command event 数；解析器先流式统计、再做有界等距采样，
不会为了采样而把全量 CSV 先构造成 Python 对象。页面会注明显示全量还是确定性采样。

7.3.2默认只有一个Stack，不会生成thermal_1.txt。要验证多Stack，先在独立目录运行
同一配置的双Stack用例，再重复传入两份热图；不能把其他运行的热图拼接到单Stack结果中：

```bash
: "${SIM_OUT:?run 7.3.2 in this shell first}"
MULTI_SIM_OUT="${SIM_OUT}_multistack"
mkdir -p "$MULTI_SIM_OUT"
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 \
  --stack-count 2 --requests 512 \
  --cmd-trace "$MULTI_SIM_OUT/commands.csv" \
  --dump-thermal-map "$MULTI_SIM_OUT/thermal_{stack}.txt" \
  --validate-cmd-trace --validate-dfi-trace \
  --stats-json "$MULTI_SIM_OUT/stats.json" \
  | tee "$MULTI_SIM_OUT/stats.txt"
python3 tools/visualize.py \
  --command-trace "$MULTI_SIM_OUT/commands.csv" --stats "$MULTI_SIM_OUT/stats.json" \
  --thermal-map "$MULTI_SIM_OUT/thermal_0.txt" \
  --thermal-map "$MULTI_SIM_OUT/thermal_1.txt" \
  --out "$MULTI_SIM_OUT/dashboard_multistack.html"
```

#### 7.3.5 页面操作方法

1. 在 **Run summary** 先核对 standard、PHY 模式、完成量、系统周期和 validation；身份不对
   时不要继续分析图。
2. 在 **Trace explorer** 先点击 overview 中的密集区域缩小周期范围，再用 Start/End 精确
   设定窗口；输入框清空表示恢复数据实际范围。
3. 用 Stack、Channel、Command 过滤器隔离目标位置；多 Stack 用例先比较各 stack，再观察
   单个 channel。`Lanes` 控制同时显示的 bank/request 行数。
4. **Command / bank view** 用于看 ACT→RD/WR→PRE、refresh/RFM 插入和 bank 并行；
   **Request swimlanes** 用于看一个 host request 从首命令到完成相关命令的生命周期。
5. 鼠标点击彩色事件，底部详情会显示 cycle、request、stack、channel、PC、SID、BG、bank、
   row 和 bus；键盘 `+`/`-` 可缩放当前周期窗口。
6. **Open command CSV** 可替换为另一轮 command trace，快速比较时间线；替换后 DFI/统计仍
   属于原 HTML，因此只用它比较命令，不要混合解释其他面板。
7. 在 **Command mix** 检查 ACT/PRE 与 RD/WR 比例；ACT/PRE 过多通常表示行冲突或 closed-page，
   refresh/RFM 比例异常则检查维护策略和运行长度。
8. 在 **DFI activity** 核对命令事件、读写 beat 和 payload byte 是否与 transaction 大小匹配。
9. 在 **Injection-rate / latency / throughput** 看实线吞吐是否趋于饱和、虚线延迟是否在饱和点
   附近快速上升；延迟使用独立比例尺，不能直接和 GB/s 的纵坐标比较。
10. 在 **Thermal map** 先选择 Stack、再选择 layer，寻找温度集中区域。颜色对应 thermal
    grid 的绝对温度；`x/y` 是热网格坐标，不是 floorplan tile 坐标。只提供一个 thermal
    文件时 Stack 下拉框自然只有一个值。

#### 7.3.6 从图得到可审计结论

| 观察 | 可能含义 | 必须回查的证据 |
|---|---|---|
| 同一 bank ACT/PRE 很密集 | 行冲突或关闭页策略 | `row_hits/misses/conflicts`、地址映射 |
| RD/WR 切换后出现空窗 | 总线换向 timing | `nRTW/nWTRS/nWTRL`、command CSV |
| 某 stack 事件显著更多 | stack mapping/QoS 不均衡 | `stack_N_*`、system/local address |
| PHY 面板有反压 | FIFO/pipeline 或低功耗状态阻塞 | `phy_max_command_fifo`/`phy_max_read_fifo`/`phy_max_write_fifo`、两项 `phy_*_backpressure`、低功耗命令与计数 |
| 吞吐饱和且延迟陡增 | 系统进入排队饱和区 | injection stall、queue average、完成率 |
| 热点与命令热点重合 | 访问局部性和命令能量集中 | thermal map、bank lane、power 分项 |

可视化是定位和解释工具，不单独构成正确性证据；结论必须回到统计字段、CSV 原始事件和验证器。

<a id="section-7-3-7"></a>

#### 7.3.7 空白或异常页面排查

```bash
: "${SIM_OUT:?run 7.3.2 in this shell first}"
# 必需 trace 是否非空、表头是否正确
test -s "$SIM_OUT/commands.csv" && sed -n '1,3p' "$SIM_OUT/commands.csv"

# 可选输入是否真实存在
ls -lh "$SIM_OUT/stats.json" "$SIM_OUT/dfi.csv" "$SIM_OUT/thermal_0.txt"

# HTML 是否生成并嵌入数据
test -s "$SIM_OUT/dashboard.html"
rg -o '"totalCommands":[0-9]+|"sampled":(true|false)' "$SIM_OUT/dashboard.html"

# 重新生成时先看工具给出的 timeline=嵌入数/总数
python3 tools/visualize.py --command-trace "$SIM_OUT/commands.csv" \
  --stats "$SIM_OUT/stats.json" --out "$SIM_OUT/dashboard_debug.html"
```

若 Run summary 有值但时间线空白，检查 Start/End、Stack/Channel/Command 过滤器；若只有
曲线或热图空白，检查是否分别提供了 performance JSON 或启用了 thermal；若浏览器仍报错，
打开开发者工具 Console 检查本地文件权限、损坏 CSV 或旧页面缓存。

---

<a id="chapter-8"></a>

## 8. 平台与模型验证

<a id="section-8-1"></a>

### 8.1 当前模型完成度

当前项目已经达到“可独立构建、可运行四标准、可进行多 Stack/PHY/真实数据实验、可导出并
验证产物”的软件研究模型阶段；尚未达到“指定厂家器件的电气级、功耗温度绝对值级校准”阶段。

| 模块面 | 当前已经实现 | 自动/可重放证据 | 当前成熟度与剩余边界 |
|---|---|---|---|
| 构建与库 | C++20 core library、CLI、Clang/G++、CMake/Ninja、Make | CMake build、CTest | 软件结构完整；无第三方 C++ 运行依赖 |
| 四标准入口 | HBM3/HBM4/LPDDR5/LPDDR6 traits、profile、配置、CLI | smoke、model validation | 入口和公共行为可回归；部分参数是研究基线 |
| Frontend | synthetic、trace、burst、payload/mask/expect、流式生成 | sequence/model tests | 已实现；不直接模拟 CPU cache/coherence |
| 多 Stack 平台 | system/local 地址、interleaved/blocked、ingress、QoS、per-stack 统计 | sequence tests、四个 demo、`multistack_backend_smoke` | 主动多 Stack 已实现；外部 NoC/UCIe 协议需适配 |
| Controller | FCFS/FR-FCFS、行策略、状态机、表驱动 timing、刷新/RFM/低功耗 | sequence、timing boundary、validator | 命令级研究可用；器件私有策略需校准 |
| Mem PHY | Direct/Behavioral、生命周期、FIFO、pipeline、HBM/LPDDR adapter、DFI event | `phy_tests`、`phy_smoke`、DFI validator | 行为级完整；不是 pin/electrical PHY |
| Mem Stack | per-stack MemoryImage、row buffer、payload、mask/init、物理坐标 | model/sequence tests | 数据和结构语义已闭环；非晶体管阵列模型 |
| 存储后端 | sparse、mmap_sparse、chunk_file、持久 sidecar | model validation | 三后端语义已实现；宿主 I/O 不代表 DRAM 时延 |
| 数据正确性 | 写后读、expect、golden、mismatch、SECDED shadow | sequence/model tests | 研究级数据闭环；ECC/RAS 非完整硅实现 |
| 功耗与热 | configured pJ、IDD 公式、行为级稀疏三维耦合、TSV coupling | model validation、DRAMsim3 辅助工具 | 趋势可用；不是完整 RC/有限元模型，绝对值须目标器件/封装校准 |
| 输出与可视化 | stats、command/response/DFI、timing、memory、thermal、dashboard | visualization smoke | 离线审计链已实现；不含实时 Web 服务 |
| 外部验证 | Ramulator2 差分、DRAMsim3 辅助、性能曲线、敏感性区间 | Python tools | 方法和工具已实现；外部结果取决于本地参考模型 |

当前 CMake 的回归入口如下，最终数量以 `ctest --test-dir build-clang-debug -N` 为准：

| CTest 名称 | 主要证明内容 |
|---|---|
| `sequence_tests` | 命令序列、数据、请求响应、多 Stack 等 C++ 契约 |
| `timing_boundary_tests` | 四标准核心约束的 `t-1/t` 边界 |
| `phy_tests` | PHY 生命周期、FIFO、adapter 和数据完成 |
| `config_tests` | 2 个主配置、4 个验证配置的清单/同行注释，自包含研究示例、验证继承、preset/类型/范围校验及 resolved config 往返 |
| `phy_smoke` | CLI Behavioral PHY 路径 |
| `visualization_smoke` | 实际 CLI 产物可被 dashboard 生成器消费 |
| `multistack_backend_smoke` | HBM4 双 Stack 在 sparse/mmap/chunk 下隔离，文件后端跨进程重开仍保持数据 |
| `multistack_demos_smoke` | 四种标准的 2-Stack demo、resolved config、验证字段和 dashboard 均可生成 |
| `architecture_sweep_smoke` | 定向 trace 跑各标准真实 JEDEC 密度档与组织扰动组；完成性、容量核算、组织漂移、基线稳定性、行为不变性、地址编码和 refresh scope 自动检查通过 |
| `smoke` | 四标准及主要 CLI 功能集合 |
| `timing_boundary_validation` | C++ probe 的边界矩阵审计 |
| `model_validation` | 项目身份、数据、后端、DFI、物理/功耗/热和门槛 |
| `performance_curve_validation` | CI 规模注入率—延迟—吞吐曲线 |
| `sensitivity_uncertainty_validation` | Timing 敏感性和联合扰动区间 |

使用下一节命令列出和运行当前测试，并保存本次结果。模型能力与边界如下：

```text
软件工程与可运行性        已完成并有默认回归
周期/命令/事务数据模型    已完成研究级实现
多 Stack + Behavioral PHY 已完成研究级实现
可重放输出与离线可视化    已完成
开源参考差分方法与工具    已完成，结果需本地参考仓库
指定厂商器件参数校准      未完成
真实电气 PHY/封装 SI      不在当前模型范围
硬件级绝对功耗/温度校准   未完成
```

<a id="section-8-2"></a>

### 8.2 可信度证据链

验证采用分层证据：

1. **编译与静态质量**：Clang/G++、warning、clang-tidy；
2. **单元和序列测试**：地址、状态、命令、数据、response、PHY；
3. **Timing 边界**：每个核心约束 `t-1` 禁止、`t` 允许；
4. **内部闭环**：写后读、mask、expect、golden、ECC、命令和 DFI validator；
5. **不变量**：完成数、容量、地址范围、系统/stack 聚合关系、后端一致性；
6. **外部差分**：Ramulator2.1 的共同命令/时序面；
7. **辅助交叉验证**：DRAMsim3 的较老共同协议、IDD 功耗公式和热趋势；
8. **性能曲线**：注入率—延迟—吞吐饱和趋势；
9. **敏感性/不确定性**：Timing 单因素及联合扰动；
10. **最终校准**：厂家模型、RTL、FPGA 或硬件测量。

证据强度从“软件是否按设计运行”逐步提升到“是否能代表目标器件”。默认回归可以证明当前
实现没有破坏已覆盖行为，但只有目标厂家模型或硬件数据才能完成器件级数值校准。

下表是复查模型时应逐项使用的判定矩阵。“PASS”只能支持该行所列的结论，不能跨级推导。

| 验证层 | 原理与激励 | PASS 判定 | 失败主要说明 | 应保存的证据 | 能支持的结论 |
|---|---|---|---|---|---|
| 配置与解析 | 遍历四标准，检查主配置→验证继承→family→standard→preset→override→CLI 覆盖顺序并将 resolved config 重新读入 | 类型、范围、枚举、6 份 configs cfg 清单及同行注释检查成功；4 份 cfg Demo 自包含且覆盖对应主配置全部字段；resolved 往返有效 | 解析器、层级优先级、配置 schema 或示例选型存在不一致 | `config_tests` 日志、每次运行的 `resolved.cfg` | 运行确实使用了声明的参数；不证明参数属于某厂器件 |
| 单元/序列契约 | 对地址映射、请求拆分聚合、行状态、命令序列、response、PHY 生命周期给定确定输入 | 所有断言与预期事件序列一致 | 某个软件契约被破坏，需按首个失败断言定位 | CTest 日志、最小失败序列 | 已覆盖路径按项目定义运行 |
| Timing `t-1/t` 边界 | 对每个核心时序分别在最早合法周期前一拍和该拍尝试发令 | `t-1` 必须被拒绝，`t` 必须允许，命令条件及 scope 与预期相同 | timing 换算、same/different BG/SID、总线换向或状态条件实现错误 | `timing_boundary_tests` 和 `timing_boundary_validation` 矩阵 | 这些约束在离散 tick 上实现正确；参数本身仍需来源声明 |
| 在线 command/DFI 验证 | 每拍对已发命令重做状态/timing 检查，对 Behavioral PHY 检查 DFI 命令、write-data、read-return 顺序 | `cmd_validation=pass`、`dfi_validation=pass`，进程正常退出且无未配对事件 | scheduler/PHY 发出了与自身规则矛盾的事件 | `stats.txt`、`commands.csv`、`dfi.csv`、`timing.csv` 和 stderr/退出码 | 实际运行轨迹满足当前实现的规则 |
| 数据/ECC/后端闭环 | 写入 payload 后读回，检查 byte mask、expect/golden、未初始化读及 SECDED shadow；对 sparse/mmap/chunk 做同语义对照 | 完成数一致、`data_mismatches=0`、后端重开后数据与 sidecar 仍一致 | transaction 完成、mask 合并、持久化、地址隔离或 ECC 契约存在错误 | response/memory trace、stats、后端文件和 sidecar | 功能数据语义闭环；不等于完整硬件 RAS/ECC |
| 多 Stack 与聚合不变量 | 将地址定向不同 stack，对 interleaved/blocked、per-stack ingress/QoS、独立后端及统计求和进行比较 | 每个 stack 均有请求/产物，互不污染，系统计数等于各 stack 之和 | 地址联动、stack 实例化、文件命名或聚合口径错误 | 四个 demo 目录、`multistack_backend_smoke` 日志、per-stack stats/thermal | 平台确实主动仿真 N Stack；不证明实物封装/NoC 布线 |
| 架构趋势实验 | 分别使用定向 trace 扫描三组 bank、三组 row/column、per-bank/all-bank refresh，其余变量尽量固定 | 全部 case 完成且无 mismatch；bank 并行性、几何行局部性、refresh scope 的预期方向检查均 PASS | workload 未激发对应结构，变量未隔离，或实现趋势与建模因果矛盾 | `results.csv`、`checks.csv`、`summary.md`、`trends.html` 及每 case 的 trace/resolved/stats | 对已实现机制的定性趋势正确；不是厂商性能复现 |
| 注入率—延迟—吞吐 | 在同一 profile 下递增 offered load，记录完成吞吐和排队/读延迟 | 低负载可完成，吞吐随负载上升后趋于饱和，饱和区延迟不会反常降低 | 注入器、反压、完成口径、带宽换算或 cycle limit 有问题 | performance JSON/CSV 和 dashboard 曲线 | 模型具有排队与饱和的自洽行为 |
| 敏感性/不确定性 | 对时序参数做单因素与联合扰动，比较延迟、带宽、命中/冲突和热功耗 | 输出可重放的参数范围、基线和响应区间，无失败 case/非法值 | 参数对实现未生效、单位错误或模型响应不连续 | sensitivity JSON/CSV/摘要 | 结论在声明的参数区间内有稳健性，不代表厂商公差分布 |
| Ramulator2 外部差分 | 两边对齐 workload/组织/timing，对共同面的解码、命令、issue cycle、latency 做规范化比较 | 所有 supported scenario 在显式容差内一致；参考 commit/dirty 状态已记录 | 共同抽象面不一致，或双方规范化口径未对齐 | `ramulator2_diff.json`、双方 trace/config、参考 Git 身份 | 与该版本开源模型的共同抽象面一致 |
| DRAMsim3 辅助对照 | 对较老共同协议及可对齐的 IDD/温度趋势使用另一开源实现 | 规范化口径下趋势同向，差异项有参数/方法解释 | 公式、单位、活动计数或热边界设定需复核 | 工具 JSON/CSV、两边版本与参数表 | 通用公式/趋势获得第二来源支持；不验证 HBM4/LPDDR6 全协议 |
| 厂家/RTL/硬件校准 | 对目标 speed-bin、RL/WL、refresh、IDD、PHY 训练、带宽/延迟/温度实测做参数拟合与留出数据复验 | 在预先声明的误差、工况和留出集上达标 | 研究参数或抽象不足以代表目标器件 | 原始测量、校准/留出报告、版本与误差预算 | 只有此层才能声明目标器件级定量精度；当前尚未完成 |

<a id="section-8-3"></a>

### 8.3 默认回归

```bash
cmake --build build-clang-debug --parallel
ctest --test-dir build-clang-debug --output-on-failure
```

CTest 条目以当前 `CMakeLists.txt`/`ctest -N` 为准，不在文档中固定编译步骤数量。
验证结论以本次测试的退出状态和实际日志为依据。

<a id="section-8-4"></a>

### 8.4 项目内验证

```bash
python3 tools/model_validation.py \
  --binary ./build-clang-debug/hbm_sim \
  --json-out outputs/model_validation.json

python3 tools/timing_boundary_validation.py \
  --probe ./build-clang-debug/timing_boundary_tests
```

项目内验证检查四标准入口、命令序列、数据、full-stack、DFI、物理/功耗/热、来源和项目身份。

<a id="section-8-5"></a>

### 8.5 Ramulator2 差分

下文的 `/path/to/ramulator2` 和 `/path/to/DRAMsim3` 都是可移植写法中的占位符，运行前必须
换成本机对应仓库的根目录；它们不是本项目内置路径。

```bash
python3 tools/ramulator2_differential.py \
  --binary ./build-clang-debug/hbm_sim \
  --ramulator-root /path/to/ramulator2 \
  --json-out outputs/ramulator2_diff.json
```

差分是“同 workload、同组织、同 timing、同统计口径下比较共同可观察行为”的方法。
它适合发现命令间隔、地址解码、same/different BG、总线换向、tFAW、refresh/RFM、
自动预充等错误。对方不支持的 HBM4/LPDDR6 行为必须标为 unsupported/项目扩展，不能
为了数值一致而删除本项目协议特性。

工具按标准加载对应的 `configs/validation/*.cfg`，选择其中的
`ramulator2_reference_1ch`。这些配置只负责把本项目的共同抽象面对齐；脚本随后会独立
生成 Ramulator2 输入并启动其可执行程序，不是在本项目中换一个 preset 后比较自己：

| 标准 | master / preset | 主要规范化 |
|---|---|---|
| HBM3/HBM4 | `configs/validation/hbm3.cfg` 或 `hbm4.cfg` / `ramulator2_reference_1ch` | 单 channel、32 B transaction、关闭自动维护、Direct PHY |
| LPDDR5/LPDDR6 | `configs/validation/lpddr5.cfg` 或 `lpddr6.cfg` / `ramulator2_reference_1ch` | 单 channel、共同 x16/x12 与 burst 面、关闭项目扩展维护 |

LPDDR5 差分 preset 还显式保留 Ramulator2 的 1024-entry 原始 column-address 编码，以复现
对方 scenario 的地址解码；它不是标准主配置的容量声明。日常 `configs/lpddr.cfg` 已将 column
统一为本项目的 32 B transaction column：64 columns × 64K rows × 16 banks = 2 GiB。

每个 scenario 先比较 resolved timing 映射，再比较 canonical command sequence、解码坐标、
相对 issue cycle 和 read latency。split ACT、auto-precharge 编码、LPDDR 的 CAS+RD/WR 与
合并列访问编码、以及 HBM 双总线的等价表示只在报告中显式规范化；LPDDR CAS 仅在其紧随一条
指向同一 bank/row/column 的列命令时才被规范化，孤立出现的 CAS 仍参与比较。延迟比较带
1e-9 绝对 epsilon：延迟由 ns/ticks 换算而来会带约 1e-14 的浮点残差，没有 epsilon 时
「差值恰好等于容差」会被残差推成失败，报出的是浮点噪声而不是模型分歧。容差以及
Ramulator Git commit/describe/dirty 状态和 Python 版本均随 JSON 保存。`git_dirty=true` 时必须同时归档外部 diff，或改用干净 checkout 复跑。判定为
PASS 只说明该共同抽象面在给定版本和
配置下相符，不说明 Ramulator2 或本项目等于 JEDEC 器件，更不验证本项目的多 Stack、真实
payload、DFI、ECC、功耗热等独有路径。

<a id="section-8-5-1"></a>

#### 8.5.1 DRAMsim3 辅助对照

DRAMsim3 只用于较老 HBM2 共同命令面和 IDD 公式的第二来源检查，不是 HBM4/LPDDR6
协议参考。先运行不含热求解器的快速检查：

```bash
python3 tools/dramsim3_aux_validation.py \
  --binary ./build-clang-debug/hbm_sim \
  --dramsim3-root /path/to/DRAMsim3 \
  --skip-thermal-runtime \
  --json-out outputs/dramsim3_aux.json
```

工具会分别启动 DRAMsim3 和本项目，比较 ACT→RD 序列、`tRCDRD` 间隔、读延迟，以及
ACT/RD/WR/REF 的 IDD 能量公式。成功时进程返回 0，JSON 中
`external_engine_executed=true`、`passed=true`；`thermal.runtime_executed=false` 是该快速
模式的预期结果，不得据此声称热模型已对照。

只有本地已经准备带热功能的 DRAMsim3 build 时，才去掉 `--skip-thermal-runtime`：

```bash
python3 tools/dramsim3_aux_validation.py \
  --binary ./build-clang-debug/hbm_sim \
  --dramsim3-root /path/to/DRAMsim3 \
  --thermal-cycles 20000 \
  --json-out outputs/dramsim3_aux_thermal.json
```

完整模式检查相同环境温度和 footprint 下两边都出现正温升，只支持定性趋势结论。本项目的
稀疏行为级耦合热模型与 DRAMsim3 的 floorplan/求解器不同，不能比较绝对温度数值，更不能
把 HBM2 的共同结果外推为目标 HBM3/HBM4/LPDDR 器件校准。JSON 中还会保存 DRAMsim3 commit、
外部可执行文件和参考配置路径，归档时应与本项目 commit 一起保存。

<a id="section-8-6"></a>

### 8.6 性能曲线和不确定性

```bash
python3 tools/performance_curve.py \
  --binary ./build-clang-debug/hbm_sim \
  --standards hbm4,lpddr6 \
  --json-out outputs/performance_curve.json \
  --csv-out outputs/performance_curve.csv

python3 tools/sensitivity_uncertainty.py \
  --binary ./build-clang-debug/hbm_sim \
  --json-out outputs/sensitivity.json
```

合理曲线通常随注入率上升经历低负载、吞吐增长、排队延迟陡增和饱和。异常零曲线应先
排查请求未注入、cycle limit、完成口径或前端反压，而不是直接调整 timing。

#### 8.6.1 定制化 HBM/LPDDR 架构实验

`experiments/architecture_sweep/` 是独立实验目录，不新增标准主配置。每个 case 以该标准
**自己的完整标准组织**为基线，只做一处改动：**密度组**（3 点）按各标准的真实 JEDEC
规格取密度档位——变密度时**不变**的是 `channels`、`banks_per_group`、`columns`，
**变**的是 `rows`，以及（HBM4 的堆叠配置）`stack_height` 与随之确定的 SID 域数量
`sids`；**refresh 组**（2 点）只改 `refresh_policy`；**组织扰动组**（4 点）含三个单键 ×2 的点
（`banks_per_group`、`bank_groups`、`columns`），加一个 `mixed_shift`——同时挪动
`banks_per_group`/`bank_groups`/`rows`/`columns` 四个键、有增有减，用于观察模型在
多维度同时偏离时是否仍然自洽。这些键在真实器件里是随密度档固定的，所以本组是
**有意偏离 JEDEC 的研究型扰动**，不是器件规格。`mixed_shift` 的取值由固定种子
`20260915` 生成后冻结成表（不在运行时随机，否则实验不可复现、`resolved.cfg` 无法
作为审计依据），生成时约束 `bank_groups` 为偶数且 ≥2、至少一键变小一键变大、
容量比落在 [0.4, 4]。基线不是硬编码的，而是每个标准跑一次
`--check-config --dump-resolved-config`（约 7 ms，不跑仿真）解析出来并存为
`<out>/<standard>/baseline.cfg`，这样容量门禁的两侧才是独立的。两个组使用不同的定向
trace，而不是给所有配置复用一份随机流量：密度组让请求同时到达并轮转全部
`(lane, bank)`，同一 bank 每轮使用不重复 row，避免 FR-FCFS 借 row wrap 重排出额外命中；
refresh 组在相同研究型刷新间隔下保持持续流量，两个 case 使用完全相同的 trace 以隔离
刷新 scope：

```bash
python3 experiments/architecture_sweep/run.py

# 默认是 HBM4 + LPDDR6；需要统一复跑四标准时：
python3 experiments/architecture_sweep/run.py \
  --standards hbm3,hbm4,lpddr5,lpddr6

# 快速查看 HBM4 工具链
python3 experiments/architecture_sweep/run.py --standards hbm4 \
  --out outputs/experiments/architecture_sweep_quick
```

每个 case 都由 master + 临时 `[override]` 运行，并保存 `workload.trace`、`resolved.cfg`
与 `stats.txt`。汇总输出是 `results.csv`、`checks.csv`、`summary.md` 和离线
`trends.html`。脚本逐 case 输出进度，默认单 case 超时 120 秒；自动检查 cycle limit、
数据错误、**容量核算**（Python 按容量公式算出的组织容量必须严格等于引擎报告的
`aggregate_capacity_bytes`）、**组织漂移**（每个 case 只在其声明的字段上偏离基线）、
基线稳定性、**行为不变性**、地址编码（用 `--cmd-trace` 的显式 DRAM 坐标校验）、
密度 scaling，以及 REFpb/REFdb/REFab scope。任一检查失败便返回非零。

密度档位取值的依据：HBM3 的 `SID, BA[3:0]` 恒为 32 banks/PC、4 banks/BG，密度只由
rows 区分（8Gb RA[12:0]=8192、16Gb RA[13:0]=16384、32Gb RA[14:0]=32768），故 8/16/32 GiB。
HBM4 的 8 banks/BG 固定，8Hi/12Hi/16Hi 分别给出 2/3/4 个 SID 域，即 32/48/64 banks/PC；
24Gb die 因 RA[13:12]=11 非法，rows 取 3×2¹²=12288，故 32/36/64 GiB。`stack_height` 与
`sids` 都显式写进 override，不依赖引擎从 `stack_height` 自动推导 `sids`——隐式推导会让
trace 生成器按旧 `sids` 编码地址、被新 `sids` 的引擎解码，落点与预期不符。

读结果时有两处要留意。其一，**`density behavior invariance` 是正向断言**：按 JEDEC，
HBM3 与 LPDDR 的 bank 组织与列宽在全密度档固定，因此这些标准的密度轴只改变容量与地址
范围，行为指标本就应当逐位相同；脚本把它写成被检验的事实，而不是让"三点没差异"伪装成
空洞的通过。其二，**`density scaling` 只在 HBM4 上有效**——只有 HBM4 的 bank 数随密度档
变化（SID 域 2/3/4 → 2048/3072/4096 banks），HBM3 恒 1024、LPDDR5 恒 16、LPDDR6 恒 32，
这些标准没有可分辨的并行度轴，脚本会明说而不是编造趋势。该门禁还要求各档**每 lane 负载
相等**：lane 数随密度档变，若把请求数固定成常数，档位越高每 lane 分到的请求越少，
带宽下降只是负载摊薄而非并行度变差，此时脚本报告"不可比较"（`--density-requests` 用于
固定该值，0 表示自适应）。`density behavior invariance` 与 `density scaling` 按
`group == "density"` 过滤，因此不会被组织扰动组污染。

其三，**组织扰动组不给带宽趋势结论**。该组（`bpg_double`/`bg_double`/`cols_double`，
各把 `banks_per_group`/`bank_groups`/`columns` 放大一倍，容量均为基线 2 倍）的请求数
与基线对照相同——只有组织变、负载不变，这是唯一不混淆变量的比较方式。代价是 bank 数
翻倍时覆盖率必然降到约一半，新增 bank 在固定请求数下到不了；在未覆盖到的 bank 上，
带宽差异既可能来自并行度、也可能只是负载被摊薄。脚本因此只对该组做容量核算与单键
漂移校验，并在 `summary.md` 中把三个 case 的 bank 数/覆盖率/带宽与基线对照并列供
人工判读。在 HBM3/HBM4 上三者实际访问的物理 bank 集合与基线一致，带宽逐位相同，
这是预期行为而非缺陷。

LPDDR5 只收录 x16 两个档位：x8 模式（128Mb × 8DQ）在同密度下
把接口位宽减半，而引擎的 `achieved_bw_GBps` 只按 payload ÷ 仿真时间计算、不随
`data_bus_bits` 变化，把位宽设成 8 会得到 `achieved > peak`（9.526 > 6.4 GB/s）的
物理不可能结果；在“半宽接口下相同 payload 耗时翻倍”这层耦合明确之前该档位无法忠实建模。
扫描还显式把 `channel_mapper`
设为 `decoded`：`configs/hbm.cfg` 默认的 `xor` 会按地址哈希覆盖解码出的 channel，
而该哈希多对一、需在 row 上搜索才能反解，实验无法控制落点；因此结果不代表 HBM 主配置
默认的 xor 路由行为，这是为定向隔离而改的口径。

刷新组覆盖的短 `nREFI/nREFIpb/nRFC/nRFCpb` 均标记为 `research`；其中 `nREFI/nREFIpb`
取 32 是为了让维护批次数在短窗口内可观测（覆盖到 64 请求的 smoke），不是物理间隔，
不替代 master 中的 JEDEC/reference timing。比较时只在同一 standard、同一 group 内
判断模型趋势。LPDDR6 始终保留标准 `bank_groups=4`（偶数），REFdb 及其偶数 BG 成对
约束一直成立。细节与逐 case 容量表见
[experiments/architecture_sweep/README.md](experiments/architecture_sweep/README.md)。

<a id="section-8-7"></a>

### 8.7 可行性结论

可以声称：

- 软件结构、状态机、数据闭环和回归在已覆盖场景下可复核；
- 与外部开源模型的共同抽象可做差分；
- 模型适合架构趋势、调度、带宽/延迟和相对功耗热研究。

不应声称：

- 已等价于某家 HBM4/LPDDR6 量产器件；
- Behavioral PHY 是真实 pin-level/electrical PHY；
- 绝对功耗、结温或错误率具有器件级精度。

---

<a id="chapter-9"></a>

## 9. 审计与完成情况

<a id="section-9-1"></a>

### 9.1 来源分级

| 等级 | 含义 | 记录方式 |
|---|---|---|
| JEDEC/DFI | 公开标准可核对 | 写明标准号、版本和字段 |
| vendor | 厂家 datasheet/model | 写明器件、speed-bin、版本 |
| external | Ramulator2/DRAMsim3 等 | 写明仓库版本和规范化方式 |
| derived | 由容量/频率/总线计算 | 给出公式和输入 |
| research_default | 缺少器件依据的假设 | 明确标注并做敏感性区间 |

<a id="section-9-2"></a>

### 9.2 当前已完成

- 四标准统一 CLI、配置、traits/profile/finalize；
- 主动多 stack、per-stack ingress/QoS 和 `[stack][channel]` Controller/PHY；
- FCFS/FR-FCFS、行策略、bank 状态、TimingEngine、refresh/RFM/低功耗；
- Behavioral PHY、HBM/LPDDR adapter、DFI-oriented 输出；
- 三种真实 payload 后端、数据校验、物理坐标、功耗热和 ECC shadow；
- command/DFI/response/memory/thermal 输出和离线可视化；
- 项目内验证、Timing 边界、差分、性能曲线、敏感性工具。

<a id="section-9-3"></a>

### 9.3 尚需器件级工作

- 目标厂商 HBM3/HBM4/LPDDR5/LPDDR6 的完整 speed-bin timing；
- PHY training、电气、封装、链路错误率和真实 DFI/RTL 端口校准；
- IDD/电压、命令能量、floorplan、TSV、材料/封装和散热边界；
- RFM/PRAC、RAS、on-die ECC/link ECC 的器件实现细节；
- 厂家模型或硬件带宽、延迟、功耗和温度数据的最终拟合与误差区间。

本章提供完整验证路线；逐项证据要求和器件校准边界见[审计](文档/审计.md)。

---

<a id="appendix-a"></a>

## 附录 A：配置参数说明

本附录说明日常选择和修改的输入。完整参数字典、单位别名、可修改范围及联动规则见
[cfg指南](文档/cfg指南.md)。默认值以所选主配置与程序内置基准合并后的生效值为准，
不在手册中另存一套数值副本。

复制 configs/hbm.cfg 或 configs/lpddr.cfg，选择基础协议，再修改需要研究的主参数。
修改后先检查并保存快照：

```bash
mkdir -p outputs/config_check
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 \
  --check-config --dump-resolved-config outputs/config_check/resolved.cfg \
  --dump-timing-table outputs/config_check/timing.csv
```

自定义运行替换 --config 路径即可。快照包含派生值，适合复现；
继续改变主输入时不要用旧快照中的显式派生值约束新模型。

<a id="appendix-a-1"></a>

### A.1 实验和输入

| 常用输入 | 含义与使用 |
|---|---|
| standard | hbm3/hbm4/lpddr5/lpddr6 基础协议 |
| pattern / trace | 合成 stream/random 或 trace 文件；trace 决定实际请求 |
| requests | 请求数上限；trace 中 0 表示读取全部展开请求 |
| read_ratio / seed | 合成读比例（%）与随机种子 |
| inject_interval | Host 注入间隔，tick；拒收时还需等待 |
| random_address_space_bytes | 合成 random 的聚合地址范围；小后端应显式限制 |
| max_cycles / progress_interval | 运行上限 / stderr 进度间隔；截断不等于完成 |

详细输入格式及边界见[cfg指南 3.1](文档/cfg指南.md#appendix-a-1)。

<a id="appendix-a-2"></a>

### A.2 Profile、组织与时钟

| 常用输入 | 含义与联动 |
|---|---|
| channels、pseudo_channels、sids、ranks | 逻辑寻址层级；HBM 中性 ranks=1，SID 不等同 die |
| sids=auto | HBM 4/8/12/16Hi 自动选择 1/2/3/4 SID；先参与容量/密度计算；显式正整数固定研究组织，LPDDR auto 为 1 |
| bank_groups、banks_per_group、rows、columns | 每逻辑范围的 Bank 组织及每 Bank 行/事务列数，参与容量计算 |
| line_size / dram_transaction_bytes | Host 请求粒度 / 单 DRAM 事务 payload，Byte |
| stack_height | HBM 物理研究层数；不在逻辑容量外重复相乘 |
| data_rate_mbps | 每 pin 数据率，联动 speed_bin 与 CK |
| density_gb、tCK_ps、data_bus_bits | 通常由主输入推导；显式输入需遵守相应一致性/研究覆盖规则 |

每实例容量 = channels × PC × SID × ranks × BG × banks × rows × columns × 事务字节。
总容量再乘 stack_count；后端实际分配量不等于几何容量。
更改几何/速度并不自动获得某个厂商料号，完整推导规则见[cfg指南第 2 节](文档/cfg指南.md#coupling)。

<a id="appendix-a-3"></a>

### A.3 Controller 和多 Stack

| 常用输入 | 含义 |
|---|---|
| scheduler / row_policy | 从已有调度器和行政策中选择；未知算法拒绝 |
| read_buffer_size / write_buffer_size | 每控制器读/写队列容量 |
| address_mapping / channel_mapper | 地址解码及 Channel 分发 |
| stack_count / stack_mapping | 实例数量及系统地址路由 |
| response_delivery_mode | disabled/host/transaction/both；按观察目的选择 |
| 两级 response_queue_capacity | 0 为不限；有界时外部调用方必须及时消费 |

多 Stack 的路由、共享范围和响应循环见第 2、4、6 章；
完整 QoS/入口/维护队列参数见[cfg指南 3.3](文档/cfg指南.md#appendix-a-3)。

<a id="appendix-a-4"></a>

### A.4 PHY/DFI

mem_phy_mode 选择 direct 或 behavioral。Behavioral 建模初始化、训练、队列和流水线，
不是电气电压波形。命令 FIFO 深度决定可容纳条目，pipeline 决定占用时间；
浅流水线下零反压不证明已校准 CA 带宽。

DFI 标签不等于接口认证；详细延迟单位、FIFO 与导出参数见
[cfg指南 3.4](文档/cfg指南.md#appendix-a-4)。

<a id="appendix-a-5"></a>

### A.5 Refresh、RFM、低功耗和协议模式

普通运行首先确认 supports_refresh/supports_rfm 及 refresh_policy 是否符合实验目标。
禁用维护可以隔离基础命令开销，但不能把结果称为包含完整刷新代价。
低功耗、WCK/DVFS、链路保护是专项模式，切换时需要同时核对时序和来源。
完整开关与限制见[cfg指南 3.5](文档/cfg指南.md#appendix-a-5)。

<a id="appendix-a-6"></a>

### A.6 Timing 参数

nCL/nCWL 控制读写延迟，nRCDRD/nRCDWR、nRAS/nRP/nRC 控制行访问，
列间隔、tFAW、刷新和 WCK 等共同限制发令。调整一个数值并不一定只影响一种性能。

Timing 内部以 nCK 保存，执行等待再换成 tick；时间输入按所选 CK 向上取整。
普通使用只填写本次需要改变的项，不必复制整张 Timing 表：

- 未指定的项使用所选协议基准；显式 nCK 固定周期数，ns 输入按最终 CK 换算。
- 显式修改 nRAS/nRP 且未指定 nRC 时，补推 nRC=nRAS+nRP；nRPab 不会随覆盖的 nRP 自动同步。
- nCWL+nBL+nWR 等是运行时组合等待，不代表修改一项会自动改写其余输入。
- 普通 Timing 不支持填写 auto；需要恢复基准/补推时，应移除该项的有效显式输入，
  同时检查 preset、[override] 和 CLI 是否仍在覆盖。source 只声明来源，不开启自动计算。

逐项修改选择、刷新/RFM 的联动例外、冲突处理和可执行核验步骤见
[cfg指南 3.6.12：Timing 修改操作清单](文档/cfg指南.md#timing-editing)。
修改后查 timing.csv 的值、来源和适用模式；特别核对 nRC≥nRAS+nRP。
标准与项目每项 Timing 的范围、公式、deadline 和单位别名，完整列在
[cfg指南 3.6](文档/cfg指南.md#appendix-a-6)。研究覆盖应如实标为 research_default，
不因改了 source 标签就成为厂商校准。

<a id="appendix-a-7"></a>

### A.7 数据后端、功耗、热和 ECC

| 常用输入 | 使用边界 |
|---|---|
| memory_backend | sparse/mmap_sparse/chunk_file，改变宿主保存方式 |
| memory_capacity_bytes | 后端地址上限；不能扩大 DRAM 几何 |
| memory_data_file | 持久文件路径，多实例建议保留 {stack} |
| memory_chunk_size / memory_chunk_cache_entries | chunk 缓存配置，不改变 DRAM 容量 |
| power_model / thermal_model | 启用能量和热事件模型；参数校准决定器件可信度 |
| ecc_shadow | payload 校验模型，不等于完整厂商 on-die ECC |

后端与热原理仍见第 4、6、7 章；详细参数见
[cfg指南 3.7](文档/cfg指南.md#appendix-a-7)。

<a id="appendix-a-8"></a>

### A.8 输出和验证参数

| 常用输入 | 用途 |
|---|---|
| stats_view=summary | 日常英文精简报告 |
| stats_json | 工具读取的公共结果 JSON |
| dump_resolved_config / dump_timing_table | 保存生效配置及 Timing |
| cmd_trace / response_trace | 导出命令 / Host 响应，按需增加 transaction 响应 |
| validate_cmd_trace / validate_dfi_trace | 显式执行离线检查；未启用不算通过 |
| dump_memory_* / dump_thermal_map | 按需导出最终镜像或热图 |

专项 diagnostic 与全部导出项见[cfg指南 3.8](文档/cfg指南.md#appendix-a-8)。
日常命令不需要加入 diagnostic 才能生成公共 JSON。

---

<a id="appendix-b"></a>

## 附录 B：普通输出指标说明

本附录保留日常精简报告的解读和分析方法。终端使用英文 MODEL / PARAMETERS / RESULTS；
完整参数、诊断计数和各类 CSV 字段分别查 [cfg指南](文档/cfg指南.md)、
[输出指南](文档/输出指南.md)。summary 文本供人阅读，自动分析使用 --stats-json。

<a id="appendix-b-1"></a>

### B.1 身份、周期和完成

| 普通报告项 | 如何看 |
|---|---|
| Model / Standard、Organization | 实际模型身份及逻辑层级，不代表厂商认证 |
| Capacity、Host / Transaction Bytes | 每实例/总几何容量、Host 与 DRAM 事务字节 |
| Policies / Features | 生效调度、行政策、映射、PHY/后端及能力开关 |
| Interface / Clock、Workload、Queues | 数据率/时钟、本次输入负载、队列配置 |
| Comparison Baseline / Parameter Change | 相对当前内置基准的有效差异；完整列表在 JSON changes |
| Run Status | completed 正常完成；truncated/failed 不能混入有效曲线 |
| Requests / Transactions | Host 与拆分后事务的不同计数 |
| Remaining Work | 排队/未注入与 pending 两类剩余工作 |
| Simulation Time | 模拟时间 ns，不是程序运行耗时 |

没有读事务时延迟 N/A/null，不能按 0 ns 的有效测量比较。
所有普通英文标签的逐项释义见[输出指南第 1 节](文档/输出指南.md#summary)。

<a id="appendix-b-2"></a>

### B.2 性能、延迟和队列

| 普通报告项 / JSON 指标 | 解释 |
|---|---|
| Bandwidth / achieved_bw_GBps | 完成 payload / 模拟时间，十进制 GB/s |
| Utilization / bandwidth_util_pct | payload 吞吐占接口理论峰值百分比 |
| Read Transaction Latency / avg_read_latency_ns | 已完成读事务平均延迟，不是 Host burst 整体延迟 |
| Read / Write Queues | 配置队列容量，不是平均占用 |

吞吐需要和延迟一起看；只提高 buffer 往往会增加排队而不提高最终带宽。
等待来源、平均队列与接口开销的诊断项见[输出指南全输出词典](文档/输出指南.md#full-fields)。

<a id="appendix-b-3"></a>

### B.3 行行为和命令

普通报告的 `Row Hit Rate` 对应结果 JSON 的 `metrics.row_hit_pct`，表示控制器首次
调度分类时，目标行已经在目标 Bank 中打开的比例。它反映行局部性，不是数据是否正确，
也不是“读请求占比”或缓存命中率。

| 首次分类时目标 Bank 的状态 | 分类 | 通常需要的后续动作 |
|---|---|---|
| 已打开的就是目标行 | 行命中 `row_hits` | 无需为该访问重新激活行，但仍须等待其他 Timing/PHY 条件 |
| 没有打开的行 | 行未命中 `row_misses` | 先 ACT，再读写 |
| 打开的是另一行 | 行冲突 `row_conflicts` | 通常先 PRE 关闭旧行，再 ACT 打开目标行，最后读写 |

本项目的 miss 和 conflict 是互斥分类，不能只用 `hits + misses` 作分母：

```text
row_hit_pct = 100 × row_hits / (row_hits + row_misses + row_conflicts)
```

例如命中 100 次、未命中 50 次、冲突 50 次，行命中率为 50%。没有任何已分类事务时，
结果 JSON 为 null、普通报告为 N/A，而不是一次有效的 0% 测量。

统计单位是参与控制器调度分类的 DRAM 事务，每条事务只分类一次；随后发出多条命令
不会重复计数，也不会因为自己的 ACT 打开了行，就把原先的 miss 改记为 hit。
64 B Host 请求可能拆成两条 32 B 事务，因此不能直接以 Host 数作分母。
维护请求不参与分类；读转发等绕过普通命令调度的路径也可能不进入这些计数。
多 Stack/Channel 的总体比例按三类计数汇总后计算，不对各实例百分比直接取平均。

判断“同一行”必须比较完整的 `(Stack, Channel, PC, SID, Rank, BG, Bank, Row)`
地址范围，只看 Row 数字相同不够。同一已打开行中访问不同 Column 也可以命中。
默认 HBM4 主模板的顺序流、64 B Host/32 B 事务和 XOR Channel 路由下，1024 个 Host
请求的事务落在不同的 Bank/Row 组合中，所以 0% 是合理结果；这里不能漏掉 Channel。
修改 SID、Bank、行列、映射或请求规模后，应重新判断地址复用，不能套用这个例子的结论。

行命中率高通常意味着更少的行切换，但不保证吞吐更高或延迟更低：Bank 并行度、排队、
读写换向、刷新和 PHY 反压仍可能成为瓶颈。比较时固定组织、负载、映射和行政策，
结合带宽、读事务延迟及 ACT→读写→PRE 时间线分析，不要单独追求更高命中率。

`MemoryImage` 的 `rowbuf_hits` 是数据存储层行缓冲访问计数，与上述控制器首次调度
分类不是同一口径，不能互相代替。完整字段词典在输出指南维护。
完整命令计数、总线、刷新与行分类字段见[输出指南 6.3](文档/输出指南.md#appendix-b-3)。

<a id="appendix-b-4"></a>

### B.4 数据、后端与拓扑

普通 Backend Allocation 显示已分配后端行与数据字节记账，不是模型逻辑容量、
宿主 RSS 或磁盘实际块数。Golden Check 仅在执行对照时有意义。

数据检查还应结合 Validation：零 mismatch 且检查数为零，不证明数据闭环。
镜像与坐标详细列名见[输出指南产物说明](文档/输出指南.md#artifacts)，后端用法仍见第 6 章。

<a id="appendix-b-5"></a>

### B.5 PHY 与 DFI

普通 Features 显示 PHY 模式；Validation 显示 DFI 是否实际检查。
它们不展示全部 FIFO/beat 计数，也不要求普通用户打开 diagnostic。

需要定位阻塞或事件关联时才查看完整 PHY/DFI 字段。
抽象事件通过不等于电气/训练认证，说明见
[输出指南 6.5](文档/输出指南.md#appendix-b-5)。

<a id="appendix-b-6"></a>

### B.6 功耗、热、ECC、RAS 和模式

| 普通报告项 | 含义 |
|---|---|
| Energy | 累计模型能量，pJ；未启用功耗时不输出 |
| Mean / Peak Temperature | 触达热节点平均温度 / 历史峰值，°C |
| Validation / WARNING | 数据与 ECC 等检查结果及需关注的风险 |

能量不是功率，平均温度不是整个器件的体积平均；热图空白也不是 0°C。
基础计算见 B.9，完整分项诊断见[输出指南 6.6](文档/输出指南.md#appendix-b-6)。

<a id="appendix-b-7"></a>

### B.7 多 Stack 和验证动态字段

普通 Stack 表逐实例显示完成读写、带宽和读事务延迟。
先看负载是否真正分布到各实例，再判断扩展性；对单 Stack 高负载不应期待总带宽线性增加。

系统采用共同的 system_cycles；各控制器 cycle 累加不能替代物理时间。
Command/DFI 的 not_run 表示未执行，不是 pass；具体覆盖计数见
[输出指南 6.7](文档/输出指南.md#appendix-b-7)。

<a id="appendix-b-8"></a>

### B.8 补充精确输出字段

普通机器结果是 schema 2 JSON，分为 model、parameters、metrics、validation、
comparison_baseline/changes 和 stacks。metrics 不重复存放全部配置。

常用读取例子（先运行 make visualize-example 生成示例 result.json）：

```bash
python3 tools/view_stats.py outputs/visualization/example/result.json \
  --keys standard,host_requests,dram_transactions,system_cycles,achieved_bw_GBps,avg_read_latency_ns
```

完整公共字段含义见[输出指南第 3 节](文档/输出指南.md#json)，
内部 diagnostic 字段字典见[输出指南第 6 节](文档/输出指南.md#full-fields)。
不要用英文终端标签作为 JSON key，也不要把缺省字段补成零。

<a id="appendix-b-9"></a>

### B.9 可选：带宽、延迟、传输率、功耗和热的联合分析实例

以下保留从结果到结论的基本步骤；更细的队列诊断、扫描设计和文件列解释见
[输出指南性能分析](文档/输出指南.md#performance)。

#### B.9.1 第一步：先确认运行可用

先核对退出码、completed、剩余工作为零、完成事务数及数据错误量。
验证未执行、无独立 expect 或没有完成读时，应明确标记，不能声称全部验证通过。

#### B.9.2 把 system tick 换成真实时间

CK 是 DRAM 时钟，tick 是控制器推进单位；系统每次 step 并行推进各控制器。
tick_duration_ps=tCK_ps/tick_multiplier；
simulation_time_ns=system_cycles×tick_duration_ps/1000。
这不是宿主程序秒数，也不是多个控制器周期的总和。

#### B.9.3 区分速率、理论带宽和实际带宽

data_rate_mbps 是每 pin 数据率；普通 Bandwidth 是 payload 的有效吞吐。
例如完成 32,768 Byte，耗时 4,096 ns，则实际带宽为 8 GB/s。
理论峰值按实际建模接口位宽、数据率和实例数计算，利用率为实际带宽/峰值。
刷新、激活、换向及保护开销会使有效吞吐低于峰值。

#### B.9.4 延迟应与吞吐一起看

增大注入率后，吞吐先上升，饱和后队列等待通常继续增加。
比较时固定请求粒度、读写比、地址域与 seed；跨速度比较须区分固定 requests/tick
和固定 requests/ns。平均读事务延迟不等于一个 Host burst 的整体完成延迟。

#### B.9.5 从能量得到平均功率

平均功率 W = power_energy_pJ / simulation_time_ns × 0.001。
该结果来自已配置能量/IDD 模型，不能直接称为实测功率。比较能量需确认完成了相同工作量。

#### B.9.6 热结果和功耗的联合解读

热节点先按距上次更新的 tick 数冷却，再叠加事件温升：
T ← T − (T−Tambient)×min(1, Δtick×cooling)；T ← T + E_pJ×rise。
启用时还按温差进行横向/纵向/TSV 稀疏耦合，属于行为模型，不是完整三维材料求解。

地址先解码至 PC/SID/BG/bank/row/column；bank 与 SID 映射 layer，
bank 在 tile 中排布，row/column 按比例映射 tile 内热网格：
thermal_x=tile_x×grid_cols+grid_x，thermal_y=tile_y×grid_rows+grid_y。
当前热节点没有独立 Channel 维度，多个 Channel 可能汇入同一节点。
HBM 有层结构，LPDDR 使用简化器件映射；这不是厂家物理版图。

节点按事件/耦合触达生成，空白区域表示未导出节点，不是零温度。
首次直接访问不会清除节点已有的耦合温升。热图末列 `address_kind` 区分直接事件和
仅耦合节点；后者未知的 DRAM 地址字段为 -1，但 tile/grid 由自身热坐标计算。
比较温度应固定环境、系数、负载和运行时长；短暂低负载图不能当作稳态散热能力。
完整坐标公式和模型限制见[项目指南第 6 节](文档/项目指南.md#thermal)。

#### B.9.7 实验对照时的记录边界

至少保存配置与 resolved、结果 JSON、输入 trace 或 seed/地址域，以及启用的检查报告。
增加 Stack 时明确固定总负载还是每实例负载；改变后端时分开比较数据一致性与宿主开销。
敏感性区间描述所选研究参数范围，不是自动获得的厂商误差界。

---

<a id="appendix-c"></a>

## 附录 C：四标准运行用例

每个用例都应保存精简 `stats.txt` 和公共 `stats.json`（含 command/DFI validator 状态与检查量）、timing table、
command/DFI trace（小窗口）、performance JSON/CSV 和 dashboard，并记录 commit 与运行命令。
`model_validation.json`、外部差分 JSON 属于项目/参考版本级证据，应另行生成，不能假定单次
`hbm_sim` 运行会自动输出“验证 JSON”。

**跨标准结果的可比性。** 本附录四个标准的用例可以拿来比较**所配置系统的整体表现**，
但不同标准的通道数、地址映射（例如 HBM 主配置默认 `xor`，而 LPDDR 只有单通道、该键不生效）、
请求粒度和注入负载本来就不同，**不能把差异单独归因于 DRAM 标准本身**。要做同标准的趋势实验时，
应保持这些条件一致；跨标准并列时，先核对 `resolved.cfg` 中这几项是否可比。

<a id="appendix-c-0"></a>

### C.0 四标准通用查看工具和三后端审计

本附录使用仓库脚本将统计值输出为对齐表格：
下例以 C.2.1 已生成 `outputs/delivery/hbm4/stats.json` 为前提；若尚未运行 C.2.1，
应先生成产物，不能把该路径当成仓库预置文件。

```bash
python3 tools/view_stats.py outputs/delivery/hbm4/stats.json \
  --keys standard,completed_reads,system_cycles,achieved_bw_GBps,avg_read_latency_ns
```

多份文件可以并排查看；`-` 表示该文件没有这个字段。该工具只负责展示，不把不同实验
判成相等。需要带容差的回归判断时仍使用 `tools/compare_stats.py`。

C.1–C.4 的日常表格只列核心完成性和性能。下文保留的 WCK、命令计数、PHY 反压、
接口保护、低功耗等详细解释属于可选归因：配置模式看 resolved 快照、事件看 command/DFI CSV，
内部累计计数按 7.1.2 在同一用例的独立重放中采集。不要为了读取这些少用字段，
把普通交付命令改回 diagnostic，也不要把它们加入每次打印的默认表格。

三后端审计已经固化为 `tools/audit_case_backends.sh`，因此**不需要先在当前终端定义
`audit_case_backends` 函数**。调用格式为：

```bash
bash tools/audit_case_backends.sh CONFIG STANDARD PRESET_OR_NONE OUT_DIR [HBM_SIM_BINARY]
```

这一行是语法说明，大写词是必须替换的占位符，不能原样执行。例如 HBM4 的可执行命令是：

```bash
mkdir -p outputs/delivery/hbm4
hbm4_audit_out="$(mktemp -d outputs/delivery/hbm4/backend_audit_XXXXXX)"
bash tools/audit_case_backends.sh configs/hbm.cfg hbm4 none "$hbm4_audit_out"
```

脚本用相同的标准无关初始 image（`examples/backend_audit_image.txt`）和 trace 分别运行
`sparse`、`mmap_sparse`、`chunk_file`，导出
text/CSV/mismatch/stats，比较最终 image，检查文件头并打印对齐汇总。文件后端会持久化，
所以若 `OUT_DIR` 中已有 `data.bin`/`.meta`，脚本会拒绝混入旧状态；复测请使用诸如
`backend_audit_run2` 的新目录，或人工确认后归档旧目录。

判定方法与 [6.5.2–6.5.5](#section-6-5-2) 相同：脚本末尾必须打印
`PASS`；三列 `remaining_requests=0`、`hit_cycle_limit=false`、`data_mismatches=0`；mmap 和
chunk 的 `golden_mismatches=0`；两个 `.meta` 都显示 `HBMBACK\0`。`diff` 成功时本来就
没有内容，脚本最后的 PASS 表示两次比较均成功。该小测试证明三后端的数据语义一致，
不等于大容量持久化验证；正式大容量运行还要做重开和宿主资源检查。

<a id="appendix-c-1"></a>

### C.1 HBM3

#### C.1.1 完整仿真与产物

```bash
mkdir -p outputs/delivery/hbm3
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm3 \
  --requests 1024 --floorplan true --power-model true --thermal-model true \
  --cmd-trace outputs/delivery/hbm3/commands.csv \
  --response-trace outputs/delivery/hbm3/responses.csv \
  --dfi-trace outputs/delivery/hbm3/dfi.csv \
  --dfi-signal-trace outputs/delivery/hbm3/dfi_signal.csv \
  --dump-timing-table outputs/delivery/hbm3/timing.csv \
  --dump-resolved-config outputs/delivery/hbm3/resolved.cfg \
  --dump-thermal-map outputs/delivery/hbm3/thermal_map.txt \
  --validate-cmd-trace --validate-dfi-trace \
  --stats-json outputs/delivery/hbm3/stats.json \
  | tee outputs/delivery/hbm3/stats.txt
```

#### C.1.2 指标查看与预计效果

```bash
python3 tools/view_stats.py outputs/delivery/hbm3/stats.json \
  --keys run_status,completed_reads,completed_writes,simulation_time_ns,achieved_bw_GBps,avg_read_latency_ns,row_hit_pct,data_mismatches

# 命令/PHY 内部计数按 7.1.2 做可选专项诊断；不扩充日常指标表。

column -s, -t < outputs/delivery/hbm3/timing.csv | sed -n '1,12p'
column -s, -t < outputs/delivery/hbm3/commands.csv | sed -n '1,8p'
```

预计：`standard : HBM3`、`phy_protocol : hbm`、Behavioral PHY；请求全部完成、未命中周期
上限、command/DFI 验证通过。本命令继承 `configs/hbm.cfg` 的 stream、90% read workload，
通常同时产生 RD/WR；是否出现行命中和冲突必须以当前 mapping 和统计输出为准。若要研究
random 75% read，应显式添加 `--pattern random --read-ratio 75`，不能仅在文字中假定。
command CSV 应能看到 PC/SID/BG/bank 解码。当前参数是 Ramulator2.1-derived 研究基线，
不能写成目标厂商 HBM3 的实测结果。

读数时按下面顺序判断：

1. HBM3 的 64 B host line 拆成两个 32 B PC transaction，因此 `host_requests=1024`、
   `dram_transactions=2048`，`completed_reads + completed_writes` 应等于 2048；同时
   `remaining_requests=0`、`hit_cycle_limit=false`；否则后面的性能数字无效。
2. `cmd_validation` 和 `dfi_validation` 应为 `pass`，分别证明命令时序检查和
   Controller—PHY 事件/数据检查通过，但不代表已通过真实 HBM3 芯片认证。
3. `peak_bandwidth_GBps` 是 profile 给出的接口理论峰值；`achieved_bw_GBps` 是有效 payload
   除以本次 `system_cycles × tick_duration_ps` 对应的物理时间，二者之比是 `bandwidth_util_pct`。该短 stream 用例用于验证路径，
   不能把较低利用率解释成器件极限。
4. `row_hits/misses/conflicts` 描述打开行局部性；`row_conflicts` 会引入 PRE/ACT，通常抬高
   延迟。`dual_issue_cycles` 是同拍行/列总线均发命令的次数，不是额外仿真周期。
5. `phy_*_backpressure` 按“某总线在一个 tick 最终因 PHY 无法接收而未发命令”计数，
   用于定位 FIFO/数据通道限制；它不是候选命令被检查的次数。
6. timing 表的 `source` 和 `vendor_required_for_numeric` 决定数值能否作器件级声明；
   command CSV 的一行是一条已发命令，坐标列用于人工核查地址解码和命令间距。

#### C.1.3 可视化

```bash
python3 tools/performance_curve.py --binary ./build-clang-debug/hbm_sim \
  --standards hbm3 --intervals 32,16,8,4,2,1 --read-ratios 50,100 \
  --requests 512 --pattern random --seed 11 \
  --csv-out outputs/delivery/hbm3/performance.csv \
  --json-out outputs/delivery/hbm3/performance.json

python3 tools/visualize.py \
  --command-trace outputs/delivery/hbm3/commands.csv \
  --dfi-trace outputs/delivery/hbm3/dfi.csv \
  --stats outputs/delivery/hbm3/stats.json \
  --performance-json outputs/delivery/hbm3/performance.json \
  --thermal-map outputs/delivery/hbm3/thermal_map.txt \
  --title 'HBM3 delivery case' \
  --out outputs/delivery/hbm3/dashboard.html
xdg-open outputs/delivery/hbm3/dashboard.html
```

`visualize.py` 不会从一次 stats 自动推导负载曲线；必须先生成 JSON 并用
`--performance-json` 传入，否则该面板会明确显示 `No performance JSON supplied`。预计
Command mix 同时出现 ACT、RD/WR、PRE；bank view 可检查 same/different BG 的命令间距，
DFI 面板显示 HBM command/data event，热图仅用于相对热点趋势。

曲线横轴是 offered requests/tick，即 `1 / inject_interval`；实线是左轴 payload 吞吐量，
虚线是独立右轴平均读延迟，`R50/R100` 表示读比例。寻找“吞吐开始变平而虚线快速上升”
的位置，它是该固定单 channel 验证配置和 synthetic workload 的饱和拐点。曲线脚本使用
四份 `configs/validation/<standard>.cfg` 中的 `ramulator2_reference_1ch` preset，
而上面的 dashboard trace 来自完整运行配置，
所以曲线用于比较负载响应形状，不能与 full-stack 峰值作绝对一一对应。

#### C.1.4 其他有价值的命令

```bash
# Direct/Behavioral 对照
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm3 \
  --requests 1024 --mem-phy direct --stats-json outputs/delivery/hbm3/direct.json \
  | tee outputs/delivery/hbm3/direct.txt

# 相同稀疏 workload 下的 per-bank / all-bank refresh 策略对照
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm3 \
  --requests 1024 --inject-interval 16 \
  --refresh-policy per_bank --rfm-policy per_bank \
  --stats-json outputs/delivery/hbm3/per_bank_slow.json \
  | tee outputs/delivery/hbm3/per_bank_slow.txt
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm3 \
  --requests 1024 --inject-interval 16 \
  --refresh-policy all_bank --rfm-policy all_bank \
  --stats-json outputs/delivery/hbm3/all_bank_slow.json \
  | tee outputs/delivery/hbm3/all_bank_slow.txt
```

并排查看：

```bash
python3 tools/view_stats.py \
  outputs/delivery/hbm3/stats.json outputs/delivery/hbm3/direct.json \
  --labels behavioral,direct \
  --keys completed_reads,completed_writes,system_cycles,avg_read_latency_ns,achieved_bw_GBps

python3 tools/view_stats.py \
  outputs/delivery/hbm3/per_bank_slow.json outputs/delivery/hbm3/all_bank_slow.json \
  --labels per_bank,all_bank \
  --keys completed_reads,completed_writes,remaining_requests,hit_cycle_limit,inject_interval,system_cycles,avg_read_latency_ns,achieved_bw_GBps
```

Direct/Behavioral 应先确认完成数一致，再解释 `system_cycles`、延迟、带宽和 PHY 反压差异；
Direct 去掉的是 Behavioral PHY pipeline/队列行为，不是另一个 DRAM timing profile。
refresh 对照刻意把请求间隔设为 16，使运行时间超过 all-bank `nREFI`；否则短用例可能在
首次 REFab 到期前就结束，得到“per-bank 有 REFpb、all-bank 却为 0”的误导结果。先用
`REFab/REFpb` 和 batch 数确认策略确实执行，再观察延迟与吞吐。HBM3 profile 默认不支持
RFM，RFM 命令为 0 是能力配置结果，不应用来比较两种 RFM 策略。

#### C.1.5 HBM3 三后端人工查验

```bash
mkdir -p outputs/delivery/hbm3
hbm3_audit_out="$(mktemp -d outputs/delivery/hbm3/backend_audit_XXXXXX)"
bash tools/audit_case_backends.sh configs/hbm.cfg hbm3 none "$hbm3_audit_out"
column -s, -t < "$hbm3_audit_out/sparse/final.csv" | sed -n '1,12p'
```

除 C.0 的通用判据外，三份 stats 的 `standard` 都应为 `HBM3`、backend line size 应为
32 B HBM3 PC transaction，而不是 64 B host line。`final.csv` 每行给出地址、有效数据/初始化信息及物理坐标，
用于抽查写后读最终状态；三份最终 image 已由脚本逐字节比较。若后端一致而 command
validation 失败，应转向 timing/命令层排查，不要归因于存储容器。

<a id="appendix-c-2"></a>

### C.2 HBM4

#### C.2.1 完整仿真与产物

```bash
mkdir -p outputs/delivery/hbm4
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 \
  --requests 1024 --floorplan true --power-model true --thermal-model true \
  --cmd-trace outputs/delivery/hbm4/commands.csv \
  --response-trace outputs/delivery/hbm4/responses.csv \
  --dfi-trace outputs/delivery/hbm4/dfi.csv \
  --dfi-signal-trace outputs/delivery/hbm4/dfi_signal.csv \
  --dump-timing-table outputs/delivery/hbm4/timing.csv \
  --dump-resolved-config outputs/delivery/hbm4/resolved.cfg \
  --dump-thermal-map outputs/delivery/hbm4/thermal_map.txt \
  --validate-cmd-trace --validate-dfi-trace \
  --stats-json outputs/delivery/hbm4/stats.json \
  | tee outputs/delivery/hbm4/stats.txt
```

#### C.2.2 指标查看与预计效果

```bash
python3 tools/view_stats.py outputs/delivery/hbm4/stats.json \
  --keys run_status,completed_reads,completed_writes,simulation_time_ns,achieved_bw_GBps,avg_read_latency_ns,row_hit_pct,data_mismatches

# 命令/PHY 内部计数按 7.1.2 做可选专项诊断；不扩充日常指标表。

column -s, -t < outputs/delivery/hbm4/timing.csv | sed -n '1,15p'
column -s, -t < outputs/delivery/hbm4/dfi_signal.csv | sed -n '1,8p'
```

预计：`standard : HBM4`、32 channel full stack、2048-bit 聚合数据总线、32B DRAM
transaction；理论峰值约 2048 GB/s。`achieved_if_bw_GBps` 因 16-bit/transaction ECC
metadata 通常高于 payload `achieved_bw_GBps`。stream workload 可能只激活部分 channel，
所以短测试不应期待接近理论峰值。

详细读法如下：

- 先用完成数、`remaining_requests=0` 和 `hit_cycle_limit=false` 判断运行是否完整，再看性能。
- `data_rate_mbps × data_bus_bits / 8000` 给出整个 stack 的理论带宽（GB/s），即这里的 2048 GB/s；
  它不是当前 workload 保证达到的带宽。`bandwidth_util_pct` 用 payload 带宽除理论峰值。
- HBM4 host line 为 64 B，但这里 `dram_transaction_bytes=32`，因此一个 host 请求拆为两个
  DRAM transaction。completed 数和 RD/WR 命令数的口径不同，不能直接要求它们相等。
- `achieved_bw_GBps` 只计有效 payload；`achieved_if_bw_GBps` 还计 ECC/CRC/RAS metadata，
  但只是记账等效需求带宽，不代表额外 bit 已占用真实数据总线周期。
  `payload_efficiency_pct = payload / interface`，不能把接口额外字节称为应用吞吐。
- `row_bus_issues`、`column_bus_issues` 是两条命令总线累计发射次数；
  `dual_issue_cycles` 是两者同 tick 均发射的交集。`rising/falling_edge_ticks` 则是启用
  pairing 时各 controller 每个 tick 的边沿相位观察计数，即使该拍没有发令也会增长；它不是
  CA 利用率，也不是 `system_cycles`，聚合后可能远大于总仿真周期。
- `rfm_events=0` 只说明本 workload 没达到 `rfm_act_threshold`；要验证 RFM，应构造反复 ACT
  的热点行场景。`hbm_*bits_req` 和 `interface_overhead_bits` 用于核对每 transaction 的保护开销。
- timing CSV 的 `value_nck` 是 DRAM CK 数，执行时换算为
  `value_nck × tick_multiplier` 个内部 tick；`source=research_default` 且
  `vendor_required_for_numeric=true` 的 RL/WL/row timing 必须等厂商 speed-bin 表校准。
  DFI signal CSV 可沿 `cycle/phase/request_id` 追踪命令使能、数据有效和 payload。

#### C.2.3 可视化

```bash
python3 tools/performance_curve.py --binary ./build-clang-debug/hbm_sim \
  --standards hbm4 --intervals 32,16,8,4,2,1 --read-ratios 50,100 \
  --requests 512 --pattern random --seed 12 \
  --csv-out outputs/delivery/hbm4/performance.csv \
  --json-out outputs/delivery/hbm4/performance.json

python3 tools/visualize.py \
  --command-trace outputs/delivery/hbm4/commands.csv \
  --dfi-trace outputs/delivery/hbm4/dfi.csv \
  --stats outputs/delivery/hbm4/stats.json \
  --performance-json outputs/delivery/hbm4/performance.json \
  --thermal-map outputs/delivery/hbm4/thermal_map.txt \
  --title 'HBM4 full-stack delivery case' \
  --out outputs/delivery/hbm4/dashboard.html
xdg-open outputs/delivery/hbm4/dashboard.html
```

重点观察行列双总线是否形成 dual issue、rising/falling edge pairing、32 个 channel 的负载
分布，以及 payload/interface 两种带宽口径。曲线横轴、实线、虚线和饱和拐点的读法与
C.1.3 相同；它是单 channel validation sweep，不是上面 full-stack trace 的二次统计。
短时间热图只表示访问位置，不表示稳态结温。

#### C.2.4 多 Stack、链路开销和性能对照

```bash
# 六颗独立 HBM4 stack
mkdir -p outputs/delivery/hbm4_1stack_4096 outputs/delivery/hbm4_6stack
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 \
  --requests 4096 --stats-json outputs/delivery/hbm4_1stack_4096/stats.json \
  | tee outputs/delivery/hbm4_1stack_4096/stats.txt
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 --stack-count 6 \
  --requests 4096 --cmd-trace outputs/delivery/hbm4_6stack/commands.csv \
  --stats-json outputs/delivery/hbm4_6stack/stats.json \
  | tee outputs/delivery/hbm4_6stack/stats.txt
python3 tools/view_stats.py outputs/delivery/hbm4_6stack/stats.json \
  --keys stack_count,aggregate_capacity_bytes,peak_bandwidth_GBps

# CRC16/retry 行为开销入口（项目合成研究项：JESD270-4A 无 CRC 与 retry，标准链路保护为 parity）
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 --requests 1024 \
  --hbm-link-crc crc16 --hbm-link-crc-bits 16 --hbm-link-retry true \
  --stats-json outputs/delivery/hbm4/link_protection.json \
  | tee outputs/delivery/hbm4/link_protection.txt

# random 高并行 workload
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 --requests 10000 \
  --pattern random --seed 42 --addr-mapping RoBaRaCoCh \
  --stats-json outputs/delivery/hbm4/random_mapping.json \
  | tee outputs/delivery/hbm4/random_mapping.txt

python3 tools/view_stats.py \
  outputs/delivery/hbm4_1stack_4096/stats.json outputs/delivery/hbm4_6stack/stats.json \
  --labels one_stack,six_stack \
  --keys stack_count,aggregate_capacity_bytes,peak_bandwidth_GBps,completed_reads,completed_writes,system_cycles,achieved_bw_GBps,avg_read_latency_ns

python3 tools/view_stats.py \
  outputs/delivery/hbm4/stats.json outputs/delivery/hbm4/link_protection.json outputs/delivery/hbm4/random_mapping.json \
  --labels baseline,crc_retry,random_mapping \
  --keys completed_reads,completed_writes,system_cycles,achieved_bw_GBps,avg_read_latency_ns
```

预计：六 Stack 的精简报告和 JSON `stacks` 数组包含 0 到 5 六个实例，controller 数和聚合容量随 stack 数增加；
CRC/retry 配置增加接口开销/事件入口；random + channel-friendly 映射通常激活更多 controller，
但实际带宽和延迟必须以输出为准。

单/六 Stack 使用相同的 4096 请求，才能解释周期、吞吐和延迟差异。六 Stack 表中先验证
`stack_count=6`、完整系统的 controller 数为 `6×channels`，并在 Stack 表确认各实例有完成量；
`peak_bandwidth_GBps` 和容量按 stack 数线性扩展只是拓扑能力。实际 `achieved_bw_GBps` 还取决于
映射与每个实例读写数、带宽、延迟的均衡程度；入口反压仅按需诊断。若只有少量
active controller，说明地址序列没有展开到全部 channel，不能用理论峰值评价调度器。

链路保护表先看模式字段和每请求 bit 数，再看 `interface_overhead_bits`、接口带宽与 payload
效率。启用 retry 只是建立机制入口；当前 stats 没有独立 retry 次数字段，未注入链路错误时
不能从 `hbm_link_retry=true` 推断发生过重试。
random mapping 对照应同时解释 active controller、行命中/冲突、周期、吞吐和延迟；它与
baseline 同时改变了 workload/mapping，所以用于展示高并行场景，不是单变量因果证明。

#### C.2.5 HBM4 三后端人工查验

```bash
mkdir -p outputs/delivery/hbm4
hbm4_audit_out="$(mktemp -d outputs/delivery/hbm4/backend_audit_XXXXXX)"
bash tools/audit_case_backends.sh configs/hbm.cfg hbm4 none "$hbm4_audit_out"
column -s, -t < "$hbm4_audit_out/sparse/final.csv" | sed -n '1,12p'
```

三份 stats 的 `standard` 都应为 `HBM4`，当前 full-stack 模型的 backend line 应为 32 B DRAM
transaction，而不是 64 B host line。还要检查 `storage_channels_touched` 和 CSV 中的
PC/SID/BG/bank 坐标，不能只比较 payload 字符串。多 Stack 文件查验另按 [6.5.6](#section-6-5-6) 执行。

<a id="appendix-c-3"></a>

### C.3 LPDDR5

#### C.3.1 完整仿真与产物

```bash
mkdir -p outputs/delivery/lpddr5
./build-clang-debug/hbm_sim --config configs/lpddr.cfg --standard lpddr5 \
  --requests 1024 --floorplan true --power-model true --thermal-model true \
  --cmd-trace outputs/delivery/lpddr5/commands.csv \
  --response-trace outputs/delivery/lpddr5/responses.csv \
  --dfi-trace outputs/delivery/lpddr5/dfi.csv \
  --dfi-signal-trace outputs/delivery/lpddr5/dfi_signal.csv \
  --dump-timing-table outputs/delivery/lpddr5/timing.csv \
  --dump-resolved-config outputs/delivery/lpddr5/resolved.cfg \
  --dump-thermal-map outputs/delivery/lpddr5/thermal_map.txt \
  --validate-cmd-trace --validate-dfi-trace \
  --stats-json outputs/delivery/lpddr5/stats.json \
  | tee outputs/delivery/lpddr5/stats.txt
```

#### C.3.2 指标查看与预计效果

```bash
python3 tools/view_stats.py outputs/delivery/lpddr5/stats.json \
  --keys run_status,completed_reads,completed_writes,simulation_time_ns,achieved_bw_GBps,avg_read_latency_ns,row_hit_pct,data_mismatches

# 命令/PHY 内部计数按 7.1.2 做可选专项诊断；不扩充日常指标表。

column -s, -t < outputs/delivery/lpddr5/timing.csv | sed -n '1,12p'
column -s, -t < outputs/delivery/lpddr5/dfi.csv | sed -n '1,8p'
```

预计：`standard : LPDDR5`、`phy_protocol : lpddr`、WCK `cas_sync`、默认 100% read；16 Gb
x16 组织的逻辑容量为 2 GiB，64 B host line 拆成两个 32 B BL16 transaction，因此本命令
应有 `host_requests=1024`、`dram_transactions=2048` 且 2048 个读 transaction 全部完成。写命令
可能为 0 是 workload 结果，不是故障。当前 profile 是 Ramulator2.1-derived 研究基线，
适合与 LPDDR6 进行相同 workload 的趋势对照，不代表指定 LPDDR5 器件。

先用完成数/周期上限/两项 validation 判断用例有效。`peak_bandwidth_GBps` 是通道数据率和
位宽给出的理论接口能力，`achieved_bw_GBps` 是有效 payload；LPDDR5 运行配置与 HBM 的
channel/位宽不同，不能只按标准名字比较绝对带宽。`CAS_RD/CAS_WR` 是带 WCK 同步语义的
列命令入口，`RD/WR` 是实际数据访问；纯读 workload 下 CAS_WR、WR 和读写换向为 0 合理。
`wck_syncs/training_events` 用来确认 WCK 策略被走到，读写换向空窗目前需在 command trace
按相邻 RD/WR 的 cycle 差人工核对；DFI CSV 则检查命令到读写数据事件的
延迟。当前这些是数字时序/行为模型，不包含眼图、抖动、阻抗或电压训练收敛。

#### C.3.3 可视化和模式对照

```bash
python3 tools/performance_curve.py --binary ./build-clang-debug/hbm_sim \
  --standards lpddr5 --intervals 32,16,8,4,2,1 --read-ratios 50,100 \
  --requests 512 --pattern random --seed 13 \
  --csv-out outputs/delivery/lpddr5/performance.csv \
  --json-out outputs/delivery/lpddr5/performance.json

python3 tools/visualize.py \
  --command-trace outputs/delivery/lpddr5/commands.csv \
  --dfi-trace outputs/delivery/lpddr5/dfi.csv \
  --stats outputs/delivery/lpddr5/stats.json \
  --performance-json outputs/delivery/lpddr5/performance.json \
  --thermal-map outputs/delivery/lpddr5/thermal_map.txt \
  --title 'LPDDR5 delivery case' \
  --out outputs/delivery/lpddr5/dashboard.html
xdg-open outputs/delivery/lpddr5/dashboard.html

# 增加写流量，观察 WCK 和读写换向
./build-clang-debug/hbm_sim --config configs/lpddr.cfg --standard lpddr5 \
  --requests 4096 --pattern random --read-ratio 50 --seed 21 \
  --cmd-trace outputs/delivery/lpddr5/mixed_rw_commands.csv \
  --stats-json outputs/delivery/lpddr5/mixed_rw.json \
  | tee outputs/delivery/lpddr5/mixed_rw.txt

# power-down 行为入口
./build-clang-debug/hbm_sim --config configs/lpddr.cfg --standard lpddr5 \
  --requests 16 --inject-interval 128 --low-power power_down \
  --low-power-entry-cycles 8 --low-power-exit-cycles 4 \
  --stats-json outputs/delivery/lpddr5/power_down.json \
  | tee outputs/delivery/lpddr5/power_down.txt

python3 tools/view_stats.py \
  outputs/delivery/lpddr5/stats.json outputs/delivery/lpddr5/mixed_rw.json outputs/delivery/lpddr5/power_down.json \
  --labels read_only,mixed_rw,power_down \
  --keys completed_reads,completed_writes,system_cycles,achieved_bw_GBps,avg_read_latency_ns
```

预计：混合读写会出现 CAS_WR/WR 和 turnaround 空窗；稀疏注入的 power-down 用例应出现
低功耗入口/退出计数。16 个请求、128 tick 的注入间隔用于在请求之间形成空闲窗口。
比较时先确认三个用例请求均完成；再由 CAS/数据命令证明混合流量生效，并在
`mixed_rw_commands.csv` 中按相邻 RD/WR cycle 核对 turnaround 空窗。这里的 power-down
是 controller 自动空闲驻留模型，不向 PHY 显式发送 PDE/PDX，所以应以
`low_power_entries/exits/cycles/exit_blocked` 共同证明转换；PDE/PDX 为 0 是预期结果。
显式 PHY 低功耗配对在 C.4.3 的 `lpddr6_full` 中验证。曲线只代表单 channel 固定 profile
的负载响应。

#### C.3.4 LPDDR5 三后端人工查验

```bash
mkdir -p outputs/delivery/lpddr5
lpddr5_audit_out="$(mktemp -d outputs/delivery/lpddr5/backend_audit_XXXXXX)"
bash tools/audit_case_backends.sh configs/lpddr.cfg lpddr5 none "$lpddr5_audit_out"
column -s, -t < "$lpddr5_audit_out/sparse/final.csv" | sed -n '1,12p'
```

三份 stats 的 `standard` 都应为 `LPDDR5`、`phy_protocol` 都应为 `lpddr`；最终 image
必须一致。后端对照不会单独验证 WCK，因此仍需保留 C.3.2 的 WCK/CAS stats 和
DFI trace 检查。

<a id="appendix-c-4"></a>

### C.4 LPDDR6

#### C.4.1 完整仿真与产物

```bash
mkdir -p outputs/delivery/lpddr6
./build-clang-debug/hbm_sim --config configs/lpddr.cfg --standard lpddr6 \
  --requests 1024 --floorplan true --power-model true --thermal-model true \
  --cmd-trace outputs/delivery/lpddr6/commands.csv \
  --response-trace outputs/delivery/lpddr6/responses.csv \
  --dfi-trace outputs/delivery/lpddr6/dfi.csv \
  --dfi-signal-trace outputs/delivery/lpddr6/dfi_signal.csv \
  --dump-timing-table outputs/delivery/lpddr6/timing.csv \
  --dump-resolved-config outputs/delivery/lpddr6/resolved.cfg \
  --dump-thermal-map outputs/delivery/lpddr6/thermal_map.txt \
  --validate-cmd-trace --validate-dfi-trace \
  --stats-json outputs/delivery/lpddr6/stats.json \
  | tee outputs/delivery/lpddr6/stats.txt
```

#### C.4.2 指标查看与预计效果

```bash
python3 tools/view_stats.py outputs/delivery/lpddr6/stats.json \
  --keys run_status,completed_reads,completed_writes,simulation_time_ns,achieved_bw_GBps,avg_read_latency_ns,row_hit_pct,data_mismatches

# 命令/PHY 内部计数按 7.1.2 做可选专项诊断；不扩充日常指标表。

column -s, -t < outputs/delivery/lpddr6/timing.csv | sed -n '1,15p'
column -s, -t < outputs/delivery/lpddr6/commands.csv | sed -n '1,10p'
```

预计：`standard : LPDDR6`、10667 Mbps、24-bit、BL24/prefetch 24、两个 pseudo-channel、
32B DRAM transaction，64B host line 拆成两个 transaction。命令面可出现 ACT1/ACT2 和
WCK/CAS 行为；默认 link protection 关闭。RL/WL、训练和保护开销未作目标器件校准时只能
形成架构趋势结论。

读数时注意三种不同口径：`host_requests` 以 64 B 前端请求计，`dram_transactions`、
`completed_reads/writes` 和 RD/WR 以 32 B DRAM transaction 计，DFI beat 又由 lane bytes
决定；因此不能把三组计数直接要求为 1:1。前端响应数要看 `response_trace` 行数或
`host_responses_exported`。split-ACT 用例中
ACT1 与 ACT2 应成对，`act2_deadline_forced` 表示调度器为满足 ACT2 最晚期限而强制优先，
不是 timing violation。REFdb 是 LPDDR6 bank-pair 刷新；应结合 `commands.REFdb`、刷新事件
和 trace 中的目标 bank pair 判断，单看非零命令数不足以证明 bank-pair 表正确。

WCK sync/train、DVFS、接口保护都要先确认相应模式被启用；默认用例为 0 可以是预期配置。
`payload_efficiency_pct` 用于区分有效数据与 DBI/link ECC/CA parity 等建模开销。timing 来源中
仍标为 research/vendor-required 的 RL/WL、刷新和训练参数，在厂家数据到位前只能做趋势研究。

#### C.4.3 可视化和专用模式

```bash
python3 tools/performance_curve.py --binary ./build-clang-debug/hbm_sim \
  --standards lpddr6 --intervals 32,16,8,4,2,1 --read-ratios 50,100 \
  --requests 512 --pattern random --seed 14 \
  --csv-out outputs/delivery/lpddr6/performance.csv \
  --json-out outputs/delivery/lpddr6/performance.json

python3 tools/visualize.py \
  --command-trace outputs/delivery/lpddr6/commands.csv \
  --dfi-trace outputs/delivery/lpddr6/dfi.csv \
  --stats outputs/delivery/lpddr6/stats.json \
  --performance-json outputs/delivery/lpddr6/performance.json \
  --thermal-map outputs/delivery/lpddr6/thermal_map.txt \
  --title 'LPDDR6 delivery case' \
  --out outputs/delivery/lpddr6/dashboard.html
xdg-open outputs/delivery/lpddr6/dashboard.html

# 完整初始化/低功耗控制序列
./build-clang-debug/hbm_sim --config configs/lpddr.cfg --standard lpddr6 \
  --requests 64 --init-sequence lpddr6_full --init-sequence-interval 8 \
  --stats-json outputs/delivery/lpddr6/full_init.json \
  | tee outputs/delivery/lpddr6/full_init.txt

# 链路保护/DBI/link ECC/CA parity 研究入口
./build-clang-debug/hbm_sim --config configs/lpddr.cfg --standard lpddr6 --requests 1024 \
  --lpddr-link-protection true --lpddr-dbi true \
  --lpddr-link-ecc true --lpddr-ca-parity true --lpddr-wck always_on \
  --stats-json outputs/delivery/lpddr6/link_protection.json \
  | tee outputs/delivery/lpddr6/link_protection.txt

# 低速运行点（4267 Mb/s，非 JEDEC DVFSL 档）
./build-clang-debug/hbm_sim --config configs/lpddr.cfg --standard lpddr6 --requests 1024 \
  --preset low_rate_4267 --lpddr-wck cas_sync \
  --stats-json outputs/delivery/lpddr6/low_rate.json \
  | tee outputs/delivery/lpddr6/low_rate.txt

python3 tools/view_stats.py \
  outputs/delivery/lpddr6/stats.json outputs/delivery/lpddr6/full_init.json outputs/delivery/lpddr6/link_protection.json outputs/delivery/lpddr6/low_rate.json \
  --labels baseline,full_init,protected,low_rate \
  --keys run_status,completed_reads,completed_writes,simulation_time_ns,achieved_bw_GBps,avg_read_latency_ns,row_hit_pct,data_mismatches
```

预计：`lpddr6_full` 形成成对的 PDE/PDX、SREFEN/SREFEX 控制行为；链路保护用例增加
interface overhead、降低 payload efficiency；低速运行点改变模式/训练路径。若需要严格的
低速 timing 数值，应加载对应低速 profile，而不能只依赖模式标签。
注意：LPDDR6 的 link-protection/link ECC 与 DBI 是互斥特性。JESD209-6 Table 5
把二者同时使能列为 Prohibited setting；本项目在 `validation_mode=exploratory`
下放行并打印 warning，`standard`/`device` 模式会直接拒绝。需要展示共享 metadata
lane 记账时，只能把该组合声明为非标准研究用例，不能作为标准合规配置。

并排表应先检查每列 `dram_transactions = completed_reads + completed_writes`、无剩余请求且
未命中周期上限。full-init 只有 64 个 host 请求，另外三列为 1024 个，所以它只验证控制
序列，不能直接与其余三列比较吞吐。full-init 重点看四个低功耗命令是否配对，以及退出阻塞
是否符合配置；protected 重点看每请求保护 bit、总接口开销、接口带宽和 payload efficiency；
low-rate 重点看模式/transition/WCK 事件以及周期、延迟变化。这里使用
`--preset low_rate_4267` 同时选择低速流程和 4267 Mb/s/pin 实际速率。schema 3 下，
仅传 `--lpddr-dvfs low`、却保留 master 的 10667 实际速率会因矛盾被拒绝；也可在自定义
配置中显式设置一致的模式与速率。4267 Mb/s 高于 JESD209-6 DVFSL 的 <=3200 Mb/s
范围，因此模型只保留 DVFS/训练流程语义，核心 timing 与 nACU 回退 non-DVFSL 表列；
自动换算 CK 不等于完成厂商低速时序校准。
性能曲线仍按 C.1.3 的双轴规则阅读。

#### C.4.4 LPDDR6 三后端人工查验

```bash
mkdir -p outputs/delivery/lpddr6
lpddr6_audit_out="$(mktemp -d outputs/delivery/lpddr6/backend_audit_XXXXXX)"
bash tools/audit_case_backends.sh configs/lpddr.cfg lpddr6 none "$lpddr6_audit_out"
column -s, -t < "$lpddr6_audit_out/sparse/final.csv" | sed -n '1,12p'
```

三份 stats 的 `standard` 都应为 `LPDDR6`，backend line 应是 32 B DRAM transaction；
64 B host line 对应两个 transaction 的拆分结果不得因后端而变。还应对比
`split_activate`、`commands.ACT1/ACT2`和 `data_write_commits`，把 split-ACT 协议问题与存储持久化
问题分开定位。

---

<a id="appendix-d"></a>

## 附录 D：新 HBM4 自定义用例

本例假定一款名为 **HX-HBM4-9000-48G-16Hi** 的研究器件：9000 Mbps/pin、48Gb/die、
16Hi、两颗 stack。它用于展示完整配置流程，名称和数值不对应任何真实厂商；在获得厂家表
之前，所有新增 timing、功耗和热参数必须标为 `research_default`，不得用于器件级声明。

<a id="appendix-d-1"></a>

### D.1 定义研究问题和不可变条件

研究问题：在相同 70% read random workload 下，假定的 9000 Mbps HBM4 双 Stack 系统采用
Behavioral PHY 时，吞吐、延迟、行冲突、PHY 反压、接口开销和热点分布如何；Direct PHY、
blocked mapping 和文件后端会带来什么变化。

为保证实验可比较，固定 requests、seed、read ratio、地址映射、Controller 队列、timing、
功耗热参数；每次只改变一个模式。需要同时记录 Git commit、配置 SHA256 和完整命令。

<a id="appendix-d-2"></a>

### D.2 自包含配置文件与实验覆盖项

完整输入是仓库中的 [hx_hbm4_9000_48gb_16hi.cfg](experiments/local/hx_hbm4_9000_48gb_16hi.cfg)。
它从 `configs/hbm.cfg` 复制后定制，包含公共设置、HBM3/HBM4 基准段和末尾的 HX 覆盖段；
`[model]` 选择 HBM4，未选中的 HBM3 基准段不生效。运行时只加载这一份文件，
无需额外加载主模板或验证配置。

下面逐行展示实际文件从 `[override]` 到文件末尾的原文，包含注释，**不是可独立运行的完整配置**。
`channels=32`、`bank_groups=2`、`columns=32` 等仍由前面的 HBM4 基准段提供，不再重复搬入摘要。
基准段的 `sids=auto` 在本例被下方显式 `sids=4` 覆盖；其他同名字段也以下方覆盖值为准，
命令行显式参数优先级更高。

直接运行本例时使用仓库文件即可。若从主模板另建同样的用例，应保留主模板前面的各段，
用下面内容替换已有的 `[override]` 段，不要创建两个同名段。修改 HX 已覆盖的参数时应修改
该覆盖项，或删除它以恢复基准/自动选择，不能只修改前面的基准值。
通用自定义模型仍建议从主模板复制，不必沿用 HX 的手写 Timing。

```ini
[override]
# 复制本文件、选择 base_standard、修改上方当前标准的参数即可；也可在此集中写实验差异。
# 缺省/auto 联动项：speed_bin_mbps、tCK_ps、density_gb、data_bus_bits；不必重复填写。
# 容量 = Channel×PC×SID×Rank×BG×Bank×Row×事务Column×事务Byte；不能再乘 stack_height。
# HBM density 按几何容量/stack_height 得到每 die Gibit；LPDDR 按每 subchannel/rank 计算。
# 存储后端 capacity_bytes=0 跟随几何容量，实际宿主机占用取决于后端与访问量。
# 保留全部已实现高级键；陌生调度器/后端、矛盾数值和溢出会报错，不会静默回退。
# nCL = 28  # 任意已实现 Timing/PHY/功耗/热/ECC 参数仍可显式覆盖；需要器件资料时自行校准

# 此副本的实验覆盖；主参数推导规则与主模板相同。
model_name = hx_hbm4_9000_48gb_16hi_research  # 结果中的模型身份
timing_profile = hx_hbm4_9000_48gb_16hi_research  # 自定义 Timing 身份
vendor_profile = hypothetical_hx_research  # 假想研究参数，不是厂家 profile
mode_profile = full_stack_crc16_ras_research  # full-stack + CRC/RAS 研究模式
validation_mode = exploratory  # 只允许架构趋势和软件行为结论
strict_timing_table = false  # 仍含 research_default，不能使用厂家严格门槛
parameter_reference = Synthetic vendor-like HBM4 test values; not a real vendor datasheet.  # 参数来源声明

# 组织、速率和 HBM4 特有模式
stack_height = 16  # 每颗 HBM Stack 的 DRAM die 层数
data_rate_mbps = 9000  # 接口数据率，单位 Mb/s/pin
sids = 4  # 每个 PC 下独立寻址的 SID 数量；不直接等于 die 数，容量中只乘一次
rows = 24576  # 与 48Gb/die、16Hi、4 SID 和当前其余地址几何形成 96GiB/Stack
hbm_edge_pairing_matrix = hbm4_synthetic_full_matrix  # 配对矩阵来源标签；不会按字符串加载规则，实际由标准实现和 edge_pairing 布尔项决定
hbm_sid_mapping = sid_per_4hi_slice  # SID/layer 映射说明标签；不会按字符串切换映射算法，物理坐标由模型几何公式派生
hbm_ras_policy = ras_metadata_sideband  # RAS 方案说明标签；不会按字符串选择新的错误处理算法
hbm_link_crc_mode = crc16  # 启用 CRC16 开销和 retry 入口（合成研究项，非 JEDEC HBM 特性）
hbm_link_retry_enabled = true  # 启用链路 retry/replay 行为（合成研究项，标准无此机制）
hbm_link_crc_bits_per_request = 16  # 每事务 CRC 位数
hbm_ras_metadata_bits_per_request = 16  # 每事务 RAS metadata 位数
hbm_ecc_bits_per_request = 16  # 每事务 HBM ECC 位数

# Timing：全部是研究假设，替换为目标器件表后才能做器件级数值结论
timing_override_source = research_default  # 以下 Timing 的统一来源标签
nBL = 2  # Burst 占用，单位 nCK
nCL = 36  # 读 CAS latency，单位 nCK
nCWL = 12  # 写 CAS latency，单位 nCK
nRCDRD = 36  # ACT 到读，单位 nCK
nRCDWR = 20  # ACT 到写，单位 nCK
nRP = 36  # 单 Bank PRE 恢复，单位 nCK
nRPab = 36  # 全 Bank PRE 恢复，单位 nCK
nRAS = 64  # 最短激活保持，单位 nCK
nRC = 100  # 同 Bank row cycle，单位 nCK
nRTP = 10  # 读到 PRE，单位 nCK
nWR = 34  # 写恢复，单位 nCK
nCCDS = 2  # 短列命令间隔，单位 nCK
tCCDL_ns = 2.5  # 长列命令间隔，单位 ns
nCCDR = 4  # different SID 连续读间隔，单位 nCK
nRRDS = 10  # 短 ACT-to-ACT，单位 nCK
nRRDL = 12  # 长 ACT-to-ACT，单位 nCK
nFAW = 36  # 四激活窗口，单位 nCK
nRTW = 30  # 读转写，单位 nCK
nWTRS = 12  # 短写转读，单位 nCK
nWTRL = 16  # 长写转读，单位 nCK
tRFCab_ns = 560  # 全 Bank refresh recovery，单位 ns
tRFCpb_ns = 350  # 单 Bank refresh recovery，单位 ns
tRREFD_ns = 8  # 刷新命令最小间隔，单位 ns
tREFI_us = 3.9  # 全 Bank 刷新平均间隔，单位 us
tREFIpb_ns = 60.9375  # per-bank 刷新平均间隔，单位 ns
nRFMab = 1262  # 全 Bank RFM recovery，单位 nCK
nRFMpb = 789  # 单 Bank RFM recovery，单位 nCK
nMRW = 10  # Mode Register Write，单位 nCK
nMRR = 10  # Mode Register Read，单位 nCK
nECCSCRUB = 160  # ECC scrub，单位 nCK
nRASERR = 96  # RAS 错误处理，单位 nCK
nLINKRETRY = 40  # 链路 retry/replay，单位 nCK

# 系统、流量和 Controller
stack_count = 2  # 两颗独立 HBM Stack
stack_mapping = interleaved  # 系统地址按条带分布到两个 Stack
stack_interleave_bytes = 256  # Stack 条带大小，单位 Byte
stack_ingress_buffer_size = 256  # 每 Stack ingress 容量，单位 transaction
stack_dispatch_width = 4  # 每 Stack 每 system tick 最多分发 transaction 数
stack_qos_policy = strict_priority  # ingress QoS 调度策略
pattern = random  # 合成随机地址 workload
requests = 20000  # HostRequest 数；64B host line 拆成两个 32B transaction
read_ratio = 70  # 读请求比例，百分数
seed = 900048  # 固定随机种子，保证可重放
addr_stride = 64  # 地址对齐/步长，单位 Byte
inject_interval = 2  # 相邻 host 请求注入间隔，单位 system tick
max_cycles = 100000000  # 运行周期上限
address_mapping = RoBaRaCoCh  # 地址位到 DRAM 层级的映射顺序
channel_mapper = decoded  # 按地址解码结果选择 channel
read_buffer_size = 128  # 每 Controller 读队列容量
write_buffer_size = 128  # 每 Controller 写队列容量
priority_buffer_size = 4096  # 维护/控制优先队列容量
scheduler = frfcfs  # First-Ready FCFS 调度
row_policy = open_page  # 保持已打开行

# Behavioral PHY
mem_phy_mode = behavioral  # 启用 FIFO、pipeline、adapter 和 DFI
phy_command_fifo_depth = 64  # 命令 FIFO 深度
phy_read_fifo_depth = 64  # 读返回 FIFO 深度
phy_write_fifo_depth = 64  # 写数据 FIFO 深度
phy_command_pipeline_cycles = 2  # PHY 命令流水线延迟，单位 nCK；内部乘 tick_multiplier 转为 controller tick
phy_read_return_pipeline_cycles = 2  # PHY 读返回附加延迟，单位 nCK；不包含 DRAM RL
phy_write_data_pipeline_cycles = 2  # PHY 写数据附加延迟，单位 nCK；不包含 DRAM WL
phy_reset_cycles = 8  # PHY 复位持续时间，单位 nCK
phy_initialization_cycles = 16  # PHY 初始化持续时间，单位 nCK
phy_training_cycles = 32  # PHY 训练持续时间，单位 nCK
phy_auto_train = true  # 是否经过 Training 阶段；false 跳过训练，复位/初始化仍自动推进到 Ready

# 维护、后端、物理、功耗、热和 ECC
refresh_policy = per_bank  # 使用 per-bank refresh
rfm_policy = per_bank  # 使用 per-bank RFM
rfm_act_threshold = 256  # 单 bank ACT 累积触发门限
rfm_decrement = 256  # RFM 完成后的递减量
refresh_postpone_limit = 4  # refresh postpone 上限
refresh_pullin_limit = 4  # refresh pull-in 上限
refresh_credit_limit = 8  # refresh credit 绝对上限
refresh_temperature_mode = normal  # 正常温度刷新倍率
memory_backend = sparse  # 默认宿主稀疏内存后端
memory_capacity_bytes = 0  # 各类存储后端的地址容量上限，单位 Byte；0 按模型几何容量计算，不代表宿主机预分配内存
sparse_density_warning_pct = 30  # 稀疏分配密度告警阈值
topology_stats_scan_limit = 100000  # 拓扑扫描工作量上限；sparse 按已分配 line，文件后端按逻辑 line；超限跳过整个扫描，0 不限
floorplan = true  # 派生 layer/tile/grid/subarray/mat/cell 坐标
subarrays_per_bank = 16  # 每 Bank subarray 数
mats_per_subarray_x = 8  # X 方向 mat 数
mats_per_subarray_y = 8  # Y 方向 mat 数
cells_per_mat_x = 512  # 每 mat 的 X 方向 cell 数
cells_per_mat_y = 512  # 每 mat 的 Y 方向 cell 数
microbumps_x = 64  # microbump X 维数量
microbumps_y = 64  # microbump Y 维数量
power_model = true  # 启用命令事件功耗
power_source = configured_pj  # 使用研究型固定 pJ 参数
power_scale = 1.0  # 功耗能量缩放
thermal_model = true  # 启用行为级热模型
thermal_ambient_c = 40  # 环境初始温度，摄氏度
thermal_cooling_per_cycle = 0.00025  # 每 tick 冷却比例
thermal_rise_c_per_pj = 0.00002  # 每 pJ 行为级温升
thermal_grid_cols_per_tile = 4  # 每 tile 热网格 X 划分
thermal_grid_rows_per_tile = 4  # 每 tile 热网格 Y 划分
thermal_coupling = true  # 启用横向/纵向耦合
thermal_lateral_coupling = 0.05  # 同层邻域耦合系数
thermal_vertical_coupling = 0.10  # 相邻层基础耦合系数
thermal_tsv_coupling_scale = 1.25  # TSV 纵向耦合增强系数
thermal_tsvs_per_grid = 16  # 每热网格假定 TSV 数量
ecc_shadow = true  # 启用 payload SECDED shadow
ecc_check_on_read = true  # 读取时检查 ECC shadow
ecc_correct_single_bit = true  # 自动纠正单比特错误
ecc_inject_period = 0  # 本基线不周期性注错
validate_cmd_trace = true  # 运行后执行 command validator
validate_dfi_trace = true  # 运行后执行 DFI validator
fail_on_data_mismatch = true  # payload mismatch 时返回非零
```

这份完整文件将基准组织与实验覆盖合并：64 B Host 请求拆成两个 32 B HBM 事务，
最终 SID=4、rows=24576，几何容量为每 Stack 96 GiB、双 Stack 合计 192 GiB，
按 16Hi 折算为 48 Gibit/die。两颗 Stack 各自包含 32 个 Channel Controller/PHY，并共享各自的
MemoryImage；不是每个 Controller 独占一份完整 Stack 镜像。

该参数集是研究假定，不代表厂商数据，也不应写回标准主配置。9000/48Gb/16Hi 的 Timing、
能量和热参数没有厂家证据，`strict_timing_table` 保持 false。实际生效值以
`--dump-resolved-config` 导出的快照为准。配置不固化输出路径，D.4 和 D.5 的运行命令
分别指定各自产物位置，避免对照运行覆盖基线。

<a id="appendix-d-3"></a>

### D.3 配置审查和小规模预运行

```bash
mkdir -p outputs/hx9000/base
git rev-parse HEAD | tee outputs/hx9000/base/git_commit.txt
sha256sum experiments/local/hx_hbm4_9000_48gb_16hi.cfg \
  | tee outputs/hx9000/base/config.sha256

./build-clang-debug/hbm_sim \
  --config experiments/local/hx_hbm4_9000_48gb_16hi.cfg \
  --dump-resolved-config outputs/hx9000/base/resolved.cfg \
  --requests 128 --max-cycles 1000000 \
  --stats-json outputs/hx9000/base/smoke_stats.json \
  | tee outputs/hx9000/base/smoke_stats.txt

python3 tools/view_stats.py outputs/hx9000/base/smoke_stats.json \
  --keys run_status,completed_reads,completed_writes,simulation_time_ns,achieved_bw_GBps,avg_read_latency_ns,row_hit_pct,data_mismatches
```

预计：standard HBM4、9000 Mbps、48Gb、16Hi、2 stack、Behavioral/HBM PHY；128 个 host
请求全部完成，`remaining_*` 为 0，未命中 cycle limit，`cmd_validation`/`dfi_validation` 为
`pass`，data mismatch 为 0。任何一项不满足都应先修配置或实现，再运行 20000 请求。

<a id="appendix-d-4"></a>

### D.4 正式仿真命令和预计效果

```bash
./build-clang-debug/hbm_sim \
  --config experiments/local/hx_hbm4_9000_48gb_16hi.cfg \
  --dump-resolved-config outputs/hx9000/base/resolved.cfg \
  --cmd-trace outputs/hx9000/base/commands.csv \
  --response-trace outputs/hx9000/base/responses.csv \
  --dfi-trace outputs/hx9000/base/dfi.csv \
  --dfi-signal-trace outputs/hx9000/base/dfi_signal.csv \
  --dump-timing-table outputs/hx9000/base/timing.csv \
  --dump-memory-image 'outputs/hx9000/base/final_memory_{stack}.txt' \
  --dump-memory-csv 'outputs/hx9000/base/final_memory_{stack}.csv' \
  --mismatch-report outputs/hx9000/base/mismatch.txt \
  --dump-thermal-map 'outputs/hx9000/base/thermal_map_{stack}.txt' \
  \
  --stats-json outputs/hx9000/base/stats.json \
  | tee outputs/hx9000/base/stats.txt
```

预计效果不是预设某个“必须达到”的实测数，而是以下不变量和趋势：

- 2 个 stack、每 stack 32 个 channel-local Controller/PHY，报告末尾和 JSON `stacks` 给出每实例核心结果；
- 9000 Mbps × 2048 bit × 2 stack / 8000 对应约 4608 GB/s 聚合理论峰值；
- `achieved_bw_GBps` 小于理论峰值，且短/局部 workload 可能远低于峰值；
- CRC、RAS metadata、ECC 使 `achieved_if_bw_GBps` 高于 payload 带宽、效率低于 100%；
- random workload 产生 row hit/miss/conflict；普通 FR-FCFS 在 eligible 候选中先选 ready，再选先到者，不额外引入 row-hit-first 策略；
- Behavioral PHY 产生 HBM row/column、FIFO/pipeline/DFI 事件；
- 两个 stack 均应有完成量，是否均衡取决于 256B interleave 和地址分布；
- thermal peak 高于或等于 ambient，但绝对温度只代表当前研究参数。

<a id="appendix-d-5"></a>

### D.5 模式选择、命令和预期差异

D.5 不会继承 D.4 的运行状态：每条 `hbm_sim` 命令都是一个新进程，重新读取同一份
自包含 HX 配置，再由当前命令行覆盖要比较的一个变量。D.5.1–D.5.4 可单独执行；
D.5.5 内部必须按 sparse→mmap→chunk 的顺序执行，因为后两者使用 sparse 产物作 golden。
每个小节的同组对照内部固定 requests/seed/workload，只改动该组声明的变量；不同小节为激发
各自机制可以采用不同 requests 或 trace，不能跨组直接作单变量数值比较：

```bash
mkdir -p outputs/hx9000
```

#### D.5.1 PHY 模式

```bash
./build-clang-debug/hbm_sim --config experiments/local/hx_hbm4_9000_48gb_16hi.cfg \
  --requests 4096 --mem-phy behavioral \
  --stats-json outputs/hx9000/behavioral.json \
  | tee outputs/hx9000/behavioral.txt

./build-clang-debug/hbm_sim --config experiments/local/hx_hbm4_9000_48gb_16hi.cfg \
  --requests 4096 --mem-phy direct \
  --stats-json outputs/hx9000/direct.json \
  | tee outputs/hx9000/direct.txt
```

Behavioral 保留状态机、FIFO、2-cycle pipeline、adapter 和 DFI；Direct 是直接完成模式。
预计完成数据相同，但延迟、PHY 事件和可能的命令/数据反压不同。

#### D.5.2 调度器和行策略

```bash
./build-clang-debug/hbm_sim --config experiments/local/hx_hbm4_9000_48gb_16hi.cfg \
  --requests 4096 --scheduler fcfs --row-policy open_page \
  --stats-json outputs/hx9000/fcfs_open.json \
  | tee outputs/hx9000/fcfs_open.txt

./build-clang-debug/hbm_sim --config experiments/local/hx_hbm4_9000_48gb_16hi.cfg \
  --requests 4096 --scheduler frfcfs --row-policy open_page \
  --stats-json outputs/hx9000/frfcfs_open.json \
  | tee outputs/hx9000/frfcfs_open.txt

./build-clang-debug/hbm_sim --config experiments/local/hx_hbm4_9000_48gb_16hi.cfg \
  --requests 4096 --scheduler frfcfs --row-policy closed_page \
  --stats-json outputs/hx9000/frfcfs_closed.json \
  | tee outputs/hx9000/frfcfs_closed.txt
```

预计 open-page/FR-FCFS 在有局部性的 workload 下提高 row hit；closed-page 会增加自动关闭
和后续 ACT。random workload 下提升幅度可能有限，必须用相同 seed 比较。

具体归因时，先比较 `fcfs_open` 与 `frfcfs_open`（只改调度器），再比较
`frfcfs_closed` 与 `frfcfs_open`（只改行策略）。必须以实际的
`row_hits/misses/conflicts`、延迟和带宽为准，不预设 FR-FCFS 必然更快。

#### D.5.3 Stack 映射和 QoS（分开对照）

```bash
./build-clang-debug/hbm_sim --config experiments/local/hx_hbm4_9000_48gb_16hi.cfg \
  --requests 4096 --stack-mapping interleaved --stack-qos-policy strict_priority \
  --stats-json outputs/hx9000/interleaved_mapping.json \
  | tee outputs/hx9000/interleaved_mapping.txt

./build-clang-debug/hbm_sim --config experiments/local/hx_hbm4_9000_48gb_16hi.cfg \
  --requests 4096 --stack-mapping blocked --stack-qos-policy strict_priority \
  --stats-json outputs/hx9000/blocked_mapping.json \
  | tee outputs/hx9000/blocked_mapping.txt
```

预计 interleaved 更容易让两个 stack 同时活跃；blocked 是否均衡取决于地址是否跨越容量分区。
合成地址范围较小时，blocked 可能主要使用 stack 0，这是映射结果而非多 Stack 失效。

上述两次只改 Stack mapping，QoS policy 保持一致。默认 synthetic workload 的
`qos=0`，所以它们不能验证优先级重排。下面改用 `examples/qos_priority.trace`：
四个 HostRequest 声明相同 `inject_cycle`、目标均为 stack 0，QoS 分别为 0/2/7/4，
每拍只分发一个。response CSV 中的 `arrival_cycle` 是拆分后 transaction 实际进入 Controller 的拍，
不必都为 0；本对照判断的是最终 host ID 顺序。
这一对命令只改 QoS policy：

```bash
./build-clang-debug/hbm_sim --config experiments/local/hx_hbm4_9000_48gb_16hi.cfg \
  --trace examples/qos_priority.trace --requests 0 \
  --stack-mapping interleaved --stack-dispatch-width 1 --stack-qos-policy fcfs \
  --response-trace outputs/hx9000/qos_fcfs.csv \
  --stats-json outputs/hx9000/qos_fcfs.json \
  | tee outputs/hx9000/qos_fcfs.txt

./build-clang-debug/hbm_sim --config experiments/local/hx_hbm4_9000_48gb_16hi.cfg \
  --trace examples/qos_priority.trace --requests 0 \
  --stack-mapping interleaved --stack-dispatch-width 1 --stack-qos-policy strict_priority \
  --response-trace outputs/hx9000/qos_priority.csv \
  --stats-json outputs/hx9000/qos_priority.json \
  | tee outputs/hx9000/qos_priority.txt

cut -d, -f1,2,7,8,9 outputs/hx9000/qos_fcfs.csv
cut -d, -f1,2,7,8,9 outputs/hx9000/qos_priority.csv
```

FCFS 的 host response 顺序应为 request ID `0,1,2,3`；strict-priority 应按 QoS 从高到低为
`2,3,1,0`。`qos_priority_dispatches` 只计“已分发的 `qos>0` 请求”，不能单独证明发生了
重排；必须结合上述完成顺序。

#### D.5.4 存储后端

该研究用例固定为 2 Stack 且使用 `random` workload。下面把每 Stack 文件后端限制为
1 GiB，因此必须同时把聚合随机地址范围限制为 `2 × 1 GiB = 2147483648 B`。若省略该限制，
random 会按两颗 96 GiB 研究 Stack 的完整聚合容量取地址，并正确触发
`request address range exceeds DRAM capacity`。

`hx_backend_out` 必须是本次运行的新目录，下例用 `mktemp -d` 自动生成，避免
mmap/chunk 重开旧状态：

```bash
mkdir -p outputs/hx9000
hx_backend_out="$(mktemp -d outputs/hx9000/backend_XXXXXX)"
mkdir -p "$hx_backend_out"/{mmap,chunk}
printf 'output directory: %s\n' "$hx_backend_out"

./build-clang-debug/hbm_sim --config experiments/local/hx_hbm4_9000_48gb_16hi.cfg \
  --requests 4096 --random-address-space-bytes 2147483648 \
  --stack-mapping interleaved --memory-backend mmap_sparse \
  --memory-capacity-bytes 1073741824 \
  --memory-data-file "$hx_backend_out/mmap/data_{stack}.bin" \
  --dump-memory-image "$hx_backend_out/mmap/final_{stack}.txt" \
  \
  --stats-json "$hx_backend_out/mmap/stats.json" \
  | tee "$hx_backend_out/mmap/stats.txt"

./build-clang-debug/hbm_sim --config experiments/local/hx_hbm4_9000_48gb_16hi.cfg \
  --requests 4096 --random-address-space-bytes 2147483648 \
  --stack-mapping interleaved --memory-backend chunk_file \
  --memory-capacity-bytes 1073741824 \
  --memory-data-file "$hx_backend_out/chunk/data_{stack}.bin" \
  --memory-chunk-size 2097152 --memory-chunk-cache-entries 16 \
  --dump-memory-image "$hx_backend_out/chunk/final_{stack}.txt" \
  \
  --stats-json "$hx_backend_out/chunk/stats.json" \
  | tee "$hx_backend_out/chunk/stats.txt"
```

对照关键统计并逐 Stack 比较最终 payload：

```bash
: "${hx_backend_out:?run the preceding D.5.4 simulation block in this shell first}"
python3 tools/view_stats.py \
  "$hx_backend_out/mmap/stats.json" "$hx_backend_out/chunk/stats.json" \
  --labels mmap_sparse,chunk_file \
  --keys run_status,completed_reads,completed_writes,simulation_time_ns,achieved_bw_GBps,avg_read_latency_ns,row_hit_pct,data_mismatches

for stack in 0 1; do
  diff -u "$hx_backend_out/mmap/final_${stack}.txt" \
          "$hx_backend_out/chunk/final_${stack}.txt"
done
```

预期两个后端的完成数、周期、payload 和性能统计一致，均为 `remaining_requests=0`、
`hit_cycle_limit=false`、`data_mismatches=0`；每个 Stack 生成独立 data 和三类 sidecar。
这里的 1 GiB 是**每 Stack 的实验后端容量限制**，2 GiB 是本次 workload 的聚合随机地址范围，
二者都不是 48 Gb×16-Hi 研究组织的真实全容量声明。严格的 sparse/mmap/chunk golden、持久化
重开和相同 local 地址跨 Stack 隔离检查继续按下一节 D.5.5 执行。

#### D.5.5 双 Stack 三后端的人工查验

D.5.4 是正式 synthetic 性能运行；下面再用可重放数据 trace 做一次小容量语义审计。
该用例是双 Stack，因此 golden、导出和 backend 全部显式包含 `{stack}`：

```bash
mkdir -p outputs/hx9000
hx_audit_out="$(mktemp -d outputs/hx9000/backend_audit_XXXXXX)"
mkdir -p "$hx_audit_out"/{sparse,mmap,chunk}
printf 'output directory: %s\n' "$hx_audit_out"

./build-clang-debug/hbm_sim --config experiments/local/hx_hbm4_9000_48gb_16hi.cfg \
  --trace examples/data_check.trace --requests 0 \
  --memory-image examples/memory_image.txt \
  --memory-backend sparse --memory-capacity-bytes 1048576 \
  --dump-memory-image "$hx_audit_out/sparse/final_{stack}.txt" \
  --dump-memory-csv "$hx_audit_out/sparse/final_{stack}.csv" \
  \
  --stats-json "$hx_audit_out/sparse/stats.json" \
  | tee "$hx_audit_out/sparse/stats.txt"

./build-clang-debug/hbm_sim --config experiments/local/hx_hbm4_9000_48gb_16hi.cfg \
  --trace examples/data_check.trace --requests 0 \
  --memory-image examples/memory_image.txt \
  --memory-backend mmap_sparse --memory-capacity-bytes 1048576 \
  --memory-data-file "$hx_audit_out/mmap/data_{stack}.bin" \
  --dump-memory-image "$hx_audit_out/mmap/final_{stack}.txt" \
  --dump-memory-csv "$hx_audit_out/mmap/final_{stack}.csv" \
  --verify-golden "$hx_audit_out/sparse/final_{stack}.txt" \
  \
  --stats-json "$hx_audit_out/mmap/stats.json" \
  | tee "$hx_audit_out/mmap/stats.txt"

./build-clang-debug/hbm_sim --config experiments/local/hx_hbm4_9000_48gb_16hi.cfg \
  --trace examples/data_check.trace --requests 0 \
  --memory-image examples/memory_image.txt \
  --memory-backend chunk_file --memory-capacity-bytes 1048576 \
  --memory-data-file "$hx_audit_out/chunk/data_{stack}.bin" \
  --memory-chunk-size 65536 --memory-chunk-cache-entries 4 \
  --dump-memory-image "$hx_audit_out/chunk/final_{stack}.txt" \
  --dump-memory-csv "$hx_audit_out/chunk/final_{stack}.csv" \
  --verify-golden "$hx_audit_out/sparse/final_{stack}.txt" \
  \
  --stats-json "$hx_audit_out/chunk/stats.json" \
  | tee "$hx_audit_out/chunk/stats.txt"
```

然后遍历两个 stack 查一致性，并检查文件四件套：

```bash
: "${hx_audit_out:?run the preceding D.5.5 simulation block in this shell first}"
for stack_id in 0 1; do
  diff -u "$hx_audit_out/sparse/final_${stack_id}.txt" \
    "$hx_audit_out/mmap/final_${stack_id}.txt"
  diff -u "$hx_audit_out/sparse/final_${stack_id}.txt" \
    "$hx_audit_out/chunk/final_${stack_id}.txt"
done

ls -lh "$hx_audit_out"/{mmap,chunk}/data_*.bin*
stat --format='%n logical_bytes=%s allocated_blocks=%b block_bytes=%B' \
  "$hx_audit_out"/{mmap,chunk}/data_*.bin*
od -An -tc -N8 "$hx_audit_out/mmap/data_0.bin.meta"
od -An -tc -N8 "$hx_audit_out/chunk/data_0.bin.meta"
python3 tools/view_stats.py "$hx_audit_out"/{sparse,mmap,chunk}/stats.json \
  --keys memory_backend,stack_count,remaining_requests,hit_cycle_limit,data_mismatches,golden_verified,golden_mismatches
```

通过标准是两个 stack 的两组 `diff` 均无输出，文件后端每 stack 都有 data/
`.init`/`.meta`/`.present`，magic 正确，所有数据/golden mismatch 为 0。如果该小 trace
未触达 stack 1，其 final image 为空但三后端仍应一致；是否实际利用两个 stack 要以
D.5.3 的 random 工作量和 `stack_N_*` 统计另行验证。更完整的原始文件解读及重开
步骤见 [6.5.2–6.5.6](#section-6-5-2)。

<a id="appendix-d-6"></a>

### D.6 仿真过程查看

在另一个终端可以观察进程和产物增长：

```bash
# 查找正在运行的仿真进程和 CPU/内存占用
pgrep -af 'build-clang-debug/hbm_sim'
ps -o pid,etime,%cpu,%mem,rss,cmd -C hbm_sim

# 每秒观察 trace/统计文件大小；Ctrl+C 退出 watch
watch -n 1 'ls -lh outputs/hx9000/base/commands.csv outputs/hx9000/base/dfi.csv outputs/hx9000/base/stats.txt 2>/dev/null'

# command CSV 若被运行时刷新，可查看最新事件；缓冲时会分批出现
tail -n 10 -f outputs/hx9000/base/commands.csv
```

预期：进程 CPU 时间持续增加，command/DFI 文件随事件写出而增长；stdout 统计主要在仿真结束
时完整出现。如果文件暂时不增长，可能是 stdio 缓冲，不能仅据此判断死锁；应结合进程 CPU、
`max_cycles`、小规模复现和调试器判断。

<a id="appendix-d-7"></a>

### D.7 核心结果和按需参数查看

本节不是新的仿真步骤，而是读取 D.4 基线运行生成的
`outputs/hx9000/base/` 产物；应先完整执行 D.4。若使用了不同的输出目录，下面所有路径也要
同步替换。

```bash
# 需要重新查看时，直接阅读已保存的精简报告（含模型、参数、结果和 Stack 表）
less -S outputs/hx9000/base/stats.txt

# 可选：将性能指标整理成一张小表
python3 tools/view_stats.py outputs/hx9000/base/stats.json \
  --keys run_status,simulation_time_ns,achieved_bw_GBps,avg_read_latency_ns,row_hit_pct

# 最终 timing 表和关键研究默认值
sed -n '1,30p' outputs/hx9000/base/timing.csv
rg 'n(CL|CWL|RCDRD|RCDWR|RP|RAS|RC|FAW|RTW|WTR|RFC|RFM)' \
  outputs/hx9000/base/timing.csv
```

查看命令和请求过程：

```bash
# CSV 表头、前后事件和总行数
sed -n '1,10p' outputs/hx9000/base/commands.csv
tail -n 10 outputs/hx9000/base/commands.csv
wc -l outputs/hx9000/base/commands.csv outputs/hx9000/base/dfi.csv \
  outputs/hx9000/base/responses.csv

# 找第一个 refresh/RFM、写命令和 stack 1 事件
rg -m 5 ',(REF|RFM)' outputs/hx9000/base/commands.csv
rg -m 5 ',(WR|WRA),' outputs/hx9000/base/commands.csv
awk -F, 'NR==1 {print; next} $5==1 {print; if (++seen==5) exit}' \
  outputs/hx9000/base/commands.csv

# 验证和数据错误
python3 tools/view_stats.py outputs/hx9000/base/stats.json \
  --keys cmd_validation,dfi_validation,data_mismatches
sed -n '1,30p' outputs/hx9000/base/mismatch.txt
```

预计：stats.txt 仅含精简报告，stats.json 含公共结果和变化清单，内部诊断字段另见[输出指南](文档/输出指南.md#full-fields)。
全部生效参数看 D.4 导出的 resolved.cfg；需要内部计数时按 7.1.2 做独立专项诊断。
timing CSV 能看到最终 nCK 和研究来源；command
CSV 能把 request 追到 stack/channel/PC/SID/BG/bank/row；两项 validator 为 `pass` 且非故障注入时
data mismatch 为 0。

<a id="appendix-d-8"></a>

### D.8 可视化运行和分析

D.8.2 使用 D.4 生成的 command、DFI、stats 和 thermal 文件，因此必须先完成 D.4。
D.8.1 只是额外生成一条“项目标准 HBM4”性能基线，可独立选择是否执行；它不会继承、
也不会替代 D.4 的 HX9000 仿真。

#### D.8.1 生成性能基线（可选）

```bash
python3 tools/performance_curve.py \
  --binary ./build-clang-debug/hbm_sim --standards hbm4 \
  --intervals 1,2,4,8,16,32 --read-ratios 50,70,100 \
  --requests 1024 --pattern random --seed 900048 \
  --csv-out outputs/hx9000/base/performance_standard_hbm4.csv \
  --json-out outputs/hx9000/base/performance_standard_hbm4.json
```

注意：这是项目标准 HBM4 基线曲线，不自动加载 HX9000 自定义配置，dashboard 中应标注为
standard baseline。若要得到 HX9000 精确 sweep，需要扩展脚本使其传入该 config。

#### D.8.2 生成并打开 dashboard

```bash
python3 tools/visualize.py \
  --command-trace outputs/hx9000/base/commands.csv \
  --dfi-trace outputs/hx9000/base/dfi.csv \
  --stats outputs/hx9000/base/stats.json \
  --performance-json outputs/hx9000/base/performance_standard_hbm4.json \
  --thermal-map outputs/hx9000/base/thermal_map_0.txt \
  --thermal-map outputs/hx9000/base/thermal_map_1.txt \
  --title 'Hypothetical HX-HBM4-9000 48Gb 16Hi / 2-stack' \
  --max-events 100000 \
  --out outputs/hx9000/base/dashboard.html

xdg-open outputs/hx9000/base/dashboard.html
```

若跳过可选的 D.8.1，在 D.8.2 命令中同时删去
`--performance-json outputs/hx9000/base/performance_standard_hbm4.json`；其余面板仍可正常生成。

#### D.8.3 预计可视化效果

- Run summary：显示 HBM4、Behavioral、完成量、带宽/延迟和两项 `pass`；2 Stack、9000 Mbps 等详细组织参数在同目录 stats/resolved 中核对；
- Overview：命令密度随 random workload 展开，初始化/训练后开始普通访问；
- Bank view：可看到 HBM 行/列命令、edge pairing、ACT→RD/WR→PRE 和 refresh/RFM；
- Request swimlanes：64B host line 拆分的两个 32B transaction 命令可按 request 关联；
- Stack filter：stack 0/1 都应有事件，数量接近与否由地址和 interleave 决定；
- DFI activity：Behavioral PHY 的 command、write/read beat 非空；
- Performance curve：显示标准 HBM4 基线的饱和趋势，不能冒充 HX9000 自定义 sweep；
- Thermal map：两份 thermal map 均已传入，可用 Stack 下拉切换 stack 0/1，并在各 layer 找热点；温度是研究参数下的趋势而非真实结温。

发现异常时按[第 7.3.7 节](#section-7-3-7)检查原始 CSV、范围和过滤器。

<a id="appendix-d-9"></a>

### D.9 结果记录模板

普通报告记录下列核心信息即可，详细配置由 resolved.cfg、机器结果由 stats.json 附带，
不需要把每个内部计数再抄一遍：

```text
用例名称：HX-HBM4-9000-48G-16Hi 双 Stack 研究用例
研究问题：
代码 commit：
构建器/构建类型：
CTest 结果：
自包含配置：experiments/local/hx_hbm4_9000_48gb_16hi.cfg（复制 configs/hbm.cfg 后在 [override] 记录全部型号差异）
配置 SHA256：
standard/profile/vendor/speed-bin：
参数来源分级：JEDEC / external / derived / research_default
完整命令：
随机种子：900048
输入 trace/image：synthetic random / 无初始 image
host_requests：
dram_transactions：
completed_reads/writes：
remaining_requests/pending：
hit_cycle_limit：
cmd_validation：
dfi_validation：
data_mismatches：
理论峰值与计算公式：
achieved_bw_GBps：
avg_read_latency_ns：
row_hit_pct：
per-stack 负载均衡：
可选专题结论（只填本次研究的 PHY/功耗热/后端/可视化问题）：
已知限制：假定器件，未进行厂商/电气/硬件校准
产物目录：outputs/hx9000/base
验收人/日期：
```

一个合格的自定义用例至少应满足：所有请求完成、未命中 cycle limit、命令/DFI 验证通过、
无非预期数据 mismatch、关键参数来源明确、理论峰值口径正确、模式差异可解释，并可由固定
配置、seed、commit 和命令重放。只有获得真实厂家或硬件证据并完成误差评估后，才能把
`hypothetical_hx_research` 改为真实 vendor identity。

---

<a id="appendix-e"></a>

## 附录 E：完整检查清单

- [ ] Clang 和 G++ 分别在独立目录构建成功；
- [ ] `ctest` 当前全部通过并保存日志；
- [ ] 四标准主配置均完成且未命中 `max_cycles`；
- [ ] 单 stack 与多 stack 的路由、容量和统计聚合检查通过；
- [ ] Direct/Behavioral 对照差异可解释；
- [ ] sparse/mmap_sparse/chunk_file 的最终 payload 一致；
- [ ] command 和 DFI validator 通过；
- [ ] 写后读、mask、expect、golden、mismatch 退出码通过；
- [ ] Timing `t-1/t` 边界矩阵通过；
- [ ] 性能曲线呈现合理饱和趋势；
- [ ] 功耗/热结果标明是否器件级校准；
- [ ] 外部差分记录参考版本、配置、规范化和 unsupported 项；
- [ ] dashboard 可离线打开，关键视图非空；
- [ ] 最终参数表、命令、commit、输入和产物完整归档。

---

<a id="appendix-f"></a>

## 附录 F：代码与文档索引

| 内容 | 位置 |
|---|---|
| 公共头文件 | `include/hbm_sim/` |
| CLI 与参数解析 | `src/cli/` |
| 系统/存储/运行核心 | `src/core/` |
| Controller | `src/controller/` |
| DRAM traits/profile/semantics | `src/dram/` |
| Mem PHY | `src/dram/mem_phy.cpp`、`include/hbm_sim/dram/mem_phy.hpp` |
| Frontend/trace | `src/frontend/` |
| Stats | `src/stats/` |
| 验证器 | `src/validation/`、`tests/`、`tools/` |
| 可运行配置 | `configs/hbm.cfg`、`configs/lpddr.cfg` |
| 参数集/来源索引 | 主配置、验证配置中的命名 preset、自包含研究配置、`configs/profile_index.csv` |
| 验证配置与元数据 | 可执行口径在 `configs/validation/*.cfg` 及其 preset；`configs/validation/*.csv` 为来源/能力元数据 |
| 示例输入 | `examples/` |
| 持久输出 | `outputs/` |
| 构建入口 | `CMakeLists.txt`、`Makefile` |

本手册不复制 JEDEC 或厂商保密手册。引用外部标准时应由使用方另行保存合法获得的版本、
章节和校准记录。
